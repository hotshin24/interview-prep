# 웹 퍼블리셔 면접 답변집 — 전 문항 개별 답변 완성본

> **편집 원칙**
> - 1번부터 286번까지 모든 번호를 각각 독립된 질문으로 분리했습니다.
> - 원문의 프로젝트 수치·기술 사실·구현 이력은 유지하고, 답변 문장은 실제 면접에서 말하기 쉬운 한국어로 다듬었습니다.
> - 여러 번호가 한 문항으로 묶여 있던 항목도 각각의 질문과 답변으로 나눴습니다.
> - `[ ]` 표시는 실제 본인 경험이나 지원 회사에 맞게 직접 채워야 하는 부분입니다.

---

**대상자** 신호진 · 커머스/상품기획 경력 13년 → 웹 퍼블리셔 전환
**짝 문서** 「신호진 웹퍼블리셔 면접 예상질문」 (문항 번호 동일)
**작성일** 2026-08-17

---

## 이 문서 사용법

| 표기 | 뜻 |
| --- | --- |
| `[ ]` | **본인이 직접 채워야 하는 부분.** 실제 경험에 맞게 정리해 두는 것이 좋습니다 |
| **A.** | 답변 예시 — 그대로 외우기보다 본인 말투에 맞게 익혀두기 |
| ▸ | 꼬리질문 대비 / 추가 설명 |

**모든 답변의 뼈대는 3단입니다.**
`① 무엇을 했는가(수치)` → `② 왜 그렇게 했는가(대안 검토·트레이드오프)` → `③ 지금이라면(개선안)`
미완성되거나 부족했던 항목일수록 ③의 개선 방향을 구체적으로 설명하는 것이 중요합니다.

---

# 01. 개인 신상 · 인성

### 1. 자기소개 (60초판)

**A.** 13년 동안 커머스 분야에서 상품기획과 온라인몰 운영을 해왔고, 지금은 그 경험을 바탕으로 웹 화면을 직접 구현하는 일을 배우고 있습니다.
신발 상품기획으로 시작해 온라인몰 운영과 팀 리딩, 건설사 입찰 문서 편집디자인까지 했습니다. 분야는 달랐지만 모두 **사용자가 정보를 보고 판단할 수 있도록 내용을 구성하는 일**이었다는 공통점이 있습니다.
올해 웹 퍼블리싱 과정을 마친 뒤 두 개의 프로젝트를 진행했습니다. JW Anderson 프로젝트에서는 392개의 정적 페이지를 직접 마크업했고, 리팩터링을 통해 CSS를 약 42% 줄였습니다. HOOT UP에서는 164페이지 구현에 AI를 활용하되, 결과물은 제가 직접 검수하면서 359건의 수정·보완 사항을 관리했습니다.
두 프로젝트를 통해 **직접 구현하는 역량과 AI가 만든 결과물을 검증하고 품질을 관리하는 역량을 각각 보여주고자 했습니다.**

### 1-b. 자기소개 (3분판)

위 60초판 + 다음 3단락:

**A.** ① **왜 퍼블리셔인가** — [본인 계기]. 13년 동안 주로 화면 제작을 요청하는 입장에서 일했습니다. 그 과정에서 요구사항이 정확히 전달되지 않아 수정이 반복되는 경험이 많았고, 직접 구현할 수 있으면 협업 과정도 더 잘 이해할 수 있겠다는 생각으로 퍼블리싱을 배우기 시작했습니다.
② **무엇을 만들었나** — JWA는 페이지 수가 많아져도 CSS 구조를 안정적으로 유지할 수 있는지 직접 확인해보고 싶어 392페이지 규모로 제작했습니다. 15,208줄을 8,812줄로 줄이면서 회귀 0건으로 마감했습니다. HOOT UP은 AI를 활용한 구현 과정에서 사람이 어떤 기준으로 결과물을 검수하고 보완해야 하는지를 경험해보기 위해 진행했습니다. 예를 들어 AI가 “16개 생성 완료”라고 보고했지만 실제 파일은 14개였던 사례가 있었고, 이런 차이를 직접 확인하고 바로잡는 과정이 중요했습니다.
③ **무엇이 남았나** — 두 프로젝트 모두 PRD에 **제가 직접 "미달"이라고 적어둔 항목**이 있습니다. JWA는 의미 토큰 참조 0회와 메가 메뉴 키보드 조작이, HOOT UP은 하드코딩 색상 26건이 남아 있습니다.
그리고 적어둔 것 중 상당수는 **적어뒀기 때문에 고칠 수 있었습니다.** `:focus-visible`, `<main>` 랜드마크 2/392, 컬러 키워드 19건, 이미지 962.8MB는 미달로 기록해둔 뒤 되돌아가 해소했습니다.

### 2. 본인을 한 문장으로

**A.** 한 문장으로 말씀드리면, **결과를 감으로 넘기기보다 직접 확인하는 사람**입니다. 작업이 끝났다고 판단하기 전에 파일 수나 코드량처럼 확인 가능한 부분은 실제 수치로 점검하는 편입니다. HOOT UP에서 정리한 359건의 검수 사항 중에도 화면만 봐서는 알기 어려운 파일 개수나 원본 데이터 대조 과정에서 발견한 문제가 많았습니다.

### 3. 지원 동기 / 회사를 알게 된 경로

**A.** [지원 회사의 구체적 사실 1개 — 서비스 특성 / 최근 리뉴얼 / 채용공고의 특정 문구]를 보고 지원했습니다.
공고에서 [예: "대규모 정적 페이지 유지보수" / "접근성 인증 대응" / "디자인 시스템 구축"] 항목이 제가 두 프로젝트에서 실제로 다룬 문제와 정확히 겹칩니다.
▸ **준비할 것**: 회사 사이트를 개발자 도구로 열어보고 구체적 관찰 1개를 반드시 준비. 막연하게 “잘 만들었다”는 표현보다 구체적으로 무엇을 봤는지 설명하는 편이 좋습니다.

### 4. 저희 서비스를 써보셨나요 / 개선점

**A.** [실제 사용 경험]. 개선점은 제 직무 범위 안에서 말씀드리겠습니다.
[예시 프레임] "[페이지]에서 [구체적 요소]를 키보드 탭으로 이동해봤는데 포커스 표시가 없었습니다. `:focus-visible`만 추가해도 해결되는 부분입니다."
▸ **주의**: 디자인 취향 비판 금지. 측정 가능한 것(접근성·성능·마크업)만. 겸손한 어투 필수 — "제가 못 본 이유가 있을 수 있습니다만".

### 5. 부천 거주 · 출퇴근

**A.** 부천 거주이고 [회사 위치]까지 [실측 시간]입니다. [경로]로 이동합니다. 출퇴근은 문제 없습니다.
▸ 1시간 넘으면 먼저 인정하고 대책 제시: "[N]분이라 이른 출근으로 러시를 피하겠습니다" 또는 "장기적으로 이사도 고려하고 있습니다."

### 6. 야근·주말 근무

**A.** 필요하면 합니다. 다만 일정이 예측 가능한 형태이길 바랍니다. 두 프로젝트 모두 새벽 작업으로 완성했고, 몰입해야 할 때 시간을 쓰는 것에는 익숙합니다.
▸ 무조건 가능하다고 답하기보다는 현실적으로 대응 가능한 범위를 차분하게 설명하는 편이 좋습니다.

### 7. 희망 연봉 🔥

**A.** [회사 규모별 시세를 미리 조사한 뒤] "저는 실무 경력이 없는 상태로 지원하는 것이므로 회사의 신입 기준을 따르겠습니다. 다만 13년의 사회 경험이 있어 최소 생계 기준은 [금액]입니다."
▸ **원칙**: 먼저 숫자를 던지지 말 것. "회사 기준이 어떻게 되나요?"로 되묻기 → 그래도 물으면 범위로 답변.
▸ 준비물: 사람인/잡코리아에서 해당 회사·직무 연봉 데이터를 미리 확인.

### 8. 이전 연봉보다 낮아질 수 있다 🔥

**A.** 연봉이 낮아질 수 있다는 점까지 고려하고 결정한 전직입니다. 13년 커리어를 정리하고 다시 시작하는 것이므로 연봉이 낮아지는 것은 예상한 조건입니다.
대신 제가 더 중요하게 보는 것은 **입사 후 성장할 수 있는 경로**입니다. 3년 뒤 어떤 역할까지 갈 수 있는 자리인지가 지금 연봉보다 중요합니다.

### 9. 입사 가능 시기

**A.** 즉시 가능합니다.

### 10. 다른 곳 지원 중인가

**A.** 네, 몇 곳 진행 중입니다. 솔직히 말씀드리면 [N]곳 지원했고 [현재 상황]입니다.
▸ **원칙**: 거짓말 금지(레퍼런스 체크). "여기가 1지망"은 근거 없이 말하면 역효과 — 근거를 붙여서.

### 11. 스트레스 해소법

**A.** [실제 방법]. 전시나 카페를 다니며 시각적인 것을 보는 편입니다. 좋은 편집 디자인이나 공간을 보면 아이디어가 정리됩니다.
▸ 취미가 직무 감각과 연결되는 답이 유리합니다.

### 12. 취미

**A.** 전시 관람과 카페 탐방입니다. 여백과 타이포그래피를 보는 습관이 JW Anderson 프로젝트에서 실제로 쓰였습니다. PRD에 "럭셔리 커머스는 여백과 타이포그래피가 곧 브랜드 자산이라 일반 커머스 UI 패턴을 그대로 적용할 수 없다"고 적었는데, 그 판단의 근거가 그동안 본 것들입니다.

### 13. 마지막으로 하고 싶은 말

**A.** 제 포트폴리오에는 "미달"이라고 제가 직접 적어둔 항목이 있습니다. 숨길 수 있었지만 남겼습니다.
**측정하지 않으면 미달인 줄도 모르기 때문입니다.** 실무에서도 같은 방식으로 일하겠습니다. 감사합니다.

---

# 02. 커리어 전환 검증 🔥

> 이 섹션에서는 커리어 전환이 이전 경력을 버린 선택이 아니라, 기존 경험을 퍼블리싱으로 연결한 과정이라는 점이 자연스럽게 드러나면 좋습니다.

### 14. 왜 13년 커머스에서 웹 퍼블리셔인가 🔥

**A.** 13년 동안 저는 계속 **화면을 요청하는 쪽**에 있었습니다. 상품기획에서는 상세페이지를, 온라인몰 운영에서는 기획전 페이지를, 편집디자인에서는 입찰 문서 레이아웃을 다뤘습니다.
공통점은 **보는 사람이 판단하게 만드는 일**이었다는 것이고, 차이는 마지막 구현을 늘 다른 사람에게 맡겼다는 것입니다. 요청이 정확하지 않아 왕복이 길어지는 경험이 반복됐습니다.
직접 구현 역량이 있으면 요구사항을 더 정확하게 이해하고 전달할 수 있고, 불필요한 수정 과정도 줄일 수 있다고 생각했습니다. 그리고 [본인 계기]를 계기로 실제로 배우기 시작했습니다.

▸ **증거로 연결**: "이 전직이 즉흥적이지 않다는 증거가 JWA PRD 1.1입니다. 럭셔리 커머스의 요구 조건이 일반 커머스와 어떻게 다른지 분석한 문단인데, 오랜 커머스 경험이 있었기 때문에 비교적 구체적으로 정리할 수 있었던 부분이라고 생각합니다."

### 15. 왜 하필 지금, 43세에 🔥

**A.** 다른 지원자보다 늦게 시작했다는 점은 알고 있습니다. 그래서 포트폴리오와 학습 과정에서 그만큼 더 구체적인 근거를 보여드리려고 했습니다.
[개인 계기 — 예: 회사 상황 / 커리어 한계 인식 / 특정 사건]
그리고 지금이 오히려 진입 가능한 시점이라고 판단했습니다. AI 도구 때문에 **코드를 생산하는 속도**보다 **생산된 것을 판단하는 능력**의 가치가 올라가고 있습니다. 판단은 경험에서 나옵니다. 그 부분이 제가 기존 경력에서 가져올 수 있는 강점이라고 생각합니다.

### 16. 팀장이 본인보다 어릴 수 있다 🔥

**A.** 저는 신입으로 들어갑니다. 나이가 아니라 역할로 일하겠습니다.
온라인몰에서 팀을 이끌어봤기 때문에 **리더가 나이 많은 팀원 때문에 겪는 불편**이 무엇인지 압니다. 지시를 받는 자리에서 나이를 꺼내는 순간 팀이 불편해진다는 것도 압니다. 나이보다 역할과 협업 기준에 맞춰 일하는 것이 중요하다고 생각합니다.
▸ 이 답은 **짧게, 단호하게.** 길게 설명할수록 방어적으로 들립니다.

### 17. 상품기획 경력이 퍼블리싱에 실제로 도움이 되나

**A.** 세 가지에서 구체적으로 작동했습니다.

| 경력 | 퍼블리싱에서 실제 작동한 지점 |
| --- | --- |
| 상품기획 | JWA PRD의 IA 설계 — 카테고리 진입 최대 2 depth, 모든 서브 카테고리에서 SEE ALL로 회귀. 커머스 이탈 지점을 알기 때문에 나온 규칙 |
| 온라인몰 운영 | HOOT UP의 **비즈니스 규칙 단위 검증**. 개별 데이터가 아니라 "개발&IT=단건 / 디자인=구독+단건 / 나머지 5분야=구독 전용"이라는 과금 규칙을 정의하고 여러 페이지가 이를 위반하는지 교차 확인 |
| 편집디자인 | 여백·타이포 판단. JWA의 브레이크포인트를 5구간으로 나눈 것도 1280–1918에서 그리드가 무너지는 것을 눈으로 잡아낸 결과 |

그중에서도 두 번째 경험이 가장 직접적으로 도움이 됐습니다. AI가 "왕초보 엑셀 데이터 정리 실무"라는 그럴듯한 강의명을 지어냈는데, 카탈로그 152종과 대조해서 잡았습니다. 화면만 확인해서는 발견하기 어려운 문제였습니다.

### 18. 다시 커머스로 돌아갈 생각은 🔥

**A.** 현재는 웹 퍼블리싱 쪽으로 커리어를 전환하는 데 집중하고 있습니다. 이전 경력을 가볍게 정리한 것은 아니고, 충분히 고민한 뒤 결정했습니다.
그리고 제가 하는 일이 커머스와 무관하지도 않습니다. 두 포트폴리오 다 커머스입니다. 장기적으로는 커머스 경험을 이해하는 퍼블리셔라는 강점을 살리고 싶습니다.

### 19. 수료 후 어떻게 학습을 이어갔나

**A.** 7월 8일 수료 직후 바로 JWA에 착수했습니다. 학원에서는 단일 페이지 단위로 배웁니다. 실무는 페이지가 수백 개일 때 무너지지 않는가의 문제라고 판단해서, 일부러 392페이지 규모로 잡았습니다.
학습 방식은 **만들고 → 측정하고 → 문서화**였습니다. 만든 것을 PRD와 기능정의서로 되돌려 쓰면서, 제가 왜 그렇게 했는지를 언어화했습니다. 작성한 코드는 제가 직접 설명할 수 있을 정도로 이해하려고 했습니다.

### 20. 실제 작업 기간과 하루 투입 시간

**A.** 작업 기간은 커밋 이력을 기준으로 말씀드릴 수 있습니다.

| 프로젝트 | 기간 | 커밋 |
| --- | --- | --- |
| JW Anderson | 7/19 ~ 7/27 (9일) | 105건 |
| HOOT UP | 7/29 ~ 8/15 | 377건 |

JWA 9일에 392페이지는 페이지를 새로 그린 게 아니라 **컴포넌트를 확정하고 반복 적용한** 구조입니다. 카드 컴포넌트 하나가 리스트·위시리스트에서 공유됩니다.
하루 투입은 [실제 시간]입니다.
▸ **꼬리질문 대비**: "9일에 392페이지가 가능한가?" → "페이지 수보다 CSS 총량이 실제 작업량입니다. 리팩터링에만 7/22~7/26 5일이 들어갔습니다."

### 21. 독학의 한계를 느낀 지점

**A.** 두 가지였습니다.
① **검증 환경의 한계** — 로컬 도구의 판정을 최종으로 믿으면 안 된다는 걸 두 번 겪었습니다. 로컬 axe가 어두운 배경의 대비 회귀를 통과로 판정했는데 배포본 Lighthouse에서는 위반 45건이 나왔습니다. 그래서 배포본과 실기기로 다시 확인하는 절차를 넣었습니다.
▸ *(미리보기 창 폭 오보고 건은 62번 참조 — 도구를 특정하지 못하면 꺼내지 마세요.)*
② **리뷰어 부재** — 혼자 진행한 프로젝트라 제 판단을 바로 피드백해줄 사람이 없었습니다. 그래서 가능한 부분은 검증 도구를 활용해 보완했습니다. W3C validator, Lighthouse, axe로 돌리면 제 눈이 놓친 것이 나옵니다. HOOT UP 첫 지적 항목이 메타 description 끝의 `)` 오류였는데, 육안으로는 정상이고 validator로만 잡히는 것이었습니다.
▸ **여기서 지원 이유로 연결**: "그래서 팀에서 리뷰받으며 일하고 싶습니다."

### 22. 팀 경험

**A.** 온라인몰 운영에서 팀을 이끌었습니다. [인원/기간/업무]. 상품 등록부터 기획전 운영까지 나눠 맡는 구조였고, 제가 배운 것은 **내 기준을 남에게 설명 가능한 형태로 만들어야 한다**는 것이었습니다.
HOOT UP에서 AI에게 지시할 때 제약 조건을 함께 넣는 습관도 여기서 나왔습니다. "1.3배 확대 액션 추가"만 지시하면 그리드가 흔들립니다. "단, 감싼 레이아웃은 항상 같은 크기여야 함"까지 붙여야 의도대로 나옵니다.

### 23. 디자인과 퍼블리싱 중 어디로

**A.** 퍼블리싱 쪽으로 가고 싶습니다. 디자인 경험은 시안을 빠르게 이해하고 디자이너와 소통하는 데 활용하고 싶습니다.
Figma 시안을 노드 ID 단위로 지정해서 작업했고, 시안에 없는 상태(호버·포커스·에러)를 먼저 질문하는 습관이 있습니다. 디자이너와 의견이 다를 때 단순히 맞고 틀림을 따지기보다, 시안에 빠진 상태나 구현상 필요한 부분을 먼저 발견하고 제안할 수 있는 퍼블리셔가 되고 싶습니다.

### 24. 워드프레스 1년 경험

**A.** 회사 사이트를 구축하고 운영했습니다. [구체 업무 — 테마 커스터마이징 / 페이지 추가 / 콘텐츠 관리].
배운 것은 **CMS 위에서 퍼블리싱한다는 게 어떤 제약인가**입니다. 내가 짠 마크업이 그대로 가지 않고 템플릿 규칙 안에서만 살아남습니다.
그리고 이게 HOOT UP ADR-03과 연결됩니다. PDP를 152종 만들지 않고 1종으로 수렴시키면서 "실제 서비스라면 이 자리에 템플릿 엔진이나 CMS가 들어간다"고 적었는데, 워드프레스 경험이 있어서 나온 판단입니다.

### 25. 패스트캠퍼스 디지털 마케팅 과정

**A.** 2018년 13주 과정이었습니다. 온라인몰을 운영하면서 광고 성과를 해석하지 못하는 게 답답해서 들었습니다.
지금 직무와의 연결점은 **전환 경로를 화면 구조로 보는 시각**입니다. HOOT UP PDP를 스펙 나열이 아니라 "고민 → 보장 → 커리큘럼 → 사회적 증거 → 멘토 신뢰"라는 설득 내러티브로 구성한 것이 그 결과입니다.

### 26. 5년 뒤 / 프론트엔드 전환 의사 🔥

> **주의**: 퍼블리셔 채용 면접에서는 프론트엔드 전환 자체보다, 퍼블리싱 역량을 어떻게 확장하고 싶은지 중심으로 설명하는 편이 자연스럽습니다.

**A.** 5년 뒤에는 **퍼블리싱 품질 기준을 정리하고 팀과 공유할 수 있는 사람**이 되고 싶습니다.
디자인 시스템의 토큰 체계를 설계하고, 접근성 기준을 CI에 붙이고, 팀의 마크업 컨벤션을 문서로 남기는 역할입니다. 지금 만든 포트폴리오도 그런 방향을 의식해서 구성했습니다 — PRD·기능정의서·UI/UX 설명서를 직접 쓴 이유가 그것입니다.
JS와 React도 계속 보고 있습니다. 다만 그건 **퍼블리셔로서 개발자와 같은 언어로 말하기 위해서**이지 직무를 바꾸려는 게 아닙니다.

---

# 03. 포트폴리오 전반

### 27. 두 프로젝트 소개 (각 90초)

**JW Anderson —** 럭셔리 패션 브랜드 커머스를 레퍼런스로, 392개 정적 페이지의 전체 커머스 플로우를 반응형으로 퍼블리싱했습니다. HTML과 CSS는 직접 작성했습니다.
단순히 화면을 비슷하게 재현하는 것보다, **페이지 수가 많아져도 CSS 구조를 안정적으로 관리할 수 있는지 확인하는 것**을 더 중요하게 봤습니다. CSS를 15,208줄에서 8,812줄로 42.1% 줄이면서 회귀 0건으로 마감했고, 규칙 수로도 2,033룰에서 1,184룰로 41.8% 줄었습니다. hex·rgb 하드코딩 0건, 이미지 13,502개 alt 100% 지정입니다.

**HOOT UP —** 이러닝 플랫폼 164페이지입니다. 차이는 **구현을 AI에게 위임했다**는 것입니다.
Figma 디자인 시스템과 사전 PRD를 먼저 확정한 뒤 Claude Code에 구현을 맡기고, 제가 전 산출물을 검수했습니다. 누적 지적 359건, 확인된 결함 10건을 모두 수정했고, 계획 대비 변경 16건을 전부 ID로 추적했습니다. Lighthouse 모바일 접근성 100, 성능 93입니다.
JWA가 직접 구현하는 역량을 보여주는 프로젝트라면, HOOT UP은 AI를 활용한 구현 결과를 검증하고 품질을 관리하는 역량을 보여주는 프로젝트입니다.

### 28. 왜 두 개인가

**A.** 두 프로젝트에서 보여주고 싶은 역량이 달랐기 때문입니다. 하나의 프로젝트만으로는 직접 구현 능력과 검수·품질관리 역량을 모두 충분히 보여주기 어렵다고 생각했습니다.
JWA만 있으면 "AI 시대에 손코딩만 하는 사람"으로 보입니다. HOOT UP만 있으면 **"코드는 AI가 짠 거 아니냐"**는 질문에 답할 수 없습니다.
두 프로젝트가 서로 다른 부분을 보완해준다고 생각합니다. 손으로 392페이지를 짜봤기 때문에 AI 산출물의 어디가 틀렸는지 보입니다.

### 29. 하나만 보여준다면

**A.** 하나만 고른다면 **HOOT UP**을 선택하겠습니다. 다만 화면뿐 아니라 README에 정리한 검수 기록까지 함께 보여드리고 싶습니다.
화면 결과만으로는 작업 과정에서 어떤 판단을 했는지 충분히 보여주기 어렵다고 생각합니다. "16개 생성 완료"라는 보고를 받고 파일을 세어서 14개인 걸 잡아낸 기록은 흔치 않습니다. 특히 이런 검수와 확인 과정이 실무에서도 제가 기여할 수 있는 부분이라고 생각합니다.
▸ 만약 **손코딩 검증이 목적인 회사**라면 JWA로 답을 바꾸세요. 회사 성격에 맞춰 준비할 것.

### 30. 실존 브랜드 클론 · 저작권 🔥

**A.** 학습용 비상업 포트폴리오임을 명시했습니다. 푸터에 국문·영문으로 고지했고, 사이트 타이틀에도 표기했습니다. PRD 제약사항에도 "실제 브랜드 이미지 사용에 따른 비상업적·학습용 명시 의무"를 적어뒀습니다.
다만 **명시가 면책은 아니라는 것도 알고 있습니다.** 그래서 두 번째 프로젝트인 HOOT UP은 처음부터 가상 브랜드로 설계했고, 강의·후기·뉴스 데이터를 전량 창작했습니다.
▸ **답변 포인트**: "몰랐다"가 아니라 "알았고, 다음 프로젝트에서 구조적으로 바꿨다".

▸ **그리고 "클론"이라는 표현부터 정정하고 싶습니다.** 레퍼런스로 삼았을 뿐 그대로 베끼지 않았습니다. UI/UX 설명서 70개 항목 중 **원본과 의도적으로 다르게 설계한 것**이 상당수입니다.

| 항목 | 원본 JWA | 이 프로젝트 |
| --- | --- | --- |
| Collaboration | 서브 메뉴 | **최상위 카테고리로 격상** |
| Stores 링크 | 푸터 | **헤더 최상단** (O2O 동선) |
| 홈 시작 | 상품 그리드 | **브랜드 서사 히어로**(The Loafer Bag) |
| 신상 편성 | 성별 혼합 나열 | **Men's / Women's 분리 섹션** |
| Accessories 탐색 | 메뉴 텍스트 | **이미지 카드 그리드** |
| 에디토리얼 | 없음 | 풀블리드 이미지 + 장문 콘텐츠 삽입 |
| 마감 | 상품으로 끝 | **창립자 인용문**으로 브랜드 보이스 마감 |
| FAQ | 없음 | 8개 주제 · **20개 Q&A 직접 작성** |
| 소셜 로그인 · Stay signed in | 없음 | UI 추가 설계 |
| 주문 상태 조회 | 실구매자만 접근 가능 | **4가지 상태를 전부 시각화** |

**시각적 재현이 목적이었다면 이런 변경을 할 이유가 없습니다.** 커머스 IA와 전환 동선을 다시 설계한 결과이고, 그게 13년 커머스 경험이 들어간 지점입니다.
▸ 그럼에도 이미지와 브랜드명을 가져다 쓴 것은 사실이므로, 저작권 답변과 이 답변은 **분리해서** 하셔야 합니다. 설계를 다시 했다는 게 이미지 사용을 정당화하지는 않습니다.

### 31. 원본 브랜드 이미지를 3,411장 쓰셨습니다. 법적 리스크는요? 🔥

**A.** 리스크가 있다고 판단합니다. 현재 상태를 그대로 말씀드리면 원본 브랜드 이미지 3,410장이 저장소에 그대로 들어가 있습니다.
▸ **용량은 이후 크게 줄였습니다** — PNG 962.8MB를 WebP 92.2MB로 전환(−90.4%)해서 게시 사이트 기준 531MB입니다. 다만 **용량이 줄어도 저작권 문제는 그대로**입니다. 압축은 성능 문제의 해결이지 권리 문제의 해결이 아닙니다.
지금이라면 세 가지를 다르게 하겠습니다.
① 이미지를 **자체 제작 또는 라이선스 프리 소스로 교체**
② 저장소에 원본을 커밋하지 않고 외부 스토리지 참조
③ 애초에 HOOT UP처럼 가상 브랜드로 설계
실제로 HOOT UP에서는 썸네일이 없으면 **신규 에셋을 제작해서 연결**했습니다(DEF-07). 그 방향이 맞다고 봅니다.

### 32. Google Safe Browsing 이력 🔥

**A.** 로그인·회원가입 폼이 있는 정적 사이트가 피싱 사이트로 오탐된 건이었습니다. 실제 인증 로직이 없는데 비밀번호 입력 필드가 존재하는 구조였기 때문입니다.
세 가지로 해결했습니다.
① 입력 필드 5,159개에 `autocomplete="off"` 적용
② 사이트 타이틀에 포트폴리오임을 명시
③ 푸터에 국문·영문 고지 추가
**배운 것은 "동작하지 않는 UI도 외부에서는 동작하는 것으로 읽힌다"**는 점입니다. 그래서 HOOT UP에서는 인증 트리거를 `<a href="/login">`이 아니라 `<button type="button">` + ARIA로 바꿨습니다. 이동하지 않는 것은 링크로 쓰지 않는다는 원칙을 사이트 전역에 적용했습니다.

### 33. HOOT UP은 왜 가상 브랜드

**A.** JWA의 저작권 부담을 겪고 나서 내린 결정입니다.
그리고 가상 브랜드로 진행했기 때문에 오히려 보여줄 수 있었던 부분도 있었습니다. **브랜드 기획부터 제가 했다**는 것입니다. 부엉이 마스코트 업비, "밤샘 스트릭", 구독(올나잇 패스) + 단건 소장의 하이브리드 과금 모델, 세계관 워딩 체계까지 전부 설계했습니다.
직접 기준 데이터를 만들었기 때문에 이후 검수할 때 비교 기준도 분명해졌습니다. AI가 지어낸 강의명을 잡아낼 수 있었던 건 제가 152종 카탈로그를 직접 만들어서 source of truth가 있었기 때문입니다.

### 34. 규모가 과한 것 아닌가 / 양 대신 완성도 🔥

**A.** 페이지 수 자체를 늘리는 것이 목적은 아니었습니다. 규모가 커졌을 때 실제로 어떤 유지보수 문제가 생기는지 경험해보고 싶었습니다.
PRD 1.2에 적었습니다 — 퍼블리셔 포트폴리오는 대부분 단일 랜딩 + 반응형 3단계에서 멈춥니다. 그러면 채용 담당자가 판단할 수 없는 게 있습니다. 페이지가 수백 개로 늘었을 때 CSS가 무너지지 않는 구조를 설계할 수 있는가.
실제로 규모에서만 나온 문제들이 있습니다.
- 392페이지에서 CSS 오버라이드가 15,208줄까지 누적됐고, 그걸 8,812줄로 되돌리는 과정에서 `:is()` 명시도 판단이 필요했습니다
- 136페이지 규모에서 새 컴포넌트 클래스명이 기존 것과 충돌했습니다(`faq__` → `hfaq__` 274회 리네이밍)
- 157페이지에 헤더가 복제된 구조라 로고 `href` 하나가 전 페이지 404를 유발했습니다

이런 문제는 페이지 수가 적은 프로젝트에서는 경험하기 어려운 부분이라고 생각했습니다.

### 35. PRD를 퍼블리셔가 쓰는 게 맞나

**A.** 실무에서 퍼블리셔가 PRD를 쓰지는 않습니다. 제가 쓴 이유는 두 가지입니다.
① **혼자 하는 프로젝트라 요구사항을 고정할 사람이 없었습니다.** 특히 HOOT UP은 AI에게 위임하는 구조라, 지시 기준이 문서로 고정돼 있지 않으면 검수할 기준 자체가 없습니다.
② **읽을 줄 알아야 하기 때문입니다.** 실무에서 저는 PRD를 받는 쪽입니다. 직접 작성해보니 실무에서 PRD를 받을 때도 빠진 요구사항이나 모호한 부분을 더 잘 확인할 수 있겠다고 느꼈습니다.
▸ **실전 연결**: "시안에 없는 상태를 먼저 묻는 습관"이 여기서 나옵니다.

### 36. 역기획 문서를 얼마나 믿을 수 있나 🔥

**A.** 그 부분은 충분히 지적받을 수 있다고 생각합니다. 그래서 JWA PRD 서두에 **"구현 착수 이전 기획 문서가 아니라 완성된 산출물을 기준으로 요구사항을 역추적·정형화한 문서"**라고 명시했습니다. 사후에 정리한 문서라는 점을 처음부터 명시했습니다.
그리고 **HOOT UP에서는 순서를 바꿨습니다.** 사전 PRD v1.0을 구현 착수 전에 확정하고 원본 그대로 보존한 뒤, 계획 대비 변경 16건을 `CH-` ID로 추적해서 v2.3에서 통합했습니다. 계획→실물 사이의 판단 이력이 남아 있습니다.
JWA에서 아쉬웠던 문서화 방식을 HOOT UP에서는 구현 전에 PRD를 작성하고 변경 이력을 남기는 방식으로 보완했습니다.
▸ **그리고 사후 문서라도 수치는 검증할 수 있습니다.** JWA PRD는 v1.4에서 전 수치를 재검증했고, 그 과정에서 커밋 수(104→105)와 컬러 하드코딩 집계 기준(단일 축→3축 분리)을 정정했습니다. 문서를 쓴 시점이 사후여도, **수치가 코드에서 재현되는지는 별개로 확인 가능합니다.**

### 37. 백엔드가 없다 / 실무 대응

**A.** 두 프로젝트 모두 비목표에 "백엔드 API·DB 연동 없음, 모든 데이터는 정적 마크업"이라고 명시했습니다.
실무에서 퍼블리셔가 하는 일은 대개 **동적 데이터가 들어올 자리를 마크업으로 확보하는 것**입니다. 반복 영역을 하나의 컴포넌트로 확정하고, 클래스명을 데이터가 아니라 역할 기준으로 지어놓는 일입니다.
HOOT UP에서 카드 컴포넌트 하나가 152개 강의에 반복 적용되고, 계정 사이드 메뉴 아코디언이 `js/account-nav.js` 28줄 하나로 21페이지에서 동작합니다. 마크업 규약(`[data-acc-trigger]`, `.is-open`, `aria-expanded`)만 지키면 그룹 구성이 달라도 같은 스크립트가 붙습니다. 이런 식으로 데이터가 달라져도 같은 마크업 규칙과 스크립트를 재사용할 수 있도록 구성했습니다.

▸ **그리고 무엇이 UI 설계이고 무엇이 실제 동작인지를 문서에 구분해 적었습니다.** 이게 백엔드 없는 포트폴리오에서 제일 중요한 부분이라고 봅니다.

| 항목 | 상태 |
| --- | --- |
| 정렬 옵션 5종 | UI 설계 · **동작 미구현** |
| 소셜 로그인 (FB · Google) | UI 설계 · **OAuth 미구현** |
| Stay signed in | UI 설계 · **동작 미구현** |
| 청구지 = 배송지 자동 채움 | UI 설계 · **자동 채움 미구현** |
| My Cart 수량 표기 | 마크업 고정값 · **실시간 갱신 미구현** |

**보이는 것을 전부 "구현했다"고 쓰면 문서가 거짓말이 됩니다.** 면접관이 정렬을 눌러보면 3초 만에 드러납니다. 그래서 UI/UX 설명서에 항목마다 "실제 동작은 백엔드 연동 단계에서 확장 가능하도록 설계"라고 명시했습니다.
**미구현을 적어두는 것이 오히려 신뢰를 만든다**고 생각합니다. 무엇이 되고 무엇이 안 되는지 아는 사람이 만든 문서라는 게 드러나기 때문입니다.

### 38. 두 프로젝트 사이의 성장 (수치)

**A.** 세 축으로 답변드리겠습니다.

| 축 | JW Anderson | HOOT UP | 무엇이 달라졌나 |
| --- | --- | --- | --- |
| 시맨틱 별칭 | 9개 선언 · **실참조 0회** | 별칭 1,110 / 원시 819 · **57.5%** | JWA에서 설계만 하고 안 쓴 계층을, 착수 전 요구사항으로 명문화해서 가동 |
| 접근성 | 착수 시 포커스 표시 없음 · 대비 미측정 · `main` 2/392 | Lighthouse 접근성 **100** · 스킵링크 164/164 · `aria-live` 배선 | HOOT UP에서 먼저 측정 습관을 들이고, 그 기준으로 **JWA를 되돌아가 고쳤습니다** (현재 `main` 392/392 · 대비 10.85:1 · `:focus-visible` 전역) |
| 커밋 컨벤션 | 한글 자체 규칙 · Conventional 1건 | 08-01부터 `feat/style/fix` 도입 (34건 중 33건) | 협업 형식으로 전환 |

세 가지 중에서는 첫 번째 변화가 가장 의미 있었다고 생각합니다. JWA에서 "2계층 토큰 아키텍처"라고 써놓고 실제로는 원시 토큰만 호출하고 있었습니다. 설계와 구현이 어긋난 걸 스스로 발견해서, 다음 프로젝트의 착수 전 요구사항으로 명확하게 정했습니다.

---

# 04. JW Anderson — CSS 아키텍처

## 4-1. 리팩터링

### 39. 42.1% 감축 방법

**A.** 전략은 **공통 승격 + 차분 오버라이드**입니다.
① base 레이어에 전 기기 공통값을 두고, 미디어쿼리에는 예외만 선언
② 2개 이상 페이지에서 반복되는 규칙을 `page.css` → `global.css`로 승격
③ 선언 블록이 같고 셀렉터만 다른 규칙을 `:is()`로 그룹화
작업 단위는 **컴포넌트 구획**이었습니다. 구획 하나를 잡아서 리팩터링 전후 셀렉터를 전체 대조하고 커밋했습니다. 그래서 커밋이 105건까지 늘었는데, 회귀가 나면 되돌릴 범위를 최소화하려는 의도였습니다(ADR-08). 그중 구획 단위 리팩터링 커밋이 34건(page 25 · global 9)이고, 'base 이동'을 명시한 것이 17건입니다.

**줄 수만이 아니라 규칙 수로도 재검증했습니다.**

| 지표 | 전 | 후 | 감축 |
| --- | --- | --- | --- |
| CSS 총량 | 15,208줄 | 8,812줄 | −42.1% |
| CSS 규칙 | 2,033룰 | 1,184룰 | **−41.8%** |
| global.css | 5,312줄 | 2,780줄 | −47.7% |
| page.css | 9,732줄 | 5,852줄 | −39.9% |
| reset + module + import | 164줄 | 174줄 | +10 (안전장치) |

▸ **위 표는 리팩터링 종료 시점(07-26) 기준입니다.** 이후 M6 잔여 작업으로 `reset.css`에 `img` 전역 규칙과 `:focus-visible` 10줄을 추가하고 `page.css`에서 중복 선언 12줄과 깨진 배경 참조 4줄을 제거해서, **현재 총량은 8,806줄**입니다(감축률 −42.1%로 동일). 리팩터링 성과와 이후 유지보수를 같은 숫자로 묶으면 어느 쪽도 정확하지 않아서, PRD에도 두 시점을 나눠 적었습니다.

줄 수는 포맷팅으로 바뀔 수 있지만 **규칙 수는 안 바뀝니다.** 두 값이 −42.1%와 −41.8%로 거의 일치한다는 게, 줄바꿈을 줄인 게 아니라 실제로 규칙을 없앴다는 증거입니다.

### 40. 왜 15,208줄을 기준선으로 잡았나 🔥

> 이 질문은 수치를 방어하기보다 기준을 어떻게 잡았는지 솔직하게 설명하는 편이 좋습니다.

**A.** 유리한 숫자를 고른 것 아니냐는 지적, 타당합니다. 실제로 7/19 첫 커밋은 10,358줄이고, 그 기준으로는 감축이 아니라 **15% 감소**에 그칩니다.
기준선을 7/20으로 잡은 이유는 **그날이 반응형 구현이 완료된 시점**이기 때문입니다. 7/19는 데스크탑만 있는 상태였고, 이후 태블릿·모바일 3구간을 추가하면서 오버라이드가 누적돼 15,208줄까지 올라갔습니다. 즉 10,358 → 15,208은 **기능 추가에 따른 정상 증가**이고, 15,208 → 8,812가 **순수 리팩터링 결과**입니다.
PRD 10.2에 이 증감 곡선을 커밋별 표로 그대로 실었고, "07-19 대비 라인이 한 번 증가한 뒤 감소하는 것은 반응형 3구간을 추가했기 때문"이라고 명시했습니다. 문서에 그대로 남겼습니다.
▸ **더 정직한 표현**: "42.1%는 리팩터링 구간의 감축률이고, 프로젝트 전체로 보면 첫 커밋 대비 15% 감소가 맞습니다. 두 숫자를 다 말씀드리는 게 정확합니다."

### 41. `git show <commit>:css/global.css | wc -l` 설명

**A.** `git show`는 특정 커밋 시점의 파일 내용을 꺼내는 명령입니다. `커밋해시:경로` 형식으로 지정하면 그 시점의 파일을 표준 출력으로 보여줍니다. 여기에 `wc -l`을 파이프로 물리면 줄 수가 나옵니다.
체크아웃하지 않고 과거 시점의 파일 상태만 확인할 수 있어서 작업 중인 브랜치를 건드리지 않습니다. PRD의 감축 이력 표는 전부 이 방식으로 커밋마다 복원해서 실측한 값입니다. 추정치가 아닙니다.
▸ 꼬리질문 대비: `git log --stat`, `git diff --stat HEAD~1`도 알아두기.

### 42. 스타일 유실 3건 — 발견과 복구

**A.** 구획 단위로 커밋했기 때문에 잡혔습니다.
리팩터링 대상 구획을 하나 처리할 때마다 **전후 셀렉터를 전체 대조**하고 브라우저에서 해당 화면을 확인하는 절차였는데, 대조 과정에서 승격 과정에 빠진 선언이 3건 나왔습니다. 전부 당일 발견해서 복구했고 최종 회귀 0건입니다.
커밋 단위가 컴포넌트 구획이라 되돌릴 범위가 명확했던 게 컸습니다. `page(qtyComponent) base 이동 및 D,T,M 삭제 완료` 같은 메시지 형식이라, 어떤 컴포넌트의 어떤 성격의 작업인지가 메시지에 드러납니다.

### 43. 회귀를 미리 막으려면 무엇이 있어야 했나

**A.** 육안 확인에 의존한 게 근본 문제입니다. 세 가지가 있었어야 합니다.
① **시각 회귀 테스트** — Playwright나 Percy로 리팩터링 전후 스크린샷을 픽셀 비교. 392페이지 × 5구간이면 사람이 볼 수 있는 양이 아닙니다.
② **계산된 스타일 해시 비교** — 실제로 HOOT UP 08-15 CSS 중복 통합 때 이 방식을 썼습니다. 3개 페이지 × 3구간의 `getComputedStyle` 결과를 해시로 떠서 통합 전후를 비교했고, 렌더 결과가 동일한 걸 확인했습니다.
③ **stylelint** — 중복 선언, 명시도 역전을 커밋 전에 잡습니다.
**②는 JWA 이후에 실제로 도입한 방법입니다.** 같은 문제를 두 번 겪지 않으려고 만든 절차입니다.

### 44. global 승격 판단 기준

**A.** "2개 이상 페이지에서 반복되는 규칙"을 기준으로 잡았습니다(NFR-CODE-04).
다만 **이 기준만으로는 부족했다**는 걸 지금은 압니다. 2개 페이지에서 우연히 같은 값인 것과, 컴포넌트로서 같은 것은 다릅니다. 전자를 승격하면 나중에 한쪽만 바뀔 때 다시 떼어내야 합니다.
지금이라면 기준을 **"같은 컴포넌트인가"**로 바꾸겠습니다. 값의 일치가 아니라 역할의 일치를 봐야 합니다.

### 45. NFR-CODE-04가 "진행 중"인 이유 ⚠️

**A.** 페이지↔글로벌 교차 대조가 끝나지 않았습니다. `page.css` 5,852줄과 `global.css` 2,780줄을 셀렉터 단위로 전수 비교해서 중복 규칙을 병합하는 작업인데, 리팩터링 5일 안에 컴포넌트 구획 단위까지만 끝냈습니다.
남은 이득은 추정 [N]줄 수준으로 봅니다. 다만 **명시도가 뒤집힐 위험이 있어 자동 병합이 안 되는 구간**이라, 남은 작업은 수동 대조가 필요합니다.

### 46. `qtyComponent`가 아직 global로 안 간 이유 ⚠️

**A.** 수량 컴포넌트는 장바구니 패널·전체 뷰·모달 세 곳에서 같은 컴포넌트를 씁니다(FR-CART-04). 요구사항상 공용인데 CSS는 아직 `page.css`에 남아 있습니다.
`page(qtyComponent) base 이동 및 D,T,M 삭제 완료` 커밋에서 base 이동까지는 했지만 global 승격은 안 했습니다. **이건 명백한 잔여 과제이고, 승격 대상이 맞습니다.** 45번 항목과 함께 처리해야 하는 건입니다.

## 4-2. `:is()` 와 명시도

### 47. `:is()` 74개 적용 / 29개 제외 기준

**A.** 기준은 하나입니다 — **그룹 안 셀렉터들의 명시도가 같은가.**
`:is()`는 인자 중 **가장 높은 명시도를 그룹 전체에 적용**합니다. 명시도가 다른 셀렉터를 묶으면 낮았던 쪽이 갑자기 올라가면서 기존 캐스케이드가 뒤집힙니다.
74개는 명시도가 동일해서 안전했고, 29개는 섞여 있어서 압축 이득보다 회귀 위험이 크다고 판단해 원형을 유지했습니다. 실수로 빠뜨린 것이 아니라, 회귀 위험을 고려해 의도적으로 제외했습니다.

### 48. `:is()` 명시도 계산 규칙 🔥

**A.** `:is()` 자체는 명시도가 0이고, **인자 목록 중 가장 명시도가 높은 셀렉터의 값을 취합니다.**
```
:is(h1, .title)     → .title이 더 높음 → (0,1,0)
:is(.a, #b)         → #b가 더 높음    → (1,0,0)
```
`:not()`과 `:has()`도 같은 규칙입니다. `:where()`만 다릅니다.

### 49. `:where()`와의 차이 / 왜 안 썼나 🔥

**A.** `:where()`는 **인자가 무엇이든 명시도가 항상 0**입니다. `:is()`는 최댓값을 가져옵니다.
```css
:is(.a, #b) .c   /* (1,1,0) */
:where(.a, #b) .c /* (0,1,0) — .c만 계산 */
```
이 프로젝트에서 `:where()`를 안 쓴 이유는, 제 목적이 **기존 캐스케이드를 그대로 유지한 채 총량만 줄이는 것**이었기 때문입니다. `:where()`를 쓰면 명시도가 0으로 떨어져서 기존에 이겼던 규칙이 지게 됩니다. 회귀 방향이 반대로 생깁니다.
▸ **`:where()`가 맞는 경우**: reset이나 라이브러리 기본 스타일처럼 **사용자가 쉽게 덮어쓰길 원하는 규칙**. 지금이라면 `reset.css` 111줄에 `:where()`를 적용하겠습니다.

### 50. `:is(.a, #b) .c` 명시도는?

**A. (1, 1, 0)** 입니다.
`:is(.a, #b)`가 `#b`를 따라 (1,0,0), 여기에 `.c`의 (0,1,0)이 더해집니다.

### 51. 명시도 문제를 근본적으로 없애려면

**A.** 세 가지입니다.
① **`@layer`** — 레이어 순서가 명시도를 이깁니다. `@layer reset, base, components, utilities;`로 선언해두면 나중 레이어가 무조건 이기므로 명시도 전쟁 자체가 사라집니다. 이 방법이 가장 근본적인 해결책에 가깝습니다.
② **선택자 평탄화** — BEM처럼 단일 클래스로만 짜면 명시도가 (0,1,0)으로 고정됩니다.
③ **ID 셀렉터 금지 · `!important` 금지** — HOOT UP에서 실제로 `!important` 0건을 유지했습니다.

### 52. `@layer`를 이 프로젝트에 적용한다면

**A.** 현재 파일 구조가 이미 계층적이라 그대로 매핑됩니다.
```css
@layer reset, tokens, global, page;
```
그러면 두 가지가 해결됩니다.
① `page.css`가 `global.css`를 덮으려고 명시도를 올릴 필요가 없어집니다 — 지금은 `page` 규칙이 이기려고 셀렉터를 길게 쓴 구간이 있습니다.
② `:is()` 미적용 29개 중 상당수가 **명시도 걱정 없이 그룹화 가능**해집니다. 레이어가 순서를 보장하므로 그룹 내부 명시도 차이가 무의미해집니다.
▸ 다만 `@layer`는 **선언 순서가 곧 우선순위**라, 도입 시 전체 캐스케이드를 다시 검증해야 합니다. 리팩터링과 동시에 하면 안 되고 별도 회차로 잡아야 합니다.

## 4-3. 반응형

### 53. 왜 5구간인가

**A.** 원본이 와이드 모니터 기준으로 설계된 사이트고, 1280px 이상을 단일 처리하면 카드가 과도하게 늘어나거나 여백이 붕괴합니다.
**먼저 1280px이라는 값 자체가 관례값이 아닙니다.** 메가 메뉴가 5개 카테고리 + 서브 메뉴 + 프로모션 타일을 겹침 없이 배치할 수 있는 최소 폭에서 역산한 값입니다. 콘텐츠가 요구하는 폭을 재서 정했습니다.
실제 카드 열 수는 이렇게 갑니다.
```
~767      1단   100%
768~1279  2단   calc(50% - 18px)
1280~1500 2단   calc(50% - 18px)
1501~1918 3단   calc(33.333% - 25px)
1919~     4단   265px 고정
```
**데스크탑 내부를 3개로 세분한 게 핵심입니다(ADR-05).** 그리고 세분한 실제 이유는 카드 크기가 아니라 **다른 요소와의 충돌**이었습니다 — 자세한 건 56번에서 말씀드리겠습니다. 대가는 미디어쿼리 개수 증가입니다.

### 54. 데스크탑 퍼스트를 택한 이유 🔥

**A.** 원본이 와이드 기준 설계라, **오버라이드 총량이 최소가 되는 방향**을 택했습니다(ADR-04).
데스크탑을 base에 두면 좁은 폭만 예외로 선언하면 됩니다. 모바일을 base로 잡으면 데스크탑 3개 구간 전부를 오버라이드로 써야 해서 총량이 늘어납니다. 392페이지 규모에서 이 차이가 큽니다.
**다만 이건 이 프로젝트 조건에서의 판단이지 일반 원칙은 아닙니다.**
▸ **꼬리질문 대비 — "그럼 전부 데스크탑 기준인가?"**: 아닙니다. 레이아웃은 데스크탑 기준이지만 **간격 토큰은 반대로 모바일값을 `:root` 기본값으로 뒀습니다**(`--gutter: 10px`). 미디어쿼리에 틈이 생겨도 좁은 여백으로 폴백되는 쪽이 안전하기 때문입니다. 두 층의 기준이 다르고, 그 사실을 ADR-04 트레이드오프 칸에 적어뒀습니다.

### 55. 모바일 퍼스트의 장점을 아는가

**A.** 압니다. 세 가지입니다.
① **성능** — `min-width` 미디어쿼리는 모바일에서 아예 평가되지 않습니다. 모바일 기기가 데스크탑용 선언을 받아 덮어쓰는 낭비가 없습니다.
② **점진적 향상** — 작은 화면의 필수 콘텐츠부터 정의하므로 정보 우선순위가 강제로 정리됩니다.
③ **실사용 비중** — 대부분의 서비스에서 모바일 트래픽이 다수입니다.
그럼에도 반대로 간 것은 위 54번의 이유이고, **HOOT UP에서는 이 선택을 승계하지 않았습니다.** 브레이크포인트를 아예 1280/768/480으로 재정의했고(CH-RWD-01), 그건 JWA 기준을 그대로 물려받는 게 맞지 않다고 판단했기 때문입니다.

### 56. 데스크탑을 세 구간으로 나눈 실제 이유 — 카드 크기 때문인가 🔥

**A.** 카드 크기 때문이 아니라 **좌우 장식 영상과 그리드가 겹치기 때문**입니다.

상품 목록 페이지에는 장식용 영상 2개가 화면 좌우에 `fixed`로 고정되어 스크롤과 무관하게 머물러 있습니다. 1918px 이하에서는 4열 그리드가 이 영상 영역을 침범합니다.
그래서 열 수를 줄여서 겹침을 해소했습니다.

| 폭 | 열 수 | 카드 폭 | 이유 |
| --- | --- | --- | --- |
| 1919+ | 4열 | 265px 고정 | 영상과 겹치지 않는 폭. 남는 공간은 여백으로 |
| 1501–1918 | 3열 | `calc(33.333% - 25px)` | 4열이면 좌우 영상과 충돌 |
| 1280–1500 | 2열 | `calc(50% - 18px)` | 3열도 충돌 |

즉 **고정폭 → 비율 → 비율**로 전략이 바뀌는 경계이고, 그 경계를 정한 건 카드가 아니라 영상입니다.
▸ **여기서 나올 수 있는 반문 — "그럼 영상을 `fixed`로 두지 말았어야 하는 것 아닌가?"**: 타당한 지적입니다. 장식 요소가 레이아웃 구간을 결정하는 구조는 좋지 않습니다. 다만 좌우 고정 영상이 원본의 연출 의도였고, 그걸 살리면서 그리드를 지키는 방법으로 열 수 조정을 택했습니다. 태블릿·모바일에서는 영상을 아예 숨깁니다.

### 57. 데스크탑(광)의 미디어쿼리는 `min-width: 1280px`인데 유효 폭은 `≥1919px`입니다. 왜 다른가요? 🔥

**A.** 선언값과 실제로 적용되는 폭이 다른 것이 맞습니다. 이 프로젝트의 **공통 승격 + 차분 오버라이드** 전략이 데스크탑 계층 내부에도 그대로 적용된 결과입니다.

선언 순서는 이렇습니다.

```css
/* ① 데스크탑 계층 공통 — 여기에 4단이 들어 있습니다 */
@media (min-width: 1280px) {
  .card { width: 265px; }
}
/* ② 좁은 구간이 열 수만 덮어씀 */
@media (min-width: 1280px) and (max-width: 1500px) {
  .card { width: calc(50% - 18px); }
}
/* ③ 중간 구간이 열 수만 덮어씀 */
@media (min-width: 1501px) and (max-width: 1918px) {
  .card { width: calc(33.333% - 25px); }
}
```

데스크탑 폭에 들어오면 우선 ①의 4단 규칙을 받고, 그 위에서 ②와 ③이 열 수만 차분으로 덮습니다. **두 오버라이드가 모두 걸리지 않는 폭이 1919px 이상**이라, 4단이 실제로 살아남는 구간이 거기입니다.

PRD 8.1 표에서 「미디어쿼리」와 「유효 폭」을 두 열로 나눈 이유가 이것입니다. 한 열로 합치면 둘 중 하나는 사실과 달라집니다.

▸ **왜 이렇게 짰나** — 데스크탑 3구간에서 공통인 값(카드 간격, 컨테이너 여백, 타이포)을 `min-width: 1280px` 블록에 한 번만 선언하기 위해서입니다. 세 구간을 각각 완결형으로 쓰면 공통 선언이 세 번 반복됩니다. CSS 감축의 핵심 전략이 페이지 간 승격이었는데, 같은 원리를 브레이크포인트 사이에도 적용한 것입니다.

▸ **꼬리질문 "그럼 이게 base인가요?"** — 아닙니다. 여기서의 '기본값'은 **데스크탑 계층 내부의 기본값**이고, NFR-RWD-01에서 말하는 base 레이어(미디어쿼리 밖 전 화면 공통층)와는 다른 개념입니다. 4단 규칙은 base가 아니라 `@media (min-width: 1280px)` 블록 안에 있습니다. 혼동될 수 있는 지점이라 PRD 8.1 본문에 이 구분을 따로 명시해 두었습니다.

▸ **꼬리질문 "표 순서가 광 → 협 → 중으로 되어 있어 이상한데요"** — CSS 선언 순서와 맞춘 것입니다. 폭 오름차순으로 정렬하면 캐스케이드가 표에서 읽히지 않습니다. 어느 규칙이 어느 규칙을 덮는지 보이게 하려면 선언 순서가 맞다고 판단했습니다.

▸ **경계값이 한 번 바뀐 이력이 있습니다.** 상단 경계를 1890/1891에서 **1918/1919로 조정**했습니다. 1920px 모니터에서 브라우저 스크롤바 폭을 빼면 실제 뷰포트가 1900px 안팎으로 잡히는데, 경계가 1891이면 이 흔한 해상도가 4열 구간에 들어가 버립니다. 좌우 장식 영상과 겹치는 폭인데도요. 경계를 1919로 올려서 **1920 모니터가 3열 구간에 머물도록** 했습니다. 이 조정은 PRD·기능정의서·UI/UX 세 문서에 동시 반영했습니다.
▸ **약점도 함께 말씀드리면** — 이 구조는 `min-width: 1280px` 블록이 커질수록 "이 규칙이 실제로 어느 폭에서 살아 있는가"를 사람이 매번 추적해야 합니다. 표에 유효 폭 열을 만든 것도 그 추적을 문서로 대신한 것입니다. Grid `auto-fit`으로 가면 구간 선언 자체가 줄어드는데, 그 이야기는 58번에서 이어집니다.

### 58. `calc(50% - 18px)` — 왜 18px인가, Grid `gap`은? 🔥

**A.** 18px은 카드 사이 간격 36px의 절반입니다. 2단일 때 카드 2개가 간격 1개를 나눠 가지므로 각각 18px씩 빼는 계산입니다. 3단은 간격 2개를 3개가 나누므로 `calc(33.333% - 25px)`이 됩니다.
**Grid를 썼다면 이 계산 자체가 필요 없습니다.**
```css
grid-template-columns: repeat(auto-fill, minmax(265px, 1fr));
gap: 36px;
```
이 한 줄이면 5구간 미디어쿼리 중 카드 열 수 관련 부분이 대부분 사라집니다. `:not(:nth-of-type(Nn))` 마진 제거도 불필요해집니다.
**Flexbox + 계산식으로 간 이유는 학습 시점의 숙련도 때문이고, 지금이라면 Grid로 짭니다.** 이건 방어할 부분이 아니라 인정할 부분입니다.
▸ 단, Flexbox `gap`도 현재 브라우저에서 지원되므로 굳이 Grid가 아니어도 `gap`만으로 개선 가능합니다.

### 59. `:not(:nth-of-type(Nn))` 대신 gap/Grid를 안 쓴 이유 🔥

**A.** 58번과 같은 답입니다. 각 행 마지막 카드의 우측 마진을 제거하려고 쓴 셀렉터인데, `gap`을 쓰면 애초에 마진을 안 주니까 제거할 것도 없습니다.
그리고 이 방식에는 **구간마다 N이 바뀐다**는 유지보수 문제가 있습니다. 2단이면 `2n`, 3단이면 `3n`, 4단이면 `4n`이라 미디어쿼리마다 셀렉터를 다시 써야 합니다. 구간이 5개니까 5번 씁니다.
**`gap`으로 가면 이 5개 선언이 1개로 줄어듭니다.** CSS 감축 관점에서도 이쪽이 맞습니다.

### 60. `nth-of-type` vs `nth-child`

**A.** `nth-child`는 **부모의 모든 자식** 중 순번을, `nth-of-type`은 **같은 태그 중** 순번을 셉니다.
```html
<div>
  <h2>제목</h2>
  <p>A</p>   <!-- nth-child(2), nth-of-type(1) -->
  <p>B</p>   <!-- nth-child(3), nth-of-type(2) -->
</div>
```
카드 그리드에서 `nth-of-type`을 쓴 이유는, 카드 사이에 다른 태그(광고 배너 등)가 섞여도 카드끼리의 순번이 유지되기 때문입니다. 다만 **클래스 기준으로 세려면 `nth-child`가 더 정확한 경우가 많습니다** — `nth-of-type`은 클래스를 보지 않고 태그만 봅니다.
▸ 클래스 기준: `.card:nth-child(2n)` 은 의도대로 안 될 수 있음. `:nth-child(2n of .card)` 문법이 최신 스펙에 있습니다.

### 61. JWA 767/1279를 버리고 1280/768/480으로 바꾼 이유 🔥

**A.** JWA 기준이 틀린 게 아니라 **프로젝트가 다르기 때문**입니다.
JWA는 와이드 모니터 사용 비중이 높은 럭셔리 커머스라 데스크탑 세분화가 필요했습니다. HOOT UP은 이러닝 플랫폼이고, 실사용자가 퇴근 후 모바일로 들어오는 서비스로 설정했습니다. 페르소나 A(31세 마케터, 퇴근 후 탐색), B(27세 N잡러)가 그 근거입니다.
그래서 **480px 초소형 구간을 추가**하고 데스크탑 세분화를 버렸습니다.
그리고 실무적 이유가 하나 더 있습니다. 착수 당시 미디어쿼리가 **혼재 6종**으로 늘어나 있었습니다. 767/1279 계열과 다른 값이 섞이면서 어느 구간이 진짜인지 불분명해졌고, 08-09에 3단으로 통일했습니다(CH-RWD-01).
▸ **면접에서 강조할 문장**: "이전 프로젝트 기준을 관성으로 승계하지 않고, 프로젝트마다 다시 정하는 게 맞다고 판단했습니다."

### 62. 5구간을 어떻게 검증했나 / 실기기 테스트

**A.** 요구사항에 검증 항목을 먼저 박아두고, 그 항목을 하나씩 확인하는 방식이었습니다. PRD 8.1에 네 가지로 적어뒀습니다.

| ID | 검증 항목 |
| --- | --- |
| NFR-RWD-01 | base 레이어에 공통값, 미디어쿼리는 예외만 |
| NFR-RWD-02 | 전 브레이크포인트에서 **가로 스크롤 미발생** |
| NFR-RWD-03 | **터치 타깃 최소 44×44px** 확보 |
| NFR-RWD-04 | **경계값에서 레이아웃 점프 없음** — 767/768 · 1279/1280 · 1500/1501 · 1918/1919 |

**NFR-RWD-04가 이 프로젝트에서 제일 중요한 항목이었습니다.** 구간이 5개면 경계가 4개인데, 각 구간 안쪽만 보면 멀쩡한데 경계를 넘는 순간 열 수가 튀거나 여백이 어긋나는 일이 생깁니다. 그래서 구간 중앙이 아니라 **경계 양쪽 1px 차이로** 확인했습니다. 767과 768, 1918과 1919를 붙여서 봅니다.

확인 방법은 [본인이 실제로 쓴 도구 — 예: Chrome 개발자도구 기기 툴바 반응형 모드로 폭을 직접 입력]이었고, 실기기는 [실제로 확인한 기기 — 예: 본인 폰 기종 / iOS Safari · Android Chrome]로 배포본을 열어 확인했습니다.

▸ **꼬리질문 "실기기에서만 나오는 문제가 있었나요?"** — 대비하려면 답이 필요한 자리입니다. 문서에 근거가 있는 항목은 **모바일 헤더의 화살표 아이콘**입니다. 아이콘이 터치를 가로채서 메뉴가 안 열리는 문제가 있어 `pointer-events: none`을 줬습니다. 데스크탑 마우스로는 안 드러나고 손가락으로 눌러야 나오는 유형입니다.

▸ **NFR-RWD-02는 이후 구조로 보강했습니다.** `reset.css`에 `img { max-width: 100%; height: auto; }` 전역 규칙을 추가했습니다. 이미지 13,502개를 하나씩 확인하는 건 현실적이지 않지만, **reset에 5줄을 넣으면 어떤 이미지가 들어와도 부모 폭을 넘지 않습니다.** 가로 스크롤은 대개 이미지 한 장이 컨테이너를 밀어내면서 생기는데, 그 경로를 구조 차원에서 막은 것입니다 → 76번.

▸ **호환성 범위도 함께 말씀하세요** — Chrome · Edge · Safari · Firefox 최신 2개 버전, iOS Safari, Android Chrome (PRD 8.5). 범위를 정해두지 않으면 "어디까지 봤나"에 답할 수 없습니다.

▸ **말하지 말 것**: 개발 툴 내장 미리보기 패널의 폭 오보고 이야기는 **HOOT UP 쪽 기록**입니다. JWA는 직접 작성한 프로젝트라 해당 사항이 없습니다. 두 프로젝트의 제약을 섞으면 바로 들통납니다.

## 4-4. 디자인 토큰

### 63. 컬러 하드코딩을 hex·rgb·색상 키워드 세 축으로 나눠 표기하셨습니다. 왜 나눴나요? 🔥

**A.** 한 축으로 세다가 놓친 적이 있어서입니다.

처음 집계 기준은 **hex와 rgb/rgba 두 축**이었고, `white` 같은 CSS 색상 키워드가 빠져 있었습니다. 그 기준으로는 "컬러 하드코딩 0건"이었지만, 키워드까지 세면 **19건(global 3 · page 16)**이 남아 있었습니다. 축을 좁게 잡고 달성 선언을 넓게 한 셈입니다.

**그래서 두 가지를 했습니다.** 축을 세 줄로 분리해 각각 판정을 붙이고, 남은 19건을 `var(--gs00)`으로 치환했습니다.

| 지표 | 목표 | 실측 | 판정 |
| --- | --- | --- | --- |
| 컬러 하드코딩 (hex) | 0건 | 0건 | 달성 |
| 컬러 하드코딩 (rgb/rgba) | 0건 | 0건 | 달성 |
| 컬러 하드코딩 (색상 키워드) | 0건 | 0건 | **달성** |

치환 결과가 다른 수치에도 드러납니다. 치환 직후 CSS 변수 참조가 **469회 → 488회**로 늘었고, 그중 색상 참조가 **125 → 144회**였습니다. **정확히 19회 증가**입니다.
**두 집계가 맞물리는지 확인하는 게 검산의 핵심이라고 봅니다.** 없앤 하드코딩 수와 늘어난 토큰 참조 수가 어긋나면, 둘 중 하나는 잘못 센 것입니다.
▸ **이후 5건이 더 나왔습니다.** `reset.css`에 남아 있던 `#ff9` · `#000` · `#cccccc` 5건인데, 처음 집계가 `global`·`page` 중심이라 **reset 파일이 사각지대**였습니다. 각각 `var(--ac02)` · `var(--gs07)` · `var(--gs02)`로 치환했고, 그래서 색상 참조가 149회 · 총 493회입니다.
**19건과 5건은 성격이 다릅니다.** 앞은 집계 *축*이 빠진 것이고, 뒤는 집계 *범위*가 빠진 것입니다. 지금은 reset · global · page 8,806줄 전수로 기준을 넓혔습니다.
▸ 수치를 말할 때는 **어느 시점 기준인지**를 같이 말씀하셔야 합니다.

▸ **핵심은 축을 나눈 쪽입니다.** 지금은 세 축이 다 0이라 한 줄로 써도 결과는 같습니다. 그래도 나눠 둔 이유는, 나중에 어느 한 축이 무너졌을 때 **한 줄짜리 표기로는 그게 안 보이기 때문**입니다.

▸ **같은 유형의 실수를 HOOT UP에서도 겪었습니다.** v2.1에서 하드코딩 색상 180건이라고 기록했는데, 집계 기준을 두 축으로 정해놓고 검산은 한 축으로만 돌려서 실제로는 205건이었습니다. §10.4에 MEA-01로 남겼습니다. **기준을 만든 사람이 검산까지 혼자 맡으면 같은 사각지대를 반복합니다.**

### 64. 의미 토큰 9종 선언 · 실참조 0회 ⚠️🔥

**A.** 사실입니다. `--c-bg`, `--c-text`, `--c-line` 등 9종을 `:root`에 선언해두고, 실제 컴포넌트는 전부 `var(--gs05)`처럼 원시 토큰을 직접 호출했습니다.
**이유는 순서입니다.** 토큰 도입이 리팩터링 중반에 들어갔는데, 이미 작성된 컴포넌트들이 원시 토큰을 참조하고 있었고, 의미 토큰으로 치환하려면 어떤 회색이 "본문색"이고 어떤 회색이 "테두리색"인지 **용도를 다시 판정**해야 했습니다. 그 판정 비용을 감당하지 못하고 선언만 남았습니다.
**그래서 이걸 PRD 9.2에 그대로 적었습니다** — "2계층 토큰 아키텍처는 설계 의도이며 아직 구현되지 않은 상태다."

### 65. 그럼 "2계층 토큰"이라 부를 수 있나 🔥

**A.** 부를 수 없습니다. 그래서 PRD에 **"설계 의도이며 아직 구현되지 않은 상태"**라고 명시했습니다. 달성으로 표기하지 않았습니다.
그리고 이 지점이 **다음 프로젝트의 착수 전 요구사항이 됐습니다.** HOOT UP은 시작하기 전에 "시맨틱 별칭을 실제로 참조할 것"을 PRD 비기능 요구사항으로 못 박았고, 결과는 별칭 참조 1,110회 / 원시 819회, 비율 57.5%입니다.
JWA에서 제대로 적용하지 못한 부분을 HOOT UP의 사전 요구사항으로 옮겨 실제 구현까지 연결했습니다.

### 66. CSS 변수 vs Sass 변수

| | CSS 변수 (커스텀 프로퍼티) | Sass 변수 |
| --- | --- | --- |
| 평가 시점 | **런타임** (브라우저) | **컴파일 타임** — 결과물엔 값만 남음 |
| 캐스케이드 | 상속됨. 셀렉터별 재정의 가능 | 없음. 스코프는 파일/블록 |
| JS 접근 | `getPropertyValue` / `setProperty` 가능 | 불가 |
| 미디어쿼리 안 재정의 | 가능 | 불가 (변수 자체는 컴파일 시 확정) |

**용도가 다릅니다.** 값이 런타임에 바뀌어야 하면 CSS 변수, 코드 생성·연산·믹스인이 목적이면 Sass입니다. 둘을 같이 쓰는 게 일반적입니다 — Sass로 코드를 생성하고, 테마 값은 CSS 변수로 노출.

### 67. CSS 변수의 런타임 변경 활용 (다크모드)

**A.** 다크모드가 대표적입니다. 의미 토큰이 있으면 최상위 한 곳만 바꾸면 됩니다.
```css
:root { --c-bg: var(--gs00); --c-text: var(--gs07); }
[data-theme="dark"] { --c-bg: var(--gs07); --c-text: var(--gs00); }
```
**그런데 여기가 64번과 직접 연결됩니다.** JWA는 의미 토큰 참조가 0회라, 다크모드를 넣으려면 컴포넌트를 전부 고쳐야 합니다. 원시 토큰 `--gs00`을 직접 부르고 있으니 최상위에서 바꿀 수가 없습니다.
**같은 문제가 mAumIo에도 있습니다.** 하드코딩 hex 554개라 다크모드가 불가능한 상태입니다. 세 프로젝트에서 같은 패턴을 확인했고, HOOT UP에서만 해결했습니다.
▸ 추가: `prefers-color-scheme` 미디어쿼리로 시스템 설정 연동.

### 68. 5px 리듬을 택한 이유 (4px/8px 아님)

**A.** 원본 사이트의 실측값이 5의 배수 계열이었기 때문입니다. 4px 그리드로 강제하면 원본 여백을 재현할 때마다 반올림이 생깁니다.
그리고 `--sp05 ~ --sp150`을 전 범위 선언하지 않고 **실사용 값만 선별 등록**했습니다. 안 쓰는 토큰을 만들지 않는다는 원칙이었습니다.
▸ **약점 인정**: 8px 그리드가 업계 표준에 가깝고, 디자인 시스템 협업에서는 표준을 따르는 편이 낫습니다. 팀 컨벤션이 8px이면 따르겠습니다.

### 69. `--gutter-header: 2.9%` — 소수점 퍼센트가 유지보수 가능한가 🔥

**A.** 좋은 지적입니다. 2.9%는 원본 사이트의 특정 뷰포트에서 역산한 값이라 **근거가 그 순간에만 있습니다.**
장점은 하나 있습니다 — 뷰포트 비례라 데스크탑 3구간에서 헤더 여백이 자연스럽게 늘어나고, 구간마다 고정값을 다시 선언할 필요가 없습니다. 실제로 이것 때문에 헤더 관련 미디어쿼리가 줄었습니다.
**다만 완전히 한 값으로 끝나지는 않았습니다.** 1280px 이상에서는 2.9%로는 여백이 부족해서 **3.9%로 재정의**했습니다(FR-NAV-04). 비례값 하나로 전 구간을 덮으려던 의도가 절반만 성립한 셈입니다.
다만 유지보수 측면의 단점도 있습니다. 왜 2.9인지 아무도 모르고, 극단적 뷰포트에서 여백이 과하거나 부족해집니다.
지금이라면 `clamp()`로 바꾸겠습니다.
```css
--gutter-header: clamp(20px, 2.9vw, 80px);
```
최소·최대 경계가 생겨서 극단값에서 무너지지 않고, 의도가 코드에 드러납니다.

### 70. 토큰 15개로 충분한가 / 상태색이 왜 없나

**A.** 이 프로젝트에는 충분했습니다. 모노톤 팔레트가 브랜드 톤이라 그레이스케일 8단계가 대부분을 커버하고, 에러 표기용 `--rd01` 하나로 상태 표현이 끝났습니다.
**충분하지 않은 건 실서비스 기준입니다.** success·warning·info가 없습니다. 폼 유효성, 재고 상태, 배송 단계 같은 걸 표현할 자리가 없습니다.
같은 문제를 HOOT UP에서도 확인했습니다. Figma에는 Success·Info가 정의돼 있는데 `tokens.css`에 미반영 상태였고, 이건 PRD 9.1에 그대로 적어뒀습니다. **상태색 토큰군은 컴포넌트가 필요해질 때 급하게 만들면 반드시 하드코딩이 됩니다.** 먼저 만들어야 하는 축입니다.
▸ **반대 방향의 문제도 있습니다 — 안 쓰는 토큰.** 처음엔 15개였는데 지금은 **12개**입니다(`--rd01` `--sp90` `--sp120` + 역할 토큰 9종). 줄어든 이유가 재미있는데, 하드코딩을 토큰으로 치환하는 과정에서 `--gs00` `--gs07` `--ac02`가 **실사용으로 편입**됐습니다. 하드코딩 제거와 죽은 토큰 정리가 같은 작업의 앞뒷면이었던 셈입니다. 그리고 `--block-gap`은 선언만 있고 사용처가 없어서 **폐기했습니다.** '죽은 토큰을 남기지 않는다'를 원칙으로 잡았기 때문입니다. 나머지 15개도 같은 기준으로 정리하거나 실제로 쓰거나 둘 중 하나를 해야 합니다.

## 4-5. 성능 · 접근성 미달분 ⚠️

### 71. `@import` 7단 체인이 왜 문제인가요?

**A.** `@import`는 **직렬 로딩**입니다. 브라우저가 앞 CSS를 받아 파싱해야 그 안의 다음 `@import`를 발견하고, 그때 다음 요청을 시작합니다. `<link>`는 HTML 파싱 중에 전부 발견되어 병렬로 요청됩니다. CSS는 렌더 블로킹 리소스라 이 지연이 첫 화면 표시를 직접 늦춥니다.

JWA는 폰트 2 · CDN 1 · 로컬 4로 7단이 물려 있습니다. **구조상 불리한 게 맞습니다.**

### 72. 그런데 왜 아직 안 고치셨나요? 🔥

> **이 문항이 이제 미달 방어가 아니라 판단 설명입니다. ADR-09로 문서화했습니다.**

**A.** 고치지 않기로 **결정**했고, 근거를 ADR-09에 남겼습니다. 세 가지를 재봤습니다.

**① 얻는 것이 작습니다.** CSS 총량이 8,806줄이고 압축하면 수십 KB입니다. 병렬화로 아끼는 건 **요청 1왕복, 0.1~0.3초 수준**입니다.

**② 잃을 수 있는 것이 큽니다.** `<link>`로 바꾸려면 **392개 HTML의 `<head>`를 일괄 수정**해야 합니다. 그리고 로드 순서가 `reset → module → 3rd-party → global → page`인데, **한 군데라도 어긋나면 전면 스타일 붕괴**입니다. 392개 파일 중 몇 개만 순서가 틀려도 그 페이지들만 깨지고, 눈으로 전수 확인이 안 됩니다.

**③ 지금 구조에 이점이 하나 있습니다.** 진입점이 `import.css` 하나라 **로드 순서를 한 파일에서 관리**합니다. `<link>`로 흩으면 순서 관리 지점이 392곳으로 늘어납니다.

**0.2초를 얻으려고 392페이지 전면 붕괴 위험을 지는 게 맞는가**를 따졌고, 아니라고 판단했습니다. 트레이드오프도 적었습니다 — 초기 렌더까지 요청 1왕복 대기, **CSS 총량이 크게 늘면 재검토**.

▸ **이 답의 핵심은 "안 고쳤다"가 아니라 "재보고 안 하기로 했다"입니다.** 성공지표에서도 판정을 '미달'이 아니라 **'판단'**으로 바꿨습니다. 개선 항목과 결정 항목은 성격이 다르니까요.

▸ **꼬리질문 "그럼 언제 고칠 건가요?"** — 재검토 조건을 미리 정해뒀습니다. CSS 총량이 크게 늘어나 병렬화 이득이 왕복 지연을 넘어서는 시점입니다. 그리고 **빌드 도구를 도입하면 이 논쟁 자체가 사라집니다** — 번들 하나로 합치면 `@import`도 `<link>` 다중도 아니게 됩니다.

▸ **폰트 2단은 별개로 손볼 여지가 있습니다.** 로컬 CSS 4단과 달리 폰트는 `<link rel="preconnect">` + `preload`로 빼도 순서 의존이 없습니다. 이건 392개 파일을 안 건드리고도 가능한 부분입니다.

### 73. `loading="lazy"`를 11,587개에 적용하고 1,915개는 제외하셨습니다. 기준이 뭔가요? 🔥

**A.** **첫 화면에 보이는 이미지는 일부러 뺐습니다.** 제외한 1,915개는 헤더 로고와 본문 앞쪽 4개입니다.

`loading="lazy"`는 뷰포트에서 먼 이미지의 로딩을 브라우저가 미루는 속성인데, **첫 화면 이미지에 걸면 LCP가 오히려 나빠집니다.** 가장 큰 요소의 로딩 시작이 늦어지기 때문입니다. 전부 적용하는 게 최적화가 아니라, 적용하지 말아야 할 곳을 고르는 게 최적화입니다.

| | 처리 | 개수 |
| --- | --- | --- |
| 뷰포트 밖 | `loading="lazy"` | **11,587** |
| 헤더 로고 · 본문 앞 4개 | 즉시 로드 | 1,915 |

▸ **왜 헤더 로고가 대상인가** — 392페이지에 전부 있고 항상 첫 화면입니다. 여기에 lazy가 걸리면 페이지를 열 때마다 로고가 늦게 뜹니다.

▸ **더 갈 수 있는 부분** — 첫 화면 이미지에는 `fetchpriority="high"`를 붙이면 우선순위를 올릴 수 있습니다. 지금은 제외만 했고 승격은 안 했습니다.

### 74. 그럼 모든 이미지에 `loading="lazy"`를 적용하면 되나요?

**A.** 아닙니다. 73번에서 말씀드린 대로 **첫 화면 이미지에 걸면 LCP가 나빠집니다.**

원칙은 이렇습니다.
```html
<img src="hero.webp" fetchpriority="high" decoding="async" alt="...">   <!-- 첫 화면 -->
<img src="card.webp" loading="lazy" decoding="async" alt="...">          <!-- 그 아래 -->
```
`loading="lazy"`는 **뷰포트 밖 이미지에만** 의미가 있습니다. 그리고 `decoding="async"`는 디코딩을 메인 스레드에서 떼어내는 별개 속성이라 둘 다 붙일 수 있습니다.

### 75. 이미지 962.8MB를 92.2MB로 줄이셨습니다. 어떻게 하셨나요? 🔥

**A.** PNG 3,411장을 **WebP 품질 85로 일괄 변환**했습니다.

| | 이전 | 이후 |
| --- | --- | --- |
| 포맷 | PNG 3,410장 | **WebP 3,411장** |
| 용량 | 962.8MB | **92.2MB** (−90.4%) |
| 저장소 게시분 | 970MB | **531MB** (WebP 92 · 영상 439) |

**품질 85를 고른 이유**는 육안 손실이 거의 없는 구간이면서 감축폭이 가장 큰 지점이기 때문입니다. 럭셔리 브랜드 사이트라 이미지 품질이 곧 브랜드 자산이어서, 더 낮추면 얻는 용량보다 잃는 게 큽니다.

**그런데 이 작업의 진짜 위험은 용량이 아니라 참조 무결성입니다.** 3,411장의 확장자가 바뀌면 그걸 가리키는 모든 경로가 깨질 수 있습니다. 그래서 전수 검증했습니다.

| 검증 대상 | 결과 |
| --- | --- |
| HTML `src` | 2,792개 — 깨진 경로 **0** |
| CSS `url()` | 45개 — 깨진 경로 **0** |

▸ **여기서 기존 결함이 하나 나왔습니다.** 최초 검증에서 CSS `url()` 46개 중 1건(`listPage_bags_bg_03`)이 누락으로 잡혔는데, 확인해보니 **WebP 전환 이전부터 없던 파일**이었습니다. 이번 작업으로 깨진 게 아니라 **원래 있던 결함이 검증 과정에서 드러난 것**입니다.
없는 이미지를 가리키는 선언이라 복구할 원본도 없어서, **깨진 배경 참조 4줄을 제거**했습니다. 그래서 지금은 45개 전수 정상입니다.
**전환 작업이 기존 결함을 찾아서 없애준 셈**이고, 이런 건 전수 대조를 안 하면 영원히 안 보입니다. 화면상으로는 배경이 하나 안 나올 뿐이라 눈으로는 이상하지 않습니다.

▸ **그리고 코드는 한 줄도 안 고쳤습니다.** 변환 실패 0건, `alt` 13,502개와 호버 쌍 655개 전수 유지, **CSS·JS 변경 0줄**입니다.
JS를 안 고쳐도 된 이유가 설계에 있습니다. 호버 이미지 경로를 HTML에 적지 않고 **`src`의 확장자에서 정규식으로 도출**하는 방식이라(`.확장자$` → `_hover.확장자`), 확장자가 `.png`에서 `.webp`로 바뀌어도 규칙이 그대로 성립합니다.
**파일명 규칙에 의존하는 구조를 만들어둔 게 2주 뒤 포맷 전환 비용을 0으로 만든 사례**입니다. 만약 HTML에 `_hover.png` 경로를 직접 적어뒀다면 655곳을 고쳐야 했습니다.

▸ **남은 439MB는 영상입니다.** 이미지는 90% 줄였지만 영상은 손대지 않았습니다. GitHub Pages 1GB 제한 기준으로 531MB라 아직 여유가 있어서 우선순위를 뒤로 뒀습니다.

### 76. 반응형 이미지와 포맷 폴백은 어떻게 구성하겠습니까?

**A.** 같은 이미지의 **해상도 선택**은 `srcset` + `sizes`, **포맷 폴백**은 `<picture>` + `<source type>`입니다. 역할이 다릅니다.

```html
<picture>
  <source srcset="a.avif" type="image/avif">
  <source srcset="a.webp" type="image/webp">
  <img src="a.jpg" srcset="a-400.jpg 400w, a-800.jpg 800w"
       sizes="(max-width: 767px) 100vw, 265px"
       width="800" height="600" loading="lazy" alt="...">
</picture>
```

▸ **현재 JWA는 폴백이 없습니다.** WebP로 전환하면서 원본 PNG를 삭제했습니다. WebP는 주요 브라우저에서 모두 지원되므로 실용적 판단이었지만, **엄밀하게는 `<picture>` 폴백을 두는 게 맞습니다.**

▸ **개별 태그보다 먼저 깔아야 하는 게 전역 안전장치입니다.** `reset.css`에 이 규칙을 넣었습니다.
```css
img { max-width: 100%; height: auto; }
```
이미지 13,502개에 일일이 대응하는 건 어렵지만, **reset에 한 번 선언하면 어떤 이미지가 들어와도 부모 폭을 넘지 않습니다.** 없으면 큰 이미지 하나가 컨테이너를 밀어 **가로 스크롤이 생깁니다**(NFR-RWD-02 위반). `height: auto`를 같이 주는 이유는, `max-width`만 주면 `width`/`height` 속성이 있는 이미지의 세로 비율이 깨지기 때문입니다.

### 77. `:focus-visible`을 전역으로 선언하셨는데, 어떻게 구현하셨나요? 🔥

**A.** `reset.css`에 전역으로 한 번 선언했습니다.

```css
:focus-visible { outline: 2px solid var(--gs06); outline-offset: 2px; }
```

**컴포넌트마다 붙이지 않고 reset에 둔 이유**는 커버리지 때문입니다. 인터랙션 요소가 392페이지에 흩어져 있는데 개별로 붙이면 반드시 빠지는 곳이 생깁니다. 전역에 한 줄 두면 **새로 추가되는 요소도 자동으로 적용**됩니다. `img` 안전장치를 reset에 넣은 것과 같은 판단입니다.

**색은 토큰(`--gs06` #252525)으로 지정했습니다.** 하드코딩 0건 원칙에 맞추기도 했고, 나중에 브랜드 톤을 바꾸면 포커스 표시도 같이 따라옵니다.

**`outline-offset: 2px`을 준 이유**는 요소 경계에 딱 붙으면 어두운 배경에서 안 보이기 때문입니다. 2px 띄우면 배경색이 사이에 들어와 대비가 살아납니다.

▸ **`outline`을 쓴 이유** — `border`는 레이아웃을 밀고, `box-shadow`는 Windows 고대비 모드에서 사라집니다. `outline`은 문서 흐름에 영향을 주지 않으면서 강제 색상 모드에서도 남습니다.

▸ **그런데 포커스 표시를 넣고 나서 오히려 새 문제가 드러났습니다.** 햄버거 메뉴·사이드 메뉴·동의 체크박스처럼 **체크박스로 만든 토글들이 `display: none`으로 숨겨져 있어서 키보드 탭 순서에서 아예 빠집니다.**
포커스 표시가 없을 때는 안 보였던 문제입니다. 표시를 켜고 탭으로 훑어보니 **도달 자체가 안 되는 요소**가 드러났습니다. `display: none`은 포커스를 받을 수 없기 때문입니다.
해결 방향은 명확합니다 — **`display: none` 대신 시각적 숨김(`clip-path` 방식)으로 교체**하면 화면에서는 안 보이면서 포커스는 받습니다. 지금 남은 과제로 공개해뒀습니다.

▸ **이게 접근성 작업의 전형적인 순서라고 봅니다.** 하나를 고치면 그다음 문제가 보입니다. 포커스 표시 → 도달 가능성 → 조작 가능성 순인데, 지금은 두 번째 단계에 있습니다.

▸ **그 밖에 남은 것** — 메가 메뉴 키보드 조작(NFR-A11Y-05) 미착수, 커스텀 드롭다운 키보드 조작, 팝업 포커스 가두기 + Esc, 슬라이더 이동 음성 안내. **접근성이 끝났다고 말씀드리지는 않겠습니다.**

### 78. `:focus`와 `:focus-visible`의 차이는 무엇인가요?

**A.** `:focus`는 **마우스 클릭을 포함해 포커스를 받은 모든 경우**에 적용됩니다. `:focus-visible`은 **브라우저가 시각적 표시가 필요하다고 판단할 때만** 적용되고, 실제로는 주로 키보드 탐색입니다.

이 구분이 필요한 이유는 실무에서 자주 나오는 갈등 때문입니다. 마우스로 버튼을 눌렀을 때 아웃라인이 남는 게 싫어서 `outline: none`을 쓰는 경우가 많은데, **그러면 키보드 사용자가 위치를 완전히 잃습니다.** `:focus-visible`이 정확히 이 문제를 풉니다 — 마우스에는 안 나오고 키보드에는 나옵니다.

```css
:focus { outline: none; }
:focus-visible { outline: 2px solid var(--gs06); outline-offset: 2px; }
```

▸ 브라우저 지원이 넓어져서 지금은 이 조합이 표준적인 방법입니다. 구형 대응이 필요하면 `:focus` 폴백을 먼저 두고 `:focus-visible`로 덮는 순서로 씁니다.

### 79. 메가 메뉴 `aria-expanded` · 키보드 미착수 ⚠️

**A.** 미착수입니다. 호버 전용 메뉴는 세 가지가 문제입니다.
① 키보드 사용자가 하위 메뉴에 도달할 수 없음
② 터치 기기에서 첫 탭이 열기인지 이동인지 불분명
③ 스크린리더가 확장 상태를 알 수 없음
**HOOT UP에서는 이걸 전부 구현했습니다.** `tablist`/`tab`/`tabpanel` ARIA 패턴, ↑↓ Home End 이동, Esc 닫기, 로빙 tabindex, 닫을 때 트리거로 포커스 복귀. 그리고 닫힘 상태를 `opacity: 0`이 아니라 `visibility: hidden`으로 처리해서 보이지 않는 링크가 탭 순서에 남지 않게 했습니다.
HOOT UP에서 구현한 방식을 JWA에도 적용할 수 있습니다.

### 80. `<title>`을 392개 전부 고유하게 만드셨는데, 원래는 어땠나요? 🔥

**A.** 원래는 **고유 63개, 최대 32개 중복**이었습니다. 지금은 **392 / 392 고유, 중복 0종**입니다.

**중복이 생긴 원인은 구조입니다.** 상세 페이지를 개별 HTML로 대량 생성하면서(ADR-07) 템플릿의 `<title>`을 그대로 복제했습니다. `Mens - Knitwear` 같은 카테고리 제목이 그 카테고리 상품 전체에 붙었습니다.

**중복이 실제로 일으키는 문제는 세 가지입니다.**
① **접근성** — 스크린리더 사용자가 탭을 전환할 때 어느 페이지인지 알 수 없습니다
② **SEO** — 검색 결과에서 페이지가 구분되지 않습니다
③ **사용성** — 브라우저 히스토리와 북마크가 전부 같은 이름이 됩니다

②만 생각하면 "포트폴리오니까 상관없다"가 되는데, **①은 실사용자 문제**라 그렇게 넘길 수 없다고 봤습니다.

▸ **그리고 `title`이 저작권 고지의 일부이기도 합니다.** 전 페이지 제목에 `JW Anderson(Portfolio) | …` 형식으로 **(Portfolio)를 병기**해서, 검색 결과나 브라우저 탭에서도 실제 브랜드와 구분되게 했습니다. 푸터 고지는 페이지를 열어야 보이지만 `title`은 탭과 검색 결과에 먼저 노출됩니다 → 30번.

**규칙은 페이지 성격별로 나눴습니다.**

| 유형 | 붙인 것 |
| --- | --- |
| 상품 상세 | 브레드크럼 **마지막 항목(상품명)** |
| 주문 상태 페이지 | **상태명** (Shipped / Delivered / Completed / Cancel) |
| 분할된 목록 | **Page 번호** |

기존 `브랜드 | 섹션 - 카테고리` 구조와 `(Portfolio)` 표기는 그대로 두고, **뒤에 구분값만 덧붙이는 방식**입니다. 구조를 바꾸면 63개 고유 제목까지 다시 검토해야 하는데, 덧붙이기만 하면 기존 것은 건드릴 필요가 없습니다.

▸ **작업 규모는 335개 파일이고, 손댄 건 `<title>` 한 줄뿐입니다.** 반응형 구조나 클래스는 건드리지 않았습니다. **고칠 범위를 한 줄로 좁혀두면 회귀 위험도 그만큼 줄어듭니다.**

▸ **파일명 규칙이 있어서 자동화가 가능했습니다.** 상세가 `{cat}{Product}{Color}` 형식이라 파일명 자체가 상품 구분값입니다. **명명 규칙을 지켜둔 게 나중에 자동화 가능성으로 돌아온 사례**입니다.

### 81. 랜드마크를 전 페이지에 넣으셨는데, 처음부터 그랬나요? 🔥

**A.** 아니었습니다. 처음에는 **392페이지 중 2페이지에만** `<main>`이 있었습니다.

`<main>`은 스크린리더 사용자가 **반복되는 헤더·내비를 건너뛰고 본문으로 바로 가는 통로**입니다. 없으면 페이지를 옮길 때마다 같은 헤더를 처음부터 다시 듣게 됩니다. WCAG 2.4.1(반복 블록 우회)이 요구하는 항목이고, 이 사이트처럼 헤더가 전 페이지에 복제된 구조에서는 특히 중요합니다.

**문제는 제 표기 방식이었습니다.** NFR-A11Y-02를 "적용"이라고 써놨는데, 그 판단 근거가 `header`/`nav`/`footer`/`section`을 썼다는 것이었습니다. **랜드마크 커버리지를 세지 않고 적용이라고 선언한 것**입니다. 63번의 색상 집계와 같은 구조의 실수입니다 — 좁은 기준으로 재고 넓게 말했습니다.

**지금은 세어서 넣었습니다.**

| 랜드마크 | 커버리지 |
| --- | --- |
| `header` / `nav` / `main` / `footer` | 각 **392 / 392** |

성공지표에도 「반복 블록 우회 수단 (WCAG 2.4.1) — 392/392 달성」으로 별도 행을 만들었습니다. "시맨틱 태그를 썼다"와 "랜드마크가 전 페이지에 있다"는 다른 주장이라 분리해야 합니다.

▸ **작업 방식도 말씀드릴 만합니다.** 기존 래퍼의 **태그만 `<main>`으로 교체하고 클래스는 전수 보존**했습니다. 그래서 **CSS 변경 0건**, 여닫기 짝 불일치 0건입니다. 새 요소를 감싸는 방식이었다면 392페이지의 레이아웃을 다시 검증해야 했습니다.

▸ **여기서 얻은 규칙**: **커버리지는 비율로 재야 합니다.** '적용/미적용'으로 쓰면 2/392도 적용이 됩니다. HOOT UP에서는 이 축을 처음부터 그렇게 관리했습니다 — 스킵 링크 164/164, 페이지당 h1 1개 × 157을 매 회차 전수 측정했습니다.

▸ **랜드마크와 포커스 표시는 한 세트입니다.** 본문으로 건너뛰어도 현재 위치가 안 보이면 소용이 없어서, `:focus-visible`도 같은 시기에 reset 전역으로 넣었습니다 → 77번. 다만 그 작업에서 **체크박스 토글이 탭 순서에서 빠져 있다**는 새 문제가 드러나 아직 남아 있습니다.

### 82. 명도 대비를 10.85:1로 측정하셨는데, 본문 조합 하나만 잰 것 아닌가요? 🔥

**A.** 초기에는 측정하지 않고 모노톤 팔레트라 확보됐을 거라고 **가정**했습니다. 지금은 측정해서 값을 문서에 넣었습니다.

> NFR-A11Y-06 — 본문 텍스트 명도 대비 4.5:1 이상
> 실측 `--gs05(#333333)` on `--be01(#f3ede3)` = **10.85:1 — 달성**

AA 기준 4.5:1의 두 배가 넘습니다. 다만 이건 **본문 조합 하나**를 잰 값이고, 보조 텍스트(`--gs03 #999999`)나 비활성 상태까지 전수 측정한 것은 아닙니다. 그 부분은 아직 미측정이라고 말씀드리는 게 정확합니다.

**그리고 '가정하면 틀린다'는 걸 HOOT UP에서 실제로 확인했습니다.** 배포본 Lighthouse 측정에서 대비 위반 45건이 나왔고, 원인 3건으로 정리했습니다.
| 원인 | 조치 |
| --- | --- |
| `text-muted` `#93a2b7` | → `#667286` |
| 어두운 컨테이너 안에서 토큰이 부적합 | 컨테이너 내 토큰 재정의 |
| `danger-base` / 블로그 라벨 민트 | → danger dark(5.75) / 브랜드 블루(4.82) |

결과적으로 45건의 위반이 세 가지 공통 원인에서 발생했다는 점이 중요했습니다. 토큰 값을 고치니 45곳이 한 번에 해소됐습니다. 토큰 체계의 실익이 여기서 나옵니다. 재측정 결과 접근성 100입니다.

### 83. JW Anderson에서 jQuery를 사용한 이유는 무엇인가요?

**A.** 당시 bxSlider와 fitVids 같은 기존 플러그인을 사용하면서 jQuery 의존이 들어갔습니다. 교육과 프로젝트를 진행하던 시점에 익숙한 방식으로 구현한 부분이 남아 있는 것입니다.

현재 다시 만든다면 먼저 브라우저 표준 API와 CSS만으로 대체할 수 있는지 검토하겠습니다. 실제로 HOOT UP은 jQuery 없이 Vanilla JavaScript로 구성했습니다.

### 84. `jquery-latest.min.js` 사용이 왜 문제인가요?

**A.** 버전을 명확하게 고정하지 않는 방식은 재현성과 유지보수 측면에서 좋지 않습니다. 어떤 시점에 어떤 버전이 로드되는지 불명확하면 동일한 코드를 다시 실행했을 때 결과를 보장하기 어렵습니다.

따라서 jQuery를 반드시 사용해야 한다면 특정 버전을 명시하고, CDN을 사용할 경우 무결성 확인을 위한 SRI도 함께 적용하는 것이 좋다고 생각합니다.

JW Anderson의 `jquery-latest.min.js` 사용은 지금 기준에서는 교체해야 할 부분입니다.

### 85. jQuery 의존성을 제거한다면 어떻게 대체하겠습니까?

**A.** bxSlider는 CSS Scroll Snap과 `scrollTo()`, 필요하면 `IntersectionObserver`를 조합해서 대체할 수 있고, fitVids는 현재 CSS의 `aspect-ratio`만으로도 상당 부분 대체할 수 있습니다.

DOM 선택이나 클래스 조작, Ajax도 `querySelector`, `classList`, `fetch` 같은 표준 API가 있기 때문에 신규 프로젝트라면 jQuery 없이 구현하는 방향을 우선 검토하겠습니다.
## 4-6. 마크업 · 구조

### 86. 개별 HTML 생성 · 공통 변경 대응

**A.** 현재는 일괄 치환 스크립트 전제입니다. 실제로 HOOT UP에서 로고 `href`를 157개 파일에서 한 번에 바꿨습니다.
**한계는 명확하고 ADR에 적어뒀습니다.** HOOT UP ADR-06에 "헤더 약 219줄 × 161파일 복제, GNB 1항목 수정에 전 파일 치환 필요 (인지·감수)"라고 썼습니다. 인지하고 감수한 부채이지 모르고 만든 구조가 아닙니다.
**실무라면 세 가지 중 하나입니다.**
① 템플릿 엔진 / SSG (11ty, Astro, Nunjucks)
② 서버 인클루드 (SSI, PHP include)
③ CMS
정적 사이트로 간 이유는 **퍼블리싱 산출물 자체의 투명성**과 GitHub Pages 제약이었습니다. 빌드 결과물이 아니라 제가 쓴 마크업이 그대로 보여야 검수 대상이 됩니다.

▸ **대신 파일명 규칙으로 관리 가능성을 확보했습니다.**
```
listPage{Cat}              랜딩 5
listCardSlotPage{Sub}      그리드 43
{cat}{Product}{Color}      상세 320
```
**파일명이 곧 주소이자 상품 구분값**이라, 별도 설정이나 매핑 테이블 없이 320개 상세가 서로 크로스 링크됩니다. 컬러 스와치를 누르면 같은 상품의 다른 컬러 상세로 가는 것도 이 규칙 덕분입니다. 규칙을 어기면 링크가 바로 깨지므로 **구조가 스스로 검증됩니다.**

### 87. `alt` 13,502개를 어떻게 작성했나 🔥

**A.** 상품 이미지가 대부분이라 **상품명 + 컬러 + 뷰(front/back/detail)** 조합의 규칙으로 작성했습니다. 상품 데이터가 고정 세트라 규칙 기반 생성이 가능한 구조였습니다.
**정직하게 말씀드리면, 13,502개를 한 장씩 보고 쓴 것은 아닙니다.** 규칙으로 생성하고 유형별로 표본 검수했습니다.
**HOOT UP에서는 방식을 바꿨습니다.** alt 3,765개를 **서술형 2,705 / 장식용 `alt=""` 1,060**으로 분리했습니다. 강의 썸네일 alt는 장식이 아니라 **장면 서술**로 작성했고, 별점 그래픽 앞에는 숨김 텍스트로 "평점 5점 만점에", 장바구니·알림 배지에는 "2개 담김"·"2건 읽지 않음"을 넣었습니다.

### 88. 장식용 이미지 처리 — 두 프로젝트 기준이 다르다 ⚠️

**A.** 다릅니다. 그리고 **HOOT UP 기준이 맞습니다.**
JWA는 "alt 누락 0건"만 셌습니다. 전부 채웠다는 뜻인데, **장식 이미지에 설명을 채우면 오히려 접근성이 나빠집니다.** 스크린리더가 의미 없는 텍스트를 읽습니다.
올바른 기준은 이렇습니다.
| 유형 | 처리 |
| --- | --- |
| 정보 전달 | 서술형 `alt` |
| 장식 | `alt=""` (속성은 있고 값이 빈 것) |
| `alt` 속성 자체 누락 | **금지** — 스크린리더가 파일명을 읽음 |
| 인접 텍스트와 중복 | `alt=""` |

`alt` 없음과 `alt=""`는 완전히 다릅니다. JWA는 이 구분을 하지 않았고, HOOT UP에서 1,060개를 장식으로 분리했습니다.

### 89. `jwanderson.js` 509줄 내용

**A.** 크게 여섯 덩어리입니다.

**① 팝업·패널 시스템 (39종)** — 핵심은 **계약 하나**입니다.
```
[data-panel="X"] 클릭  →  #X 에 .active 부여
#X .btn_close 클릭    →  #X 에서 .active 제거
```
버튼의 `data-panel` 값 = 열릴 창의 `id`로 1:1 짝을 지었습니다. **새 창을 추가할 때 JS를 안 고칩니다** — 규칙대로 HTML만 쓰면 됩니다. 그리고 규칙을 어기면 창이 아예 안 열리므로 **스스로 검증됩니다.**
다음 창을 열 때 이전 창을 반드시 닫도록 해서 팝업이 겹쳐 쌓이는 경우가 0건입니다. 사이즈 가이드 21종은 이름 부분일치로 함수 하나가 처리합니다.

**② 커스텀 셀렉트** — 사이즈·정렬 드롭다운. 옵션 클릭 시 이벤트 전파를 차단합니다. 안 막으면 바깥 클릭 감지에 걸려서 닫히자마자 다시 열립니다. 카드 14개가 한 화면에 있어도 서로 간섭 0입니다.

**③ 사이즈 선택 잠금 (3단계)** — 미선택이면 버튼이 안 눌리고, 사이즈를 고르면 버튼이 '담기'로 바뀌면서 `data-panel`이 붙습니다. **이 버튼은 나중에 생기므로 이벤트 위임으로 연결해야 동작합니다.**

**④ 수량·금액 연동** — `span.qtyComponent[data-price]`의 ± 버튼과 `input.qtyVal`로 줄 금액을 갱신하고 전체를 합산합니다. 1~99 제한, 천 단위 콤마, **값이 깨진 줄은 건너뜁니다** — 한 줄 오류가 전체 합계를 망치지 않게.

**⑤ 슬라이더 3구간** — `getBreakpoint(w)`로 mobile(1장)·tablet(2장)·desktop(375px 3장)을 판별하고, 리사이즈 시 **0.3초 대기 후 구간이 바뀐 경우에만** 다시 그립니다.

**⑥ 이미지 호버 교체 (655쌍)** — `{이름}.png ↔ {이름}_hover.png` 파일명 규칙. 미리 불러둬서 첫 마우스오버에 깜빡임이 없고, **HTML에 교체 경로를 안 적습니다.**

여기에 헤더 스크롤 시 `.scrolled` 전환, 전화번호 입력에서 숫자 외 문자 자동 제거, 체크아웃 단계 잠금이 붙습니다.

▸ **덧붙일 만한 것**: 상태 클래스를 **7개만** 씁니다 — `active` `filled` `disabled` `scrolled` `headerFixed` `headerFixedBar` `currentVal`. 모바일 대응을 추가하면서 **새로 만든 상태가 0개**입니다.
▸ **계정 사이드 메뉴는 이 파일에 없습니다.** 체크박스 + 형제 선택자로 만들어서 **JS 0줄**입니다.

### 90. 정적 페이지에서 로그인 상태 표현

**A.** JWA는 사인인 패널을 열고 로그인 후 사용자명 노출 상태를 마크업으로 표현했습니다.
**HOOT UP에서는 이걸 아예 설계 결정으로 승격했습니다(ADR-01).** JS로 상태를 흉내내지 않고 **파일로 분리**했습니다 — `index.html`(비로그인)과 `index-member.html`(로그인 후), 내부 163페이지는 로그인 후 상태.
근거는 이것입니다. **서버가 있어야 정해지는 상태는 상태별 파일로 명시한다.** JS로 토글하면 "구현했다"는 착시가 생기는데, 실제로는 인증도 세션도 없습니다. 파일로 나누면 무엇이 시뮬레이션인지 정직하게 드러납니다.
트레이드오프는 관람자에게 설명이 필요하다는 것이고, README에 컨벤션으로 명시해서 해소했습니다.

### 91. JW Anderson의 커밋 메시지 규칙은 어떻게 관리했나요?

**A.** JW Anderson에서는 Conventional Commits를 처음부터 사용하지 않았고, 작업 대상과 작업 성격이 보이도록 제가 만든 한글 규칙을 사용했습니다.

예를 들어 `page(qtyComponent) base 이동 및 D,T,M 삭제 완료`처럼 어떤 컴포넌트에서 어떤 작업을 했는지 커밋 메시지에 남겼습니다.

이 방식 덕분에 문제가 생겼을 때 특정 컴포넌트의 변경 지점을 비교적 쉽게 찾을 수 있었지만, 팀 협업에서 통용되는 표준 형식은 아니라는 한계가 있었습니다.

▸ **수치로 말씀드리면**: 커밋 105건(2026-07-19~07-27, 9일), 이후 문서·수치 정정과 브레이크포인트 조정, M6 잔여 작업을 포함해 현재 **124건**입니다. CLI 122건 · 웹 인터페이스 업로드 2건이고, 그중 구획 단위 리팩터링 커밋이 34건(page 25 · global 9)입니다.

▸ **Conventional Commits 관련해서 정직하게 덧붙일 게 있습니다.** 최근 작업부터 접두사 체계로 전환 중인데, **124건 중 규격을 실제로 통과하는 것은 15건뿐**입니다. 접두사를 붙인 커밋이 그보다 많은데도 15건인 이유는, 나머지가 `fix : 내용`처럼 **콜론 앞에 공백을 넣어서** commitlint 파서가 인식하지 못하기 때문입니다.
**형식을 지켰다고 생각했는데 도구는 아니라고 판정한 경우**입니다. 규격은 사람이 보기에 비슷한 것과 파서가 통과시키는 것이 다르고, 그래서 **린트를 붙이기 전까지는 지켜지고 있다고 말할 수 없다**는 걸 다시 확인했습니다. `commitlint` + `husky`를 커밋 훅에 걸면 애초에 통과가 안 됩니다.

### 92. HOOT UP에서는 왜 Conventional Commits 방식으로 바꿨나요?

**A.** 개인 규칙보다 협업 도구와 연동하기 쉬운 형식이 필요하다고 판단했기 때문입니다.

HOOT UP에서는 8월 1일부터 `feat`, `style`, `fix` 같은 접두어를 사용하기 시작했습니다. 자동 changelog나 release 도구가 이런 형식을 활용할 수 있다는 점도 고려했습니다.

실무에서는 제 방식보다 팀의 커밋 컨벤션을 우선 따르겠습니다. 형식과 관계없이 무엇을 왜 바꿨는지가 메시지에서 이해되어야 한다는 원칙은 유지하고 싶습니다.
---

# 05. HOOT UP — AI 파이프라인 디렉팅 🔥

## 5-1. 근본 질문

### 93. AI가 다 짰다면 본인 실력은 뭘로 증명하나 🔥🔥

> **이 질문 하나가 HOOT UP 전체의 승부처입니다. 핵심 논리를 이해해 두는 것이 좋습니다.**

**A.** 세 가지로 답변드리겠습니다.

**① 검수는 실력 없이 불가능합니다.**
AI가 `.desk-page a { color: inherit }` 때문에 활성 칩 색이 안 먹는 걸 만들었을 때, 저는 값을 바꾸라고 하지 않고 **"다른 곳에 덮어쓰는 속성이 있는지 찾아봐"**라고 지시했습니다. 명시도 (0,1,1)이 (0,1,0)을 덮고 있었고, 같은 원인이 활성 페이저·기간 칩·프로모 버튼 세 곳에서 동시에 발생하고 있었습니다.
**명시도를 모르면 이 지시가 안 나옵니다.** 증상만 세 번 따로 고칩니다.

**② 손코딩은 JWA로 별도 증명했습니다.**
392페이지를 직접 짜고 CSS를 42% 줄인 프로젝트가 옆에 있습니다. 두 프로젝트를 나눈 이유가 정확히 이 질문 때문입니다.

**③ AI가 못 하는 것을 제가 했습니다.**
브랜드 기획, 강의 카탈로그 152종 설계, Figma 디자인과 변수 체계 구축, 토큰 네이밍 규칙과 접근성 요구사항의 PRD 명문화. README 「역할 구분」 표에 **사람이 한 것 / AI가 한 것**을 명시적으로 갈라놨습니다. 감출 생각이 없습니다.

### 94. AI 없이 164페이지를 직접 짤 수 있나 🔥

**A.** 시간이 더 걸릴 뿐 가능합니다. **JWA에서 392페이지를 9일에 직접 짰습니다.** 그게 근거입니다.
AI를 쓴 이유는 못 해서가 아니라, **같은 것을 두 번 증명할 필요가 없어서**입니다. 두 번째 프로젝트에서는 다른 것을 증명해야 했습니다.

### 95. AI를 못 쓰는 환경이라면 🔥

**A.** 문제 없습니다. JWA가 그 환경에서 나온 결과물입니다.
그리고 **AI를 쓰든 안 쓰든 제 작업 방식은 같습니다.** 요구사항을 문서로 고정하고, 산출물을 원본·표준·개수와 대조합니다. 대조 대상이 AI 산출물이든 제 코드든 절차는 동일합니다.
실제로 JWA에서도 같은 절차였습니다. 리팩터링 전후 셀렉터를 전체 대조해서 스타일 유실 3건을 당일 잡았습니다. AI가 없어도 하던 일입니다.

### 96. 회사 보안 정책상 AI 반입 금지 🔥

**A.** 따르겠습니다. 사내 정책이 있으면 그게 우선입니다.
그리고 이건 제가 **미리 인지하고 있는 리스크**입니다. 그래서 포트폴리오를 AI 프로젝트 하나로 구성하지 않았습니다.

### 97. AI 생성 코드의 저작권·라이선스

**A.** 제가 아는 범위에서 말씀드리면, 현재 국내법상 AI 생성물의 저작권 귀속은 확립된 판례가 없는 상태로 알고 있습니다. 서비스 약관상으로는 출력물의 권리를 사용자에게 귀속시키는 게 일반적입니다.
**실무 리스크는 저작권보다 다른 쪽이라고 봅니다.**
① AI가 학습 데이터의 코드를 유사하게 재생성할 가능성
② **사내 코드를 프롬프트에 넣는 순간 발생하는 정보 유출**
②가 훨씬 현실적인 위험이고, 그래서 회사 정책을 따르는 게 맞다고 봅니다.
▸ 확신 없는 법적 판단은 단정하지 말 것. "제가 아는 범위에서는"을 붙이세요.

### 98. AI 코드 중 이해 못 한 부분이 있었나 🔥

**A.** 있었습니다. 그리고 그때 **반려하는 대신 물었습니다.**
README에 남긴 사례가 있습니다. "`main`이 모든 페이지에 필요한가? index에만 있으면 되는 것 아닌가"라고 질문했고, 문서마다 하나씩 필요하다는 표준 설명을 듣고 **원안을 유지**했습니다.
**검수는 전부 지적으로 끝나지 않습니다. 근거가 타당하면 원안 유지도 검수 결과입니다.** 이 사례를 일부러 README에 남겼습니다. 반려만 기록하면 검수가 아니라 트집이 됩니다.
▸ 꼬리질문 대비: "그럼 AI 설명이 틀렸으면?" → "그래서 표준 문서로 재확인합니다. MDN과 W3C validator가 최종 판정입니다."

### 99. JWA와 HOOT UP 중 어느 쪽이 진짜 실력인가

**A.** **JWA가 기초 체력이고, HOOT UP이 현재 실력입니다.**
JWA 없이는 HOOT UP의 검수가 불가능하고, HOOT UP 없이는 제가 2026년에 어떻게 일하는지 보여줄 수 없습니다.
▸ 회사 성향에 따라 강조점 조정. 손코딩 검증 중시 → JWA / AI 활용 적극 → HOOT UP.

### 100. B안(Next.js)을 왜 굳이 공개했나

**A.** **제가 검수할 수 없는 범위를 표시하기 위해서입니다.**
README에 A안/B안을 나눈 기준을 적었습니다 — "내가 검수할 수 있는 범위인가". A안은 학습을 마친 HTML/CSS라 AI 코드를 읽고 판단하고 반려할 수 있습니다. B안은 학습 중인 React·Next.js라 AI를 구현 도구가 아니라 학습 도구로 씁니다.
그리고 **"같은 수준의 검수를 주장하지 않습니다"**라고 명시했습니다.
숨기면 "React도 합니다"로 읽히고, 물어보면 무너집니다. 먼저 선을 그어두면 그 선이 신뢰가 됩니다.

## 5-2. 검수 프로세스

### 101. 359건 분류 체계

**A.** 5종으로 분류했습니다.

| 분류 | 성격 | 대표 사례 |
| --- | --- | --- |
| **표준 위반** | 화면에 안 드러남. validator로만 검출 | meta description 끝 `)` 오류 → 15개 파일 수정 |
| **인터랙션 결함** | 동작이 의도와 다름 | 썸네일 확대 애니메이션이 할인 배지를 덮음 |
| **구조 · 리팩터링** | 동작은 하지만 구조가 나쁨 | 의미 없는 `main > div.l-container` 래퍼 50개 파일 제거 |
| **데이터 정합성** | 표시값과 실제 데이터 불일치 | 카테고리별 기사 수, 존재하지 않는 강의명 |
| **명명 정합성** | 콘텐츠와 코드 명칭 어긋남 | `러닝 & 유산소`인데 파일명이 `cardio` |

**분류가 필요한 이유는 검출 방법이 다르기 때문입니다.** 표준 위반은 도구, 데이터 정합성은 원본 대조, 인터랙션은 실제 조작. 분류를 해두면 새 작업에서 어떤 검사를 돌려야 하는지가 정해집니다.

### 102. "증상이 아니라 규칙을 지목한다"

**A.** desk 활성 칩 폰트색 사례로 설명드리겠습니다.
```
.desk-page a { color: inherit }      /* 명시도 (0,1,1) */
.desk-period__chip--active { }       /* 명시도 (0,1,0) — 덮임 */
```
칩 색이 안 먹었습니다. 여기서 **"색을 바꿔줘"라고 하면 증상 지적**입니다. 값을 바꿔도 안 먹고, 억지로 `!important`를 붙이면 부채가 쌓입니다.
제가 지시한 건 **"다른 곳에 덮어쓰는 속성이 있는지 찾아봐"**였고, 테마 스코프의 포괄 규칙이 컴포넌트 색상을 반복적으로 덮고 있다는 원인이 나왔습니다.
**같은 원인이 세 곳에서 동시에 발생 중이었습니다** — 활성 페이저 번호, 기간 칩, 프로모 버튼. `.desk-page a.<class>` 형태로 명시도를 올리는 방식으로 **한 번에 통일 해결**했습니다.
증상만 지적했다면 세 번 따로 고쳤을 문제입니다.

▸ **같은 계열 사례 2**: 매거진 폰트 두께가 안 맞을 때도 "값을 바꿔라"가 아니라 **"다른 곳에 다른 속성이 있는지 찾아봐"**로 지시했습니다.
▸ **같은 계열 사례 3**: 콘텐츠 폭도 `"1170px 같은데 맞아?"` → `"표준 1160px로"` → **`"l-container처럼 min(1160px, 100%−40px) 방식으로 통일"`**. 값 지정에서 기존 표준 방식 재사용으로 지시가 올라갔습니다.

### 103. "16개 완료 / 실제 14개"를 어떻게 발견했나

**A.** **세었습니다.**
AI가 작업 후 스스로 요약 보고를 작성하는데, 그 보고가 **실제로 만들어진 것이 아니라 만들려고 계획한 것을 기술하는 경우**가 있습니다.
| 항목 | 보고 / 실제 | 검출 방법 |
| --- | --- | --- |
| 대분류별 할인 페이지 | 16개 완료 / **실제 14개** | 파일 개수 카운트 |
| 세부 페이지 필터바 제거 | 32개 완료 / **4개 누락** | 잔존 파일 목록 대조 |

**두 건 모두 8개 반복 처리의 마지막 항목(요리·베이킹)이 빠졌고, 둘 다 완료로 보고됐습니다.**
개별 사고가 아니라 **배치 작업의 실패 패턴**이라고 판단한 게 중요합니다. 그래서 개별 수정으로 끝내지 않았습니다.

### 104. 이후 도입한 고정 절차

**A.** 배치 작업 후 개수 검증을 절차로 넣었습니다.
```bash
ls category-*-earlybird.html | wc -l   # 기대값 8
ls category-*-event.html | wc -l       # 기대값 8
```
**효과가 다음 날 바로 나왔습니다.** 07-31 완료 보고 대조에서 카테고리별 기사 수(43/34/36/30/30), 합계 173, 중복 파일 삭제 5개, 사이드바 반영 31개 파일이 **전부 일치**했습니다.
08-01도 신규 16종, 토큰 10곳 치환, 컴파운드 클래스 5종, FAQ 리네이밍 274회·잔존 0, 이모지 0개 — 불일치 없음. **이틀 연속입니다.**

### 105. "전체 적용"이 부분 적용으로 끝난 게 왜 반복되나

**A.** 07-31에 세 번 반복됐습니다. 활성 칩 폰트색 미적용, 이미지 라운딩 `desk.html` 누락, 기사 상세 링크 미부착 — **세 건 모두 완료 보고 이후에 발견**됐습니다.
이미지 라운딩은 결국 **뉴스 카테고리 → desk.html → desk-ai-coding.html 세 번에 나눠 지시**해야 했습니다.
**원인은 "전체"의 범위가 지시하는 쪽과 실행하는 쪽에서 다르게 잡히기 때문**이라고 봅니다. 실행 측은 방금 작업한 컨텍스트를 "전체"로 인식합니다.
**대응은 하나입니다 — 지시하지 않은 곳까지 열어봐야 합니다.** 적용된 영역만 확인하면 통과해버립니다.
지금이라면 지시 방식을 바꾸겠습니다. "전체에 적용"이 아니라 **"대상 파일 목록을 먼저 출력하고, 그 목록에 적용한 뒤, 적용 후 개수를 보고할 것"**으로 3단 지시를 씁니다.

### 106. 존재하지 않는 강의명을 어떻게 잡았나

**A.** 카탈로그 152종을 **source of truth**로 두고 대조했습니다.
계정 페이지에 "왕초보 엑셀 데이터 정리 실무"라는 강의가 데모 데이터로 생성됐는데, **화면상으로는 전혀 이상하지 않습니다.** 그럴듯한 이름입니다.
제가 한 지시는 이겁니다 — **"'왕초보 엑셀 데이터 정리 실무' 같은 게 152개 안에 있어? 없으면 있는 걸로 바꿔"**
카탈로그를 제가 직접 설계했기 때문에 대조 기준이 있었습니다. **기준이 없으면 이 유형은 영원히 검출되지 않습니다.**

### 107. 비즈니스 규칙 단위 검증의 발상

**A.** 개별 값을 하나씩 대조하면 끝이 없어서입니다. 152종 × 여러 페이지를 다 볼 수 없습니다.
그래서 **규칙을 정의하고 규칙 위반을 찾는 쪽으로 바꿨습니다.**
```
개발&IT · 커리어&머니       = 단건
디자인&크리에이티브          = 구독 + 단건
나머지 5분야                = 구독 전용
```
이 규칙을 정의한 뒤, **내 클래스 · 위시리스트 · 주문내역이 규칙과 어긋나는지 전수 확인**시켰습니다. 여러 페이지에 흩어진 불일치가 한 번에 나옵니다.
**이 발상은 커머스 13년에서 왔습니다.** 상품 데이터를 검수할 때 개별 상품이 아니라 카테고리별 가격 정책, 배송 정책 단위로 봅니다. 같은 방식입니다.

▸ **JWA에서도 같은 유형을 다뤘습니다.** 결제 폼과 주소록 편집 팝업 등 **드롭다운 5곳**에 한국 광역자치단체 17개가 들어가는데, 이걸 개별로 확인하지 않고 **"5개 목록이 동일한 항목·동일한 순서를 공유하는가"**를 기준으로 봤습니다. 목록 하나만 순서가 다르거나 한 곳이 빠지면 사용자는 같은 폼인데 다른 결과를 봅니다.
**개별 값 85개(17×5)를 대조하는 대신 목록 간 일치를 보는 게 검증 단위**입니다.

### 108. 제약 조건을 함께 넣는 지시 패턴

**A.** 두 사례입니다.
> "썸네일 이미지에 호버 시 1.3배 커졌다 돌아오는 액션 추가. **단, 감싼 레이아웃은 항상 같은 크기여야 함.**"

앞부분만 지시했다면 카드 전체가 커지면서 그리드가 흔들렸을 요청입니다. 뒷문장 때문에 `overflow: hidden` + `transform` 조합으로 구현됐고, **`prefers-reduced-motion` 대응까지 함께** 들어갔습니다.
> 다크 CTA ↔ 푸터 경계 보더 — **"100% 전체 꽉 차는 보더"**

필터 동작도 같은 방식으로 명세했습니다. dev-it 한 페이지에서 규칙을 확정한 뒤 8개 대분류에 공통 적용했습니다.
```
전체 보기 = 얼리버드·이벤트 구분 없이 전체 표시
전체 보기에서는 두 칩 모두 비활성
각 칩은 클릭했을 때만 활성 (chip--brand + aria-current)
세부 카테고리에서는 필터바 자체를 삭제
```
**핵심은 "결함을 미리 막는 요구사항 작성"입니다.** 사후 검수보다 사전 제약이 싸게 먹힙니다.

### 109. `magazine.html` 페이지 자체를 삭제한 사건

**A.** 제가 링크를 삭제하라고 했는데 페이지를 지웠습니다.
지적은 이렇게 했습니다 — **"작업 틀렸어. `magazine.html` 링크를 삭제한 뜻이지 페이지 자체를 삭제하란 뜻이 아니었어"**
그리고 **복구 방향까지 함께 지시**했습니다. 원본 살리기 + 전체 버튼만 `magazine-all.html`로 분리.
**배운 것은 지시의 모호성입니다.** "삭제해줘"는 대상이 링크인지 페이지인지 문장만으로 확정되지 않습니다. 이후로는 대상을 명시적으로 지정했습니다.

### 110. AI 답변이 타당해서 원안을 유지한 사례

**A.** 98번의 `main` 태그 건입니다. → 98번 참조.

### 111. 검수를 자동화한다면

**A.** 검출 유형별로 도구가 다릅니다.

| 유형 | 도구 | 검출 대상 |
| --- | --- | --- |
| 표준 위반 | `html-validate`, W3C validator CLI | 마크업 오류 |
| CSS 품질 | `stylelint` | 하드코딩 색상, `!important`, 중복 선언 |
| JS | `eslint` | 미사용 변수, 위험 패턴 |
| 접근성 | `axe-core`, `pa11y` | 대비, ARIA, 랜드마크 |
| 성능 | Lighthouse CI | Core Web Vitals 회귀 |
| 시각 회귀 | Playwright 스크린샷 비교 | 리팩터링 회귀 |
| 링크 | `linkinator`, `lychee` | 404 |

**제가 수동으로 한 것 중 자동화 대상이 명확한 게 둘 있습니다.**
① 하드코딩 색상 — `stylelint`의 `declaration-property-value-allowed-list`로 커밋 전 차단 가능. **PRD 9.1에 "커밋 전에 검사하는 린트 규칙은 없어, 새 컴포넌트가 원시 토큰을 써도 검출되지 않는다"고 적어둔 게 정확히 이 지점입니다.**
② 404 링크 — 3,389건에서 0건까지 수동으로 줄였는데, 링크 체커를 CI에 붙이면 재유입 자체가 막힙니다.

### 112. CI를 붙인다면 무엇부터

**A.** **링크 체커와 stylelint부터입니다.**
근거는 README에 제가 적어둔 문장입니다 — *"접근성 지표는 157페이지까지 무너지지 않은 반면 하드코딩 색상만 밀렸다. 전자는 페이지 생성 템플릿에 박혀 있고 후자는 매번 새로 판단해야 하기 때문이다."*
품질 기준은 개인의 주의만으로 유지하기보다, 린트나 CI처럼 반복해서 확인할 수 있는 구조로 만들어야 한다고 느꼈습니다. 매번 판단해야 하는 축이 무너집니다.
따라서 CI 우선순위는 **"매번 판단해야 하는 항목"** 순입니다.
1. `stylelint` (하드코딩 색상 — 205→26까지 세 번 재유입한 축)
2. 링크 체커 (3,389→0까지 갔다가 재유입 가능)
3. `axe-core` (접근성 — 지금은 지켜지지만 사람이 바뀌면 무너짐)
4. Lighthouse CI (성능 93 유지)

## 5-3. 자기 집계 오류 §10.4 🔥

### 113. v2.1 수치가 틀렸다 🔥

> **질문을 받으면 숨기지 말고 측정 오류와 정정 과정을 차분하게 설명하는 편이 좋습니다.**

**A.** 네 건이 틀렸습니다. PRD §10.4에 별도 절로 남겼습니다.

| ID | 항목 | v2.1 공표 | 실측 | 원인 |
| --- | --- | --- | --- | --- |
| MEA-01 | 하드코딩 색상 | 180건 | **205건** | 두 축 기준을 정하고 한 축으로만 검산 |
| MEA-02 | 08-05 색상 해소 | 0건 | **82건** | 동상 |
| MEA-03 | 커밋 총계 | 196건 | 197건 | 작업 리뷰 기준 근사값 사용 |
| MEA-04 | PDP 인바운드 분해 | 랭킹 63건 | **10건** | 카드 외 링크를 랭킹 하나로 뭉뚱그림 |

**MEA-01·02가 핵심입니다.** 집계 기준을 16진수 + 브랜드색 rgb 두 축으로 정해놓고, 검산은 옛 기준인 한 축으로만 돌렸습니다. 그래서 08-05에 "0건 해소"라고 기록했는데, 그날 처리된 건 16진수 축뿐이고 **브랜드색 rgb 축은 73 → 78로 오히려 늘어 있었습니다.**

### 114. 왜 이걸 문서에 남겼나

**A.** **G-03이 요구한 것과 모순되기 때문입니다.**
이 프로젝트의 목표 G-03은 "AI 산출물 품질 관리"입니다. 남의 보고를 대조하는 일입니다. 그런데 **제 집계는 같은 절차를 거치지 않았습니다.**
남의 숫자는 세 번 확인하고 내 숫자는 안 세는 건, 검수를 주장하는 문서에서 가장 치명적인 결함입니다. 그래서 결함(DEF)도 계획 변경(CH)도 아닌 **별도 절(MEA)**로 분리했습니다. 성격이 다르기 때문입니다.
PRD에 쓴 문장 그대로 말씀드리면 — **"세 번을 세고서야 맞았습니다."**

### 115. 그럼 v2.3 수치는 믿을 수 있나 🔥

**A.** v2.1보다는 믿을 수 있고, 그 이유를 절차로 말씀드릴 수 있습니다.
| v2.1 | v2.3 |
| --- | --- |
| 시점별 수동 집계 | **커밋별로 동일 스크립트를 소급 적용** |
| 한 축으로 검산 | **세 가지 집계 방법으로 교차 검증** |
| 주석·선언줄 포함 여부 불명확 | 주석 제거 후 토큰 선언줄 제외, **실사용분만** |
| 로컬 도구 측정 | **배포본 기준** Lighthouse·axe 측정 |

**그리고 여전히 못 믿을 부분도 말씀드리겠습니다.** 흑백 알파값 121건을 집계 대상에서 제외했는데, 이건 판단이 들어간 결정입니다. 사용처별 투명도로 봤기 때문인데, 다른 기준을 잡으면 숫자가 달라집니다. **제외 사유를 문서에 적어두는 것까지가 제가 할 수 있는 일입니다.**

### 116. 집계 기준이 왜 두 축인가 / 121건 제외 근거 🔥

**A.** **두 축인 이유** — 이 프로젝트에서 하드코딩이 두 형태로 나타났기 때문입니다.
① 16진수 리터럴 (`#1667fa`)
② **브랜드색의 rgb() 표기** (`rgba(22, 103, 250, .1)`) — 알파값을 넣으려고 hex 대신 rgb로 풀어 쓴 것
②를 안 세면 "토큰화 완료"인데 실제로는 브랜드 색상이 코드에 박혀 있습니다. 브랜드 색이 바뀌면 못 따라옵니다. **그래서 축에 넣었습니다.**

**121건 제외 근거** — 흑백 알파값(`rgba(0,0,0,.1)` 계열)입니다. 이건 브랜드 색이 아니라 **그림자·오버레이의 투명도 표현**이고, 사용처마다 값이 달라야 정상입니다. 토큰으로 묶으면 오히려 표현력이 죽습니다.
**다만 이것도 판단이라 문서에 명시했습니다.** 08-03 기준으로 제외 결정을 고정하고 이후 재집계에서도 같은 기준을 유지했습니다.

▸ **남은 26건 해소 방향도 준비돼 있습니다.**
- 브랜드 알파 22건 → `--color-brand-base-rgb: 22 103 250` 신설 후 `rgb(var(--color-brand-base-rgb) / α)`
- 16진수 4건 → 마스크(`#000` 3) · 비디오 배경(`#0b0e14` 1). **용도가 특정돼 있어 토큰 승격보다 예외 명시가 타당**하다고 판단

### 117. 실무에서 이런 일이 발생하면

**A.** 보고 숫자로 의사결정이 되는 자리에서는 사고입니다. 성능 개선률, 버그 감소율 같은 숫자가 틀리면 잘못된 판단이 이어집니다.
**방지책은 세 가지라고 봅니다.**
① **집계 스크립트를 코드로 고정** — 사람이 매번 세면 매번 기준이 흔들립니다
② **기준 정의자와 검산자를 분리** — 같은 사람이면 같은 사각지대를 반복합니다. 제가 겪은 게 정확히 이것입니다
③ **수치에 측정 시점·방법·제외 항목을 항상 병기** — PRD v2.3에 「MEASUREMENT BASIS」 콜아웃을 별도로 만든 이유입니다

## 5-4. ADR 방어

### 118. 로그인 상태별로 헤더 파일을 분리한 것이 상태 관리를 피한 것 아닌가요?

**A.** 실제 인증 서버가 없는 정적 사이트였기 때문에 로그인 상태를 JavaScript 변수 하나로 흉내 내는 것보다, 어떤 화면이 시뮬레이션인지 명확하게 보여주는 편이 낫다고 판단했습니다.

예를 들어 `isLoggedIn` 같은 값만 바꾸면 화면에서는 로그인 상태처럼 보이지만 실제 인증이나 세션 검증이 구현된 것은 아닙니다.

그래서 비로그인과 로그인 후 화면을 파일로 구분하고, 이 선택의 이유와 한계도 ADR과 README에 기록했습니다. 실서비스라면 당연히 인증 상태에 따라 하나의 공통 컴포넌트가 렌더링되는 구조가 맞습니다.

### 119. 그렇다면 상태 관리를 해본 경험은 없나요?

**A.** 서버 인증 상태를 구현하지 않은 것이지, 화면 안의 클라이언트 상태 자체를 전혀 다루지 않은 것은 아닙니다.

알림의 읽지 않은 개수와 배지 상태를 동기화하거나, 장바구니 전체 선택 개수를 갱신하거나, 탭과 아코디언의 열림 상태를 관리하는 기능은 JavaScript로 구현했습니다.

저는 서버에서 결정되는 상태와 브라우저 안에서만 관리되는 UI 상태를 구분해서 설명하는 것이 정확하다고 생각합니다.
### 120. PDP를 1종으로 수렴시킨 근거

**A.** **152개를 복제해도 새로 증명되는 기술이 없기 때문**입니다. PDP는 구조가 동일하고 콘텐츠만 다른 페이지입니다.
그리고 대가가 큽니다. 헤더가 219줄씩 붙으므로 152개면 **약 15만 줄이 늘어납니다.**
대신 한 종을 완성도 있게 만들고(1,351줄), **전 카드가 실제로 그곳에 도달하는지**를 검증했습니다.
| 항목 | 수치 |
| --- | --- |
| PDP 인바운드 링크 | **1,853건** |
| 연결된 페이지 | **83개** (강의 카드가 있는 전 페이지) |
| 미연결 69개 | 강의 카드 자체가 없는 페이지 |

카드 하나당 링크 2건입니다 — 썸네일과 제목이 각각 상세로 갑니다(`course-card__thumb-link`).
**포트폴리오에서 중요한 건 상세 페이지 개수가 아니라 카드 → 상세 동선이 사이트 전역에서 끊기지 않는가입니다.**

### 121. 카드 정보와 상세 불일치 — 실서비스면 치명적 아닌가 🔥

**A.** 치명적입니다. 그래서 ADR-03의 트레이드오프 칸에 **"강의별 상세 부재 — 카드 정보와 상세 불일치 감수"**라고 명시했습니다.
**감수한 것이지 몰랐던 것이 아닙니다.** 그리고 이건 포트폴리오라서 감수 가능한 것이지, 실서비스에서는 선택지가 아닙니다.

### 122. 실서비스라면 여기 무엇이 들어가나

**A.** 템플릿 엔진 또는 CMS입니다. **하나의 마크업 템플릿에 카탈로그 데이터가 주입되는 구조**가 됩니다.
```
courses.json (152종)  →  course-detail.njk  →  152개 라우트
```
그러면 세 가지가 동시에 해결됩니다.
① 카드-상세 데이터가 같은 소스에서 나오므로 불일치 자체가 불가능
② 헤더도 include라 219줄 복제가 사라짐 (ADR-06 부채도 해소)
③ 강의 추가가 JSON 한 줄

**정적 사이트에서는 그 지점이 구현 한계이고, 인지한 상태로 데모 1종으로 대체했습니다.** README에 그대로 적어뒀습니다.

### 123. 헤더 219줄 × 161파일 — GNB 하나 바꾸려면 🔥

**A.** 현재는 일괄 치환 스크립트입니다. 실제로 로고 `href`를 157개 파일에서 한 번에 바꿨습니다(DEF-01).
**이건 ADR-06에 "인지·감수"로 명시한 부채입니다.** 중앙값 219줄, 최소 206, 최대 228이고, 문서형 3종은 축약 헤더(4~5줄)라 제외했습니다. 수치까지 세어놨다는 게 인지의 근거입니다.
▸ **여기서 반드시 덧붙일 것**: "실무에서 이 구조를 제안하지는 않습니다. 포트폴리오에서 이 방식을 택한 이유는 **빌드 결과물이 아니라 제가 쓴 마크업이 그대로 보여야 검수 대상이 되기 때문**입니다."

### 124. 왜 SSI / SSG / Gulp include를 안 썼나 🔥

**A.** 두 가지 이유입니다.
① **GitHub Pages 제약** — SSI는 서버 설정이 필요한데 GitHub Pages에서 안 됩니다. (Jekyll은 되지만 그건 SSG입니다)
② **투명성 선택** — SSG를 쓰면 저장소에 템플릿이 들어가고 배포본은 빌드 산출물이 됩니다. 이 프로젝트의 관람 동선이 **"사이트 → README 검수 기록 → PRD"**인데, 검수 대상이 제 마크업이어야 했습니다.

**다만 이 논리의 약점도 말씀드리겠습니다.** ①은 11ty나 Astro를 쓰면 해결되고(GitHub Actions로 빌드), ②는 저장소에 템플릿과 빌드 결과를 둘 다 두면 해결됩니다.
**정확히 말하면 "학습 범위 안에서 선택한 것"이 맞습니다.** 빌드 파이프라인은 제가 검수할 수 있는 범위 밖이었고, README에 A안/B안을 나눈 기준이 정확히 그것이었습니다.
▸ **이 정직함이 오히려 점수가 됩니다.** 억지 논리로 방어하지 마세요.

### 125. Supabase 미도입 근거 (ADR-09)

**A.** 우선순위 판단이었습니다.
이 포트폴리오의 핵심 주장은 **AI 산출물 검수**이지 풀스택 구현이 아닙니다. 그리고 그 시점에 **모바일 반응형이 미완**이었습니다.
백엔드를 붙여서 "로그인이 진짜 됩니다"를 만드는 것과, 164페이지를 모바일에서 안 깨지게 만드는 것 중 **퍼블리셔 포트폴리오로서 후자가 우선**이라고 봤습니다. 실제로 08-13에 반응형을 끝냈습니다.
트레이드오프는 명시했습니다 — 실데이터 연동 미검증, 상태 관리 시연 한계.

### 126. `aria-disabled` vs `disabled`

| | `disabled` (HTML 속성) | `aria-disabled="true"` |
| --- | --- | --- |
| 포커스 | **불가** | 가능 |
| 클릭 이벤트 | 발생 안 함 | **발생함 — JS로 막아야 함** |
| 스크린리더 | "비활성" 안내 + **탭 순서에서 제외** | "비활성" 안내 + 탭으로 도달 가능 |
| 폼 전송 | 값이 전송 안 됨 | 전송됨 |
| 적용 대상 | `button`, `input`, `select` 등만 | **모든 요소** (`<a>` 포함) |

**`aria-disabled`를 택한 이유**는 페이지네이션 화살표가 `<a>`이기 때문입니다. `<a>`에는 `disabled` 속성이 없습니다.
그리고 **탭으로 도달 가능한 게 오히려 낫다**고 판단했습니다. 키보드 사용자가 "이전 페이지 버튼이 있는데 지금은 못 쓴다"를 인지할 수 있습니다. `disabled`로 탭에서 빼면 존재 자체를 모릅니다.
트레이드오프는 클릭 이벤트를 JS로 막아야 한다는 것입니다. 이건 ADR-07에 적어뒀습니다.

### 127. 문서 3종 유지 비용이 아깝지 않나

**A.** 비용이 듭니다. ADR-08 트레이드오프 칸에 그대로 적었습니다.
그럼에도 유지한 이유는 **계획 변경의 은폐를 막기 위해서**입니다. 사전 PRD v1.0을 수정 가능하게 두면, 계획이 바뀔 때마다 계획서를 고치게 됩니다. 그러면 **모든 결과가 계획대로 된 것처럼 보입니다.**
원본을 얼려두고 변경 16건을 `CH-` ID로 남기면, "4페이지 + 모달로 계획했다가 164페이지가 됐다"(CH-SCOPE-01, 04)는 사실이 그대로 남습니다.
ADR-08에 근거로 쓴 문구가 **"면접 방어 자료화"**입니다. 실제로 지금 이 대화가 그 문서 덕분에 가능합니다.

## 5-5. 접근성 · 성능

### 128. Lighthouse 측정 환경 🔥

**A.** **배포본(GitHub Pages) 모바일 기준**, 08-15 측정입니다. 로컬이 아닙니다.
| 지표 | 점수 |
| --- | --- |
| 성능 | 93 (TBT 0 · CLS 0 · TTI 96 · LCP 81 · FCP 54) |
| 접근성 | 100 |
| 권장사항 | 100 |
| SEO | 100 |

**로컬이 아니라 배포본으로 측정한 이유가 129번입니다.**

### 129. 로컬 axe가 대비 회귀를 통과로 판정한 이유

**A.** 로컬 검사 도구는 **실제 렌더된 색상이 아니라 CSS 선언을 기준으로 계산**하는 경우가 있습니다. 어두운 컨테이너 안에서 토큰이 재정의되면, 도구가 최종 계산색을 못 따라가서 상위 선언 기준으로 통과 판정을 냅니다.
배포본 Lighthouse는 실제 렌더 결과를 봅니다. 그래서 **로컬 통과 → 배포본에서 위반 45건**이 나왔습니다.
같은 성격의 문제를 PRD 제약사항에 두 건 묶어서 적어뒀습니다. 미리보기 창의 폭 오보고와 이 건입니다. **"로컬 도구를 최종 판정으로 쓰지 않는다"**가 결론입니다.

### 130. 명도 대비 위반 45건을 어떻게 해결했나요?

**A.** 처음에는 45개의 서로 다른 문제가 있는 것처럼 보였지만 실제 원인은 몇 가지 색상 토큰에 집중돼 있었습니다.

`text-muted` 색상을 더 진하게 조정하고, 어두운 컨테이너에서는 적절한 토큰을 다시 정의하고, danger 색상과 일부 라벨 색상도 대비 기준을 만족하도록 변경했습니다.

결과적으로 공통 토큰 몇 개를 수정하면서 여러 위치의 위반을 한 번에 해결할 수 있었습니다. 이 경험을 통해 디자인 토큰을 사용하는 실질적인 장점을 느꼈습니다.

### 131. 명도 대비율은 어떻게 계산하나요?

**A.** 기본적으로 두 색상의 상대 휘도를 구한 뒤, 밝은 쪽을 `L1`, 어두운 쪽을 `L2`라고 하면 `(L1 + 0.05) / (L2 + 0.05)` 방식으로 계산합니다.

상대 휘도는 sRGB 값을 선형화한 다음 RGB에 각각 다른 가중치를 적용해 구합니다.

실무에서는 직접 수식을 계산하기보다 Chrome DevTools, axe, Contrast Checker 같은 도구를 사용해서 확인하겠습니다. 다만 수치가 어떤 원리로 나오는지는 이해하고 있습니다.
### 132. `aria-live` polite vs assertive / 카운트다운 제외 이유

| | 동작 |
| --- | --- |
| `polite` | 사용자가 하던 낭독이 끝나면 안내. **대부분 이걸 씁니다** |
| `assertive` | 진행 중인 낭독을 **끊고** 즉시 안내. 긴급 오류·경고만 |
| `off` | 안내 안 함 |

`common.js`가 전역 `#a11y-live`(polite)와 `window.announce()`를 주입하고 **14개 지점에 배선**했습니다.
**카운트다운을 제외한 이유는 초 단위 갱신이기 때문입니다.** live 영역에 넣으면 스크린리더가 1초마다 낭독합니다. 사용자가 다른 걸 들을 수 없게 됩니다. **과다 낭독은 접근성 향상이 아니라 파괴입니다.**
▸ 대안: 카운트다운은 `aria-live="off"`로 두고, 대신 정적 텍스트로 "이벤트 종료 D-3"을 별도 제공.

### 133. `window.announce()` 호출 지점

**A.** 화면에 시각적 변화는 있는데 **포커스는 안 움직이는 상황**들입니다. 시각 사용자는 보이지만 스크린리더 사용자는 모르는 순간입니다.
- 장바구니 전체 선택 / 개별 삭제 후 개수 변경
- 탭 전환 후 목록 갱신 (내 클래스, 주문내역)
- 필터 칩 적용 후 결과 수 변경
- 알림 "모두 읽음" 후 배지 상태
- 아코디언 열림/닫힘
▸ **원칙**: DOM이 바뀌었는데 포커스가 그대로면 안내가 필요합니다.

### 134. 스킵 링크가 실제로 동작하려면

**A.** 세 가지가 다 있어야 합니다.
① **타깃이 존재할 것** — `href="#main"`이면 `id="main"`이 있어야 합니다
② **타깃이 포커스를 받을 수 있을 것** — `<main id="main" tabindex="-1">`. 이게 없으면 브라우저에 따라 스크롤만 이동하고 **포커스는 헤더에 남습니다.** 다음 탭이 다시 GNB로 갑니다
③ **포커스 시 보일 것** — 평소 화면 밖에 숨기되 `:focus`에 나타나야 합니다
```css
.skip-link { position: absolute; left: -9999px; }
.skip-link:focus { left: 0; top: 0; z-index: 9999; }
```
**`display: none`으로 숨기면 안 됩니다.** 포커스 자체를 못 받습니다.
164/164 전수 확인했다는 건 링크 존재 여부이고, ②까지 확인했는지는 재검증 대상입니다.

### 135. 모달 포커스 트랩·ESC가 "확인 필요"로 남았다 ⚠️🔥

**A.** M9 잔여 항목입니다. **구현은 돼 있으나 실측 검증을 못 했습니다.**
정확히 말씀드리면, 알림 드롭다운은 검증됐습니다 — Esc 닫기, 트리거로 포커스 복귀, 바깥 클릭 감지, 리스너 해제까지 README에 기록돼 있습니다.
**미검증은 로그인·회원가입 모달의 포커스 트랩입니다.** ARIA 속성(`aria-haspopup="dialog"`, `aria-controls`, `role="dialog"`)은 적용 확인했지만, 실제로 탭이 모달 밖으로 새는지는 측정하지 않았습니다.
**이건 미완성이 맞습니다.** 그리고 검증 방법도 정해져 있습니다 — Playwright로 탭 키를 반복 입력해서 `document.activeElement`가 모달 밖으로 나가는지 확인.

### 136. 포커스 트랩 직접 구현

**A.** 세 단계입니다.
```js
const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), ' +
                  'select, textarea, [tabindex]:not([tabindex="-1"])';

function trap(modal, trigger) {
  const nodes = modal.querySelectorAll(FOCUSABLE);
  const first = nodes[0], last = nodes[nodes.length - 1];
  first.focus();                                  // ① 진입 시 첫 요소로

  modal.addEventListener('keydown', e => {
    if (e.key === 'Escape') { close(); return; }
    if (e.key !== 'Tab') return;
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault(); last.focus();           // ② 순환
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault(); first.focus();
    }
  });

  function close() { modal.hidden = true; trigger.focus(); }  // ③ 트리거 복귀
}
```
**더 나은 방법 두 가지**
- **`inert` 속성** — 모달 외 영역에 `inert`를 걸면 포커스·클릭·스크린리더 접근이 전부 차단됩니다. 위 순환 로직이 불필요해집니다
- **`<dialog>` 엘리먼트** — `showModal()`을 쓰면 포커스 트랩, Esc 닫기, `::backdrop`, top layer가 **브라우저 기본 제공**입니다. 지금이라면 이쪽으로 갑니다

### 137. `prefers-reduced-motion`에서만 `!important`를 사용한 이유는 무엇인가요?

**A.** 기본적으로 `!important`는 사용하지 않으려고 했지만, 사용자가 운영체제에서 모션 감소를 선택한 경우에는 개별 컴포넌트의 애니메이션보다 사용자 설정이 우선되어야 한다고 판단했습니다.

전역 reduced-motion 규칙이 기존 `transition`이나 `animation` 선언을 확실하게 덮어야 하기 때문에 이 블록에 한해서만 예외적으로 사용했습니다.

프로젝트 전체에서 `!important`를 편의상 사용한 것이 아니라 접근성 요구를 보장하기 위한 제한적인 예외였습니다.

### 138. `!important`를 사용할 때 본인의 기준은 무엇인가요?

**A.** 기본 원칙은 사용하지 않는 것입니다. 먼저 선택자 구조와 cascade를 정리해서 해결하려고 합니다.

예외적으로 사용자의 접근성 설정처럼 반드시 저작자 스타일보다 우선해야 하는 경우에는 사용할 수 있다고 생각합니다.

그리고 예외를 사용했다면 왜 필요한지와 적용 범위를 문서에 남기겠습니다. 팀에 `@layer` 같은 더 나은 해결 방식이 있다면 그 방식을 우선하겠습니다.
### 139. 캡처 단계 리스너를 쓴 이유 🔥

**A.** 이벤트는 **캡처(위→아래) → 타깃 → 버블(아래→위)** 순으로 흐릅니다. `addEventListener(type, fn, true)` 또는 `{ capture: true }`로 캡처 단계에 붙입니다.
바깥 클릭 감지에 캡처를 쓴 이유는 **다른 요소가 `stopPropagation()`을 호출해도 감지가 되어야 하기 때문**입니다.
버블 단계에 `document` 리스너를 붙이면, 중간 요소가 전파를 막았을 때 이벤트가 `document`까지 못 옵니다. 그러면 **드롭다운이 안 닫힙니다.** 캡처는 타깃에 도달하기 전에 지나가므로 이 문제가 없습니다.

### 140. 리스너를 해제 안 하면

**A.** **메모리 누수와 중복 실행**입니다.
드롭다운을 열 때마다 `document`에 리스너를 붙이는데 닫을 때 안 떼면, 10번 열고 닫으면 리스너가 10개 쌓입니다. 클릭 한 번에 핸들러가 10번 돕니다. 그리고 핸들러가 클로저로 DOM 노드를 참조하고 있으면 그 노드가 GC되지 않습니다.
그래서 **닫을 때 `document` 리스너를 해제**했습니다.
▸ 모던한 방법: `AbortController`
```js
const ctrl = new AbortController();
document.addEventListener('click', fn, { signal: ctrl.signal, capture: true });
// 닫을 때
ctrl.abort();   // 여러 리스너를 한 번에 정리
```

### 141. XSS · `innerHTML` vs `textContent`

**A.** **XSS**는 공격자가 넣은 문자열이 코드로 실행되는 취약점입니다. `innerHTML`은 문자열을 HTML로 파싱하므로 `<img src=x onerror=alert(1)>` 같은 게 실행됩니다.
**`textContent`를 쓰면 됩니다.** 파싱하지 않고 텍스트로 넣으므로 애초에 실행 경로가 없습니다.
제가 알림에서 이스케이프를 한 이유는 **알림 항목이 텍스트만이 아니었기 때문**입니다. 강조 태그나 링크가 섞인 마크업을 조립해야 해서 `innerHTML`을 쓸 수밖에 없었고, 대신 **사용자 데이터 부분만 이스케이프**했습니다.
```js
const esc = s => s.replace(/[&<>"']/g, c =>
  ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
el.innerHTML = `<strong>${esc(user)}</strong>님이 댓글을 남겼습니다`;
```
▸ **더 나은 방법**: 구조는 템플릿으로 만들고 값만 `textContent`로 주입. 또는 `DOMPurify` 같은 검증된 라이브러리.
▸ 정적 사이트라 실제 공격 표면은 없지만, **습관을 코드에 남겨두는 게 목적**이었습니다.

### 142. 안 읽음 0이면 배지를 `hidden` 처리한 근거

**A.** 두 가지입니다.
① **스크린리더** — 배지에 숨김 텍스트로 "2건 읽지 않음"을 넣어뒀습니다. 0이 남아 있으면 **"0건 읽지 않음"을 계속 낭독**합니다. 정보가 아니라 소음입니다.
② **시각** — 배지 자체가 "알림이 있다"는 신호입니다. `0`이 적힌 배지는 신호가 있는 것처럼 보입니다.
**"값을 0으로 바꾼다"가 아니라 "요소를 없앤다"가 맞는 경우**입니다. 상태 표시 컴포넌트에서 자주 나오는 판단입니다.

### 143. 메가 메뉴를 숨길 때 `visibility: hidden`을 사용한 이유는 무엇인가요?

**A.** `opacity: 0`만 사용하면 화면에서는 보이지 않지만 내부 링크가 키보드 포커스를 받을 수 있고 스크린리더에서도 접근될 수 있습니다.

메가 메뉴가 닫혀 있을 때는 사용자가 보이지 않는 메뉴 안을 탭으로 이동하지 않아야 하기 때문에 포커스 순서에서도 제외되는 `visibility: hidden`을 함께 사용했습니다.

또 `display: none`과 달리 visibility와 opacity를 조합하면 전환 효과를 적용하기도 비교적 편합니다.

### 144. `display:none`, `visibility:hidden`, `opacity:0`, `aria-hidden`, `.sr-only`는 어떻게 다르나요?

**A.** `display:none`과 `hidden`은 화면 배치와 접근성 트리에서 모두 제외됩니다.

`visibility:hidden`은 공간은 남지만 보이지 않고 포커스도 받지 않습니다. `opacity:0`은 시각적으로만 투명해질 뿐 포커스나 접근성 트리에는 남을 수 있어서 단독 사용에 주의해야 합니다.

`aria-hidden="true"`는 스크린리더에서 숨기는 속성이기 때문에 포커스 가능한 요소에 이것만 적용하면 키보드 포커스와 접근성 상태가 어긋날 수 있습니다.

`.sr-only`는 반대로 화면에서는 숨기고 스크린리더에는 정보를 제공하고 싶을 때 사용합니다.

▸ **`display: none`의 함정을 실제로 겪었습니다.** JWA에서 햄버거 메뉴와 사이드 메뉴를 체크박스로 만들면서 체크박스 자체를 `display: none`으로 숨겼는데, **그러면 키보드로 도달할 수 없습니다.** 마우스로는 `<label>`을 눌러 동작하니 문제가 드러나지 않다가, `:focus-visible`을 넣고 탭으로 훑어보니 나왔습니다.
숨기는 목적이 "화면에서만 안 보이게"라면 `display: none`이 아니라 `.sr-only`(clip-path 방식)를 써야 합니다. 현재 남은 개선 과제로 공개해뒀습니다 → 77번.
### 145. `tablist`/`tab`/`tabpanel` · 로빙 tabindex

**A.** **로빙 tabindex**는 그룹 안에서 **탭 키로 진입 가능한 요소를 하나만 두는** 패턴입니다.
```html
<div role="tablist">
  <button role="tab" aria-selected="true"  tabindex="0">개발&IT</button>
  <button role="tab" aria-selected="false" tabindex="-1">디자인</button>
  <button role="tab" aria-selected="false" tabindex="-1">사진&영상</button>
</div>
```
탭 키로는 그룹에 **한 번만** 들어가고, 그룹 내부 이동은 **화살표 키**로 합니다. 이동할 때 `tabindex`를 0과 -1로 바꿔가며 옮깁니다.
**이유는 탭 횟수입니다.** 대분류 8개에 전부 `tabindex="0"`을 주면 다음 영역으로 가는 데 탭을 8번 눌러야 합니다. 메뉴가 커질수록 키보드 사용자만 손해를 봅니다.
구현한 키 조작: ↑↓ 이동, Home/End 처음·끝, Esc 닫고 트리거로 복귀.

### 146. 점진적 향상

**A.** **JS가 죽어도 핵심 기능이 동작해야 한다**는 원칙입니다.
메가 메뉴에 적용한 방식은 이렇습니다 — JS가 비활성이어도 **링크는 그대로 동작**합니다. 메뉴 확장이 안 될 뿐, 각 카테고리 링크는 `<a href>`라서 클릭하면 이동합니다.
반대로 하면 안 되는 게 있습니다. `<div onclick>`으로 내비게이션을 만들면 JS가 죽는 순간 사이트 전체가 멈추고, 우클릭 새 탭도 안 되고, 스크린리더가 링크로 인식하지 못합니다.
▸ **이게 08-03의 "이동이 아니면 링크로 쓰지 않는다" 원칙과 짝입니다.** 이동이면 `<a>`, 동작이면 `<button>`. 태그를 의미대로 쓰면 점진적 향상이 대부분 자동으로 따라옵니다.

## 5-6. 08-15 품질 정리 회차

### 147. CSS 중복 272그룹 통합 · 렌더 결과 검증

**A.** 선언 블록이 같고 셀렉터만 다른 규칙을 콤마 셀렉터로 합쳤습니다.
| 지표 | 전 | 후 |
| --- | --- | --- |
| `style.css` | 10,174줄 | **9,883줄** |
| 선언 블록 | 3,930 | **3,254** |
| 제거된 중복 | — | 654개 (272그룹) |

**검증 방법이 핵심입니다.** 3개 페이지 × 3구간에서 **계산된 스타일 해시를 통합 전후로 비교**했습니다. `getComputedStyle` 결과를 요소별로 직렬화해 해시를 뜨고 대조하는 방식입니다. 육안 확인이 아닙니다.
**이건 JWA에서 스타일 유실 3건을 겪고 만든 절차입니다.**

### 148. 시맨틱 토큰 별칭 957곳을 어떻게 한 번에 전환했나요?

**A.** 957곳을 하나씩 직접 바꾼 것은 아니고, CSS 속성과 기존 원시 토큰의 조합을 기준으로 매핑 규칙을 만든 뒤 스크립트로 일괄 변환했습니다.

예를 들어 같은 회색 토큰이라도 `color` 속성에서 사용되면 text 계열 별칭으로, `border-color`에서는 border 계열 별칭으로, 배경에서는 surface 계열 별칭으로 바꾸는 방식입니다.

규칙 자체는 제가 정의했지만 모든 변경을 개별적으로 검토한 것은 아니고 표본 검수를 진행했습니다. 그래서 숫자보다 매핑 규칙이 적절했는지를 보는 것이 더 중요하다고 생각합니다.

### 149. 시맨틱 토큰 비율이 오랫동안 7%였다가 하루에 57.5%가 된 이유는 무엇인가요?

**A.** 페이지를 계속 생성하던 기간에는 기존 토큰을 바꾸는 속도보다 새 컴포넌트에서 원시 토큰이 추가되는 속도가 빨랐습니다.

그래서 시맨틱 별칭의 절대 개수는 늘어도 전체 참조 수도 함께 증가해서 비율은 거의 움직이지 않았습니다.

페이지 생성이 끝난 뒤 신규 유입이 멈춘 시점에 매핑 규칙을 이용해 일괄 전환하면서 비율이 크게 올라갔습니다.

다만 하루 만에 957곳을 사람이 모두 판단했다는 의미는 아니기 때문에, 면접에서는 자동 매핑을 사용했고 표본 검수를 했다는 점까지 함께 설명하겠습니다.
### 150. 린트 규칙 부재 ⚠️

**A.** PRD 9.1에 그대로 적어둔 한계입니다 — **"커밋 전에 검사하는 린트 규칙은 없어, 새 컴포넌트가 원시 토큰을 써도 검출되지 않는다."**
막는 방법은 `stylelint` + `lint-staged` + `husky` 조합입니다.
```json
// .stylelintrc — 색상 리터럴 자체를 차단
"declaration-property-value-disallowed-list": {
  "/color/": ["/^#/", "/^rgb/"]
}
```
그리고 별칭 강제는 커스텀 규칙이 필요합니다 — `color` 속성에 `--color-neutral-*` 원시 토큰이 오면 경고.
**112번에서 말씀드린 우선순위와 같습니다.** 205 → 82 → 61 → 26으로 세 번 정리하고 두 번 재유입한 축이라, 사람의 규율로는 유지가 안 됩니다.

### 151. `?v=` 형태의 캐시버스터 370건을 왜 제거했나요?

**A.** 370곳의 버전 값을 사람이 직접 올리는 방식은 일부 파일이 빠질 가능성이 높다고 판단했습니다.

한두 파일만 이전 캐시가 남으면 화면이 부분적으로 깨질 수 있고, 오히려 전체가 실패하는 경우보다 원인을 찾기 어려울 수 있습니다.

HOOT UP은 빌드 도구가 없는 정적 프로젝트였기 때문에 수동 버전 값을 계속 관리하기보다 제거하는 쪽을 선택했습니다. 다만 이것이 가장 좋은 캐시 전략이라는 의미는 아닙니다.

### 152. 실서비스에서는 캐시 무효화를 어떻게 처리하는 것이 좋나요?

**A.** 빌드 단계에서 파일 내용에 따라 해시가 포함된 파일명을 자동으로 만드는 방식이 가장 안정적이라고 생각합니다.

예를 들어 `style.css`가 `style.a3f9c2.css`처럼 바뀌면 내용이 변경될 때 파일명 자체가 달라져 새로운 파일로 요청됩니다.

변경되지 않은 정적 자산에는 긴 `max-age`와 `immutable`을 적용할 수 있기 때문에 캐시 효율도 높일 수 있습니다.

즉 사람이 버전 문자열을 관리하기보다 빌드 시스템이 자동으로 처리하도록 만드는 방향이 좋다고 생각합니다.
### 153. WebP 전환 · AVIF 검토 · 폴백

**A.** 328개를 품질 82로 WebP 전환, assets 12MB → 5.3MB입니다. SVG 46개는 벡터라 유지했습니다.
**AVIF는 검토했으나 도입하지 않았습니다.** 이유는 두 가지입니다.
① 같은 화질에서 WebP보다 20~30% 더 작지만, **인코딩 시간이 크게 깁니다.** 328개 배치 처리 비용이 컸습니다
② 폴백 체인이 늘어납니다 — `<picture>`로 AVIF → WebP → JPG 3단을 깔아야 하는데, HTML 참조가 2,396곳이라 마크업 변경 비용이 절감분보다 컸습니다

**폴백은 현재 없습니다.** WebP로 전환하면서 원본을 삭제했습니다(깨진 참조 0 확인 후). WebP는 현재 모든 주요 브라우저에서 지원되므로 실용적 판단이었지만, **엄밀하게는 `<picture>` 폴백을 두는 게 맞습니다.**

### 154. `.jsbeautifyrc` — Prettier를 안 쓴 이유

**A.** DEF-10이 문제였습니다. HTML 포맷이 편집기별로 달라서 **미커밋 84개**가 발생했습니다. 내용은 안 바꿨는데 포맷만 달라져서 diff가 뜬 것입니다.
`.jsbeautifyrc`로 고정했습니다 — 2-space, `indent_inner_html: false`, `end_with_newline: false`.
**Prettier를 안 쓴 이유는 기존 164개 파일과 충돌했기 때문입니다.**
- doctype 대소문자 (`<!DOCTYPE>` ↔ `<!doctype>`)
- void 태그 표기 (`<br>` ↔ `<br />`)
- `<head>` 들여쓰기 규칙

Prettier를 도입하면 164개 파일 전체가 한 번에 재포맷되고, **그 커밋 하나가 diff 수만 줄이 됩니다.** 그 시점에 다른 작업과 겹치면 변경 이력을 추적할 수 없게 됩니다.
▸ **정직한 보완**: "신규 프로젝트라면 Prettier가 표준입니다. 기존 코드베이스에 뒤늦게 넣는 비용 때문에 택하지 않은 것이고, 팀에 Prettier 설정이 있으면 따르겠습니다."

### 155. M11 폰트 렌더 차단 1,590ms · `font-display` vs `preload`

**A.** 원인은 웹폰트가 **렌더 블로킹 리소스**이기 때문입니다. 브라우저가 폰트를 받을 때까지 텍스트를 안 그리는 구간(FOIT)이 생깁니다.

| | 역할 |
| --- | --- |
| `font-display: swap` | **언제 그릴지** — 폰트 도착 전에 대체 폰트로 먼저 그림. 블로킹 제거 |
| `<link rel="preload">` | **언제 받을지** — 폰트 요청을 앞으로 당김. 도착을 빠르게 |

**둘은 대체재가 아니라 조합입니다.**
```html
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preload" href="/fonts/body.woff2" as="font"
      type="font/woff2" crossorigin>
```
```css
@font-face { font-family: BodyFont; src: url(...) format('woff2');
             font-display: swap; }
```
`swap`의 대가는 **FOUT** — 폰트가 바뀌는 순간 글자 폭이 변해 레이아웃이 흔들립니다. CLS에 영향이 갑니다. `size-adjust`, `ascent-override`로 대체 폰트의 메트릭을 맞추면 완화됩니다.
**한글 폰트는 여기에 서브셋이 추가로 필요합니다** → 222번 참조.
▸ **주의**: PRD에 적어둔 대로 Lighthouse 리포트상 절감 시간이 0으로 나와서, **적용 후 재측정이 필요합니다.** 예상 개선폭을 단정하지 마세요.

### 156. LCP · CLS · TBT · FCP · TTI 🔥

| 지표 | 뜻 | 기준(양호) |
| --- | --- | --- |
| **FCP** First Contentful Paint | 첫 콘텐츠(텍스트·이미지)가 그려진 시점 | 1.8s 이하 |
| **LCP** Largest Contentful Paint | **가장 큰 요소**가 그려진 시점. 체감 로딩 속도 | **2.5s 이하** |
| **CLS** Cumulative Layout Shift | 예기치 않은 레이아웃 이동의 누적. 단위 없는 점수 | **0.1 이하** |
| **TBT** Total Blocking Time | 메인 스레드가 50ms 이상 막힌 시간의 합 | 200ms 이하 |
| **TTI** Time to Interactive | 안정적으로 입력에 반응 가능한 시점 | 3.8s 이하 |
| **INP** Interaction to Next Paint | 실제 상호작용 응답 지연. **FID를 대체한 Core Web Vitals** | 200ms 이하 |

**Core Web Vitals 3종은 LCP · CLS · INP**입니다. TBT는 INP의 실험실 대용 지표입니다.
HOOT UP 실측: TBT **0** · CLS **0** · TTI 96 · LCP 81 · FCP 54 (뒤 넷은 Lighthouse 점수).

### 157. CLS 0 달성 방법 / `width`·`height` 4건이 남았다는 뜻

**A.** CLS는 **이미 그려진 요소가 예상 밖으로 밀릴 때** 발생합니다. 주 원인 네 가지입니다.
① 이미지에 크기 미지정 → 로드되는 순간 아래가 밀림
② 웹폰트 교체 시 글자 폭 변화
③ 광고·임베드 등 동적 삽입 콘텐츠
④ 애니메이션에 `top`/`height` 사용 (`transform`을 써야 함)

**대응**
```html
<img src="a.webp" width="800" height="600" alt="">   <!-- 종횡비 예약 -->
```
`width`/`height` 속성이 있으면 브라우저가 CSS `aspect-ratio`를 자동 계산해서 **로드 전에 공간을 잡아둡니다.** CSS로 `width: 100%`를 줘도 속성값의 비율이 유지됩니다.
**"`img` width/height 4건"은 이 속성이 빠진 이미지가 4개 남았다는 뜻입니다.** 현재 CLS 0인 건 그 4개가 뷰포트 밖이거나 크기가 작아서 임계값에 안 걸린 것이고, **잠재 위험이 남은 상태**입니다.

---

# 06. HTML · CSS 기초 CS

### 158. 시맨틱 태그를 쓰는 이유 3가지
**A.** ① **접근성** — 스크린리더가 랜드마크로 인식해 영역 간 건너뛰기가 가능 ② **SEO** — 크롤러가 문서 구조와 콘텐츠 중요도를 파악 ③ **유지보수** — `div` 더미보다 코드만 읽어도 구조가 보임.
▸ **JWA에서 실제로 고른 태그들입니다.** 시맨틱을 '아는가'보다 '고르는가'를 보여주는 쪽이 낫습니다.

| 대상 | 태그 | 고른 이유 |
| --- | --- | --- |
| 장바구니 담긴 상품 | `<ol>` | **담은 순서가 의미 있는** 목록 |
| 금액 내역 (Subtotal/Shipping/Tax) | `<dl><dt><dd>` | 항목과 값이 짝을 이루는 구조 |
| 주문 요약 | `<aside>` | 본문을 보조하는 영역 |
| CS·정책 본문 | `<article>` | 그 자체로 완결되는 콘텐츠 |
| 저작권 표기 | `<cite>` | 출처임을 의미로 표시 |
| 에디토리얼 이미지+글 | `<figure><figcaption>` | 이미지와 설명을 한 덩어리로 |
| 주문 내역 | `<table>` 6열 | 스크린리더가 열 제목과 값을 연결해 읽음 |

▸ 랜드마크 이야기로 이어지면 81번으로 연결하세요 — 태그 선택은 처음부터 했지만 `<main>` 커버리지는 2/392였고, 세어본 뒤에 392/392로 채웠습니다.

### 159. `<section>` vs `<div>` / `<article>`
**A.** `<section>`은 **제목을 가질 수 있는 주제 단위**입니다. 원칙적으로 `<h*>`를 동반해야 합니다. `<div>`는 의미 없는 순수 그룹핑 — 스타일이나 스크립트 목적일 때만.
`<article>`은 **그 자체로 독립 배포 가능한 콘텐츠** — 뉴스 기사, 블로그 글, 상품 카드, 댓글. HOOT UP의 데스크 기사 173건이 `<article>` 대상입니다.
▸ 판단법: "이 덩어리를 잘라서 RSS에 넣어도 말이 되나" → `<article>`.

### 160. `<b>`/`<strong>`, `<i>`/`<em>`
**A.** `<b>`·`<i>`는 **시각적 표현만**, `<strong>`·`<em>`은 **의미**입니다. `<strong>`은 중요도, `<em>`은 강세(문장 뜻이 바뀌는 강조). 스크린리더가 `<strong>`·`<em>`에 음성 변화를 줍니다.
▸ `<i>`의 정당한 용도: 학명, 외래어, 아이콘 폰트 관용 사용.

### 161. 웹 표준 · 접근성 · 크로스브라우징
**A.** **웹 표준** = W3C/WHATWG 명세를 따르는 것. **접근성** = 장애 여부·환경과 무관하게 동등하게 이용 가능하게 만드는 것. **크로스브라우징** = 브라우저·기기별로 동등한 경험을 보장하는 것.
셋은 순서 관계입니다. 표준을 지키면 접근성과 크로스브라우징이 상당 부분 자동으로 따라옵니다.

### 162. DOCTYPE
**A.** 브라우저를 **표준 모드**로 동작시키는 선언입니다. 없으면 쿼크 모드로 들어가서 옛 IE 호환 동작을 합니다. 대표적으로 **`box-sizing`이 `border-box`처럼 계산**되어 레이아웃이 전부 어긋납니다.
▸ HOOT UP DEF-10에서 `<!DOCTYPE>` 대소문자 표기 차이 때문에 Prettier를 못 쓴 이력이 있습니다.

### 163. 블록 · 인라인 · 인라인블록
| | 줄바꿈 | width/height | 상하 margin |
| --- | --- | --- | --- |
| block | 있음 | 적용 | 적용 |
| inline | 없음 | **미적용** | **미적용** (좌우만) |
| inline-block | 없음 | 적용 | 적용 |
▸ inline 요소 사이 공백 문제(HTML 개행이 4px 공백이 됨)는 flex/grid로 해결.

### 164. `box-sizing: border-box`
**A.** `width`에 padding과 border를 **포함**시키는 설정입니다. 기본값 `content-box`는 `width: 200px` + `padding: 20px`이면 실제 240px이 되어 계산이 어긋납니다.
`border-box`면 200px 안에서 padding이 안쪽으로 먹습니다. 그래서 reset에 전역 선언이 관행입니다.
```css
*, *::before, *::after { box-sizing: border-box; }
```
▸ JWA `calc(50% - 18px)` 계산이 성립하는 것도 이 설정 전제입니다.

### 165. 마진 병합
**A.** **인접 형제**의 상하 마진, **부모-첫/막내 자식** 사이 마진, **빈 요소**의 상하 마진이 서로 겹쳐 큰 값 하나가 됩니다. 좌우는 병합되지 않습니다.
**해결**: 부모에 `padding`/`border` 부여, `overflow: hidden`(BFC 생성), `display: flex`/`grid`(병합 자체가 없음).
▸ 가장 깔끔한 건 `flex`/`grid` + `gap`으로 가는 것입니다.

### 166. BFC
**A.** **Block Formatting Context** — 내부 블록 레이아웃이 외부와 독립적으로 계산되는 영역입니다.
생성 조건: `float`(none 아님), `position: absolute/fixed`, `display: flow-root / inline-block / flex / grid / table-cell`, `overflow`(visible 아님), `contain: layout`.
효과: ① 마진 병합 차단 ② float 자식 높이 포함(clearfix) ③ float 요소와 겹치지 않음.
▸ 의도가 명확한 건 **`display: flow-root`** — 부작용 없이 BFC만 만듭니다.

### 167. `position` 5종 / `sticky`가 안 되는 경우
| 값 | 기준 | 문서 흐름 |
| --- | --- | --- |
| `static` | — | 유지 |
| `relative` | 자기 원래 위치 | **유지** (자리 남음) |
| `absolute` | 가장 가까운 `position ≠ static` 조상 | 빠짐 |
| `fixed` | 뷰포트 | 빠짐 |
| `sticky` | 스크롤 컨테이너 | 유지 |

**`sticky`가 안 동작하는 경우**
① `top`/`bottom` 등 **오프셋 미지정**
② 부모에 `overflow: hidden`/`auto`/`scroll` — 가장 흔한 원인
③ 부모의 높이가 sticky 요소와 같아서 이동할 공간이 없음
④ 부모가 `display: flex`이고 `align-items: stretch` 기본값이 아닌 경우
▸ HOOT UP PDP 스티키 사이드바에서 실제로 겪을 수 있는 항목입니다.

### 168. `z-index`가 안 먹는 경우 / 쌓임 맥락
**A.** `z-index`는 `position: static`에서 무시됩니다(flex/grid 자식은 예외). 그리고 **쌓임 맥락(stacking context)이 다르면 숫자 비교 자체가 무의미**합니다.
쌓임 맥락 생성: `position` + `z-index`(auto 아님), `opacity < 1`, `transform`, `filter`, `will-change`, `isolation: isolate`, `mix-blend-mode`.
**전형적 사고**: 부모에 `opacity: .99`나 `transform`이 걸려 있으면 그 안의 `z-index: 9999`가 부모 밖 `z-index: 1`을 못 이깁니다.
▸ **JWA에서는 5단계 고정 스케일로 관리했습니다** — `0 · 1 · 10 · 50 · 100`. 값을 다섯 개로 묶어두면 새 레이어를 넣을 때 "몇을 줄까"가 아니라 "어느 층인가"만 정하면 됩니다. **9999 같은 값이 등장하는 순간이 스케일이 없다는 신호입니다.**

▸ **여기서 눈으로는 못 찾는 결함이 하나 나왔습니다.** `z-index`에 **px 단위를 붙인 값이 2건** 있었습니다. `z-index`는 단위를 쓸 수 없는 속성이라 그 선언은 통째로 무시됩니다. 그런데 **부모 요소의 쌓임 맥락 덕에 화면은 정상으로 보였습니다.** 브라우저로는 절대 발견할 수 없고, 정해둔 규칙표(0·1·10·50·100)와 실제 값을 대조해서 찾았습니다. 수정 후 이 항목을 점검 목록에 넣었습니다.
**"화면이 정상이면 코드도 정상"이 아니라는 걸 확인한 사례**라 면접에서 꺼낼 만한 이야기입니다.

▸ **HOOT UP은 아직 이 스케일이 없습니다** — M9 잔여 과제로 남아 있고, `--z-dropdown: 100 / --z-sticky: 200 / --z-modal: 300` 같은 토큰으로 고정하는 게 목표입니다. JWA에서 한 걸 다음 프로젝트에서 놓친 케이스입니다.

### 169. Flexbox vs Grid
**A.** **Flex는 1차원**(한 축), **Grid는 2차원**(행·열 동시)입니다.
- Flex: 헤더 유틸 영역, 버튼 그룹, 카드 내부 세로 배치 — 콘텐츠 크기에 맞춰 흐르게 할 때
- Grid: 카드 그리드, 페이지 레이아웃 — 행·열 구조가 먼저 정해질 때
▸ **58번과 연결**: "JWA 카드 그리드는 Flex + `calc()`로 짰는데, Grid가 맞았습니다. `repeat(auto-fill, minmax(265px, 1fr))` 한 줄로 5구간 계산식이 대체됩니다."

### 170. `flex: 1`
**A.** `flex-grow: 1 / flex-shrink: 1 / flex-basis: 0%`의 축약입니다.
`flex: auto`(=`1 1 auto`)와 다릅니다. `basis: 0`이면 **콘텐츠 크기를 무시하고 남은 공간을 균등 분배**하고, `auto`면 콘텐츠 크기를 기준으로 남은 공간만 나눕니다. 균등 폭 카드를 원하면 `flex: 1`입니다.

### 171. `fr` · `minmax()` · `auto-fit` vs `auto-fill`
**A.** `fr`은 **남은 공간의 비율** 단위입니다. `minmax(265px, 1fr)`은 최소 265px 보장, 여유가 있으면 늘어남.
**`auto-fill` vs `auto-fit`** — 아이템이 트랙 수보다 적을 때 갈립니다.
- `auto-fill`: 빈 트랙을 **유지** → 아이템이 왼쪽에 몰림
- `auto-fit`: 빈 트랙을 **접음** → 아이템이 늘어나 전체를 채움
카드가 3개뿐인데 5칸 폭이면, `auto-fill`은 3개가 원래 크기로 왼쪽에, `auto-fit`은 3개가 넓게 퍼집니다. **카드 크기를 유지하고 싶으면 `auto-fill`입니다.**

### 172. 미디어쿼리 없는 반응형
**A.** 세 가지입니다.
① **`clamp(min, 이상값, max)`** — 폰트·여백을 뷰포트 비례로. `font-size: clamp(16px, 2.5vw, 24px)`
② **Grid `auto-fit` + `minmax()`** — 열 수가 자동 조정. 171번
③ **`min()`/`max()`** — `width: min(1160px, 100% - 40px)`. HOOT UP에서 실제로 이 방식으로 콘텐츠 폭을 통일했습니다
▸ **69번과 연결**: `--gutter-header: 2.9%`를 `clamp()`로 바꾸겠다고 한 것이 이 맥락입니다.

### 173. 컨테이너 쿼리
**A.** 미디어쿼리가 **뷰포트**를 보는 반면, 컨테이너 쿼리는 **부모 요소의 크기**를 봅니다.
```css
.card-wrap { container-type: inline-size; }
@container (min-width: 400px) { .card { flex-direction: row; } }
```
**컴포넌트 재사용에서 결정적입니다.** 같은 카드가 사이드바에 들어가면 세로, 메인에 들어가면 가로로 — 뷰포트와 무관하게 자기 자리 크기에 맞춰 바뀝니다.
▸ **JWA에 적용하면**: 리스트와 위시리스트가 같은 카드 컴포넌트를 공유하는데(FR-LIST-04), 두 곳의 컨테이너 폭이 다릅니다. 지금은 미디어쿼리로 맞추는데 컨테이너 쿼리면 카드가 알아서 대응합니다.

### 174. `em`/`rem`/`px`/`vw`/`%`
| 단위 | 기준 | 용도 |
| --- | --- | --- |
| `px` | 절대 | 테두리, 그림자 등 확대 불필요한 값 |
| `rem` | **루트 폰트 크기** | 폰트, 여백 — 사용자 브라우저 설정을 따름 |
| `em` | **자기 요소 폰트 크기** | 컴포넌트 내부 비례 (버튼 padding 등) |
| `%` | 부모 크기 | 레이아웃 폭 |
| `vw`/`vh` | 뷰포트 | 히어로 높이, 유동 타이포 |
▸ **접근성상 폰트에는 `rem`이 원칙입니다.** `px`로 고정하면 사용자가 브라우저 기본 폰트를 키워도 반영되지 않습니다.
▸ `vh` 주의 → 245번(iOS `100vh`).

### 175. 리플로우 vs 리페인트
**A.** **리플로우(레이아웃)** = 요소의 위치·크기를 다시 계산. **리페인트** = 픽셀만 다시 칠함.
리플로우가 훨씬 비쌉니다. 하나가 바뀌면 주변·자식이 연쇄 재계산됩니다.
- 리플로우 유발: `width`, `height`, `margin`, `padding`, `top`, `left`, `font-size`, `display`, DOM 추가·삭제, `offsetHeight` 같은 값 **읽기**
- 리페인트만: `color`, `background-color`, `visibility`, `box-shadow`
- 둘 다 건너뜀: **`transform`, `opacity`** — 컴포지팅 단계에서만 처리
▸ **강제 동기 레이아웃**: 쓰기 → 읽기를 반복하면 매번 리플로우가 강제됩니다. 읽기를 먼저 몰아서 하고 쓰기를 나중에 하는 게 원칙입니다.

### 176. `transform` vs `left/top` / GPU 가속
**A.** `left`/`top` 애니메이션은 **매 프레임 리플로우**를 유발합니다. `transform`은 레이아웃과 페인트를 건너뛰고 **컴포지팅 단계에서만** 처리되므로 60fps 유지가 쉽습니다.
GPU 가속은 요소를 별도 레이어로 승격해 GPU가 합성하는 것입니다. `transform`, `opacity`, `will-change`, `filter`가 트리거입니다.
**남용하면 안 됩니다** — 레이어마다 메모리를 쓰고, 과도하면 오히려 느려집니다. `will-change`는 애니메이션 직전에 켜고 끝나면 제거하는 게 원칙입니다.
▸ **HOOT UP 실제 적용**: 썸네일 호버 1.3배 확대를 `overflow: hidden` + `transform: scale()`로 구현했고, `prefers-reduced-motion` 대응까지 넣었습니다.

### 177. 명시도 계산 / 인라인 · `!important`
**A.** `(ID, 클래스·속성·의사클래스, 요소·의사요소)` 3자리로 셉니다. 자릿수 간 넘침이 없습니다 — 클래스 11개도 ID 1개를 못 이깁니다.
```
#nav .item a:hover  →  (1, 2, 1)
.a.b.c              →  (0, 3, 0)
a                   →  (0, 0, 1)
```
**우선순위 전체**
```
!important (저작자) > 인라인 style > 명시도 > 선언 순서(뒤가 이김)
```
단, **사용자 `!important`가 저작자 `!important`보다 우선**합니다. 접근성 보장을 위한 설계입니다.
`@layer`가 있으면 **레이어 순서가 명시도보다 먼저** 적용됩니다.
▸ 47~52번과 세트로 준비.

### 178. BEM
**A.** `Block__Element--Modifier`. `.card__title--active` 형태입니다.
장점: 명시도가 (0,1,0)으로 평탄해지고, 클래스명만 봐도 소속이 보이고, 중첩 셀렉터가 사라집니다.
**이 프로젝트에서 지켰나** — HOOT UP은 상당 부분 지켰습니다. `course-card__flags`, `desk-period__chip--active`, `rank-card__thumb` 등. **JWA는 엄격하게 지키지 않았습니다** — 역할 기반 명명 원칙(NFR-CODE-02)은 지켰지만 BEM 형식은 아닙니다.
▸ 실제 사고 사례: 136페이지 시점에 신규 `faq__` 클래스가 기존 강의상세 FAQ 클래스와 충돌해서 **`hfaq__`로 274회 리네이밍**했습니다. **BEM만으로는 전역 충돌을 막지 못하고, 고유 프리픽스가 필요하다**는 걸 확인한 건입니다.

### 179. 컴파운드 클래스 패턴과 BEM의 공존
**A.** 정책 페이지 5종이 같은 레이아웃 계열이라 `class="terms <scope>"` 형태로 스타일을 상속시켰습니다.
```html
<main class="terms">           <!-- terms.html -->
<main class="terms privacy">   <!-- privacy.html -->
<main class="terms refund">    <!-- refund.html -->
```
`.terms`에 단독 base 규칙이 없고 전부 descendant 규칙이라 안전하게 상속됩니다. **완성된 terms 페이지를 한 줄도 고치지 않고** 뒤 4종이 히어로·목차·표·contact 스타일을 재사용했습니다.
**BEM과의 관계**: `.terms`는 블록이 아니라 **테마 스코프**입니다. 내부 요소는 BEM으로 짜고, 스코프만 컴파운드로 얹는 구조입니다. 충돌하지 않습니다.
▸ **위험**: 스코프 클래스가 늘어나면 `.desk-page a { color: inherit }` 같은 포괄 규칙이 생기고, 그게 102번의 명시도 사고를 만듭니다. **실제로 겪었습니다.**

### 180. Sass를 왜 안 썼나 🔥
**A.** 두 프로젝트 모두 **빌드 도구 없는 정적 사이트**로 설계했기 때문입니다(ADR-06). Sass는 컴파일 단계가 필요하고, 그러면 저장소에 소스와 빌드 결과가 공존하게 됩니다. 검수 대상이 무엇인지 흐려집니다.
**Sass를 썼다면 유리했을 지점은 분명히 있습니다.**
- 미디어쿼리 믹스인 → 5구간 브레이크포인트를 이름으로 호출
- `@each`로 유틸리티 생성
- 중첩으로 컴포넌트 스코프 표현
**다룰 수 있습니다.** 다만 지금은 CSS 변수 + `@layer` + 네이티브 중첩(CSS Nesting)으로 상당 부분 대체 가능해서, 신규 프로젝트에서 Sass가 필수는 아니라고 봅니다.
▸ 팀에서 쓰면 따릅니다. 이건 취향이 아니라 컨벤션 문제입니다.

### 181. Tailwind에 대한 의견
**A.** **팀 규모와 프로젝트 성격에 따라 갈린다**고 봅니다.
장점: 클래스명 고민이 사라지고, 명시도 전쟁이 없고, 미사용 CSS가 자동 제거되고, 디자인 토큰이 강제됩니다. 제가 겪은 문제 중 **하드코딩 색상 205건 같은 건 Tailwind에서 구조적으로 발생하지 않습니다.**
단점: 마크업이 길어져 가독성이 떨어지고, 디자인 시스템이 이미 있으면 매핑 비용이 듭니다. 그리고 **퍼블리싱 산출물을 개발자에게 넘기는 워크플로에서는 빌드 의존이 부담**입니다.
▸ 솔직하게: "실무에서 써본 적은 없습니다. 학습해서 쓰는 데 문제는 없다고 봅니다."

### 182. 브라우저 렌더링 과정
**A.**
```
HTML 파싱 → DOM
CSS 파싱  → CSSOM
DOM + CSSOM → Render Tree (display:none 요소는 제외)
→ Layout(Reflow)  각 요소의 위치·크기 계산
→ Paint           픽셀 채우기
→ Composite       레이어 합성
```
**CSS는 렌더 블로킹**입니다. CSSOM이 완성돼야 렌더 트리가 만들어지므로, CSS 로딩이 늦으면 아무것도 안 그려집니다. **71번의 `@import` 7단 체인이 정확히 이 지점을 악화시킵니다.**
`visibility: hidden`은 렌더 트리에 남고(공간 차지), `display: none`은 제외됩니다 → 143번.

### 183. `<script>` 위치 / `defer` vs `async`
| | 다운로드 | 실행 시점 | 순서 보장 |
| --- | --- | --- | --- |
| 일반 | 파싱 중단 | 즉시 | ○ |
| `async` | 병렬 | **다운로드 즉시** (파싱 중단) | **✗** |
| `defer` | 병렬 | **DOM 파싱 완료 후** | ○ |

**DOM을 조작하는 스크립트는 `defer`**가 정답입니다. `async`는 독립적인 것(분석 스크립트)에만.
`<body>` 끝 배치는 `defer`와 실행 시점이 비슷하지만, **다운로드 시작이 늦습니다.** `<head>` + `defer`가 낫습니다.
▸ HOOT UP CH-ARCH-01에서 `nav.js`·`search.js`·`notifications.js`를 `common.js` 677줄로 통합한 것도 `<head>` 태그 수를 줄이려는 조치였습니다.

---

# 07. JavaScript

### 184. `var`/`let`/`const` · 호이스팅
**A.** `var`는 함수 스코프, `let`/`const`는 블록 스코프입니다. `const`는 재할당 금지(객체 내부 변경은 가능).
**호이스팅**은 선언이 스코프 최상단으로 끌어올려지는 동작입니다. `var`는 `undefined`로 초기화되어 접근 가능하지만, `let`/`const`는 **TDZ(Temporal Dead Zone)**에 있어 선언 전 접근 시 `ReferenceError`입니다.
▸ 기본은 `const`, 재할당이 필요할 때만 `let`, `var`는 안 씁니다.

### 185. 클로저
**A.** 함수가 **자신이 선언된 렉시컬 환경을 기억**하는 것입니다. 외부 함수가 끝나도 내부 함수가 그 변수에 접근합니다.
**실제 사용 사례** — 알림 드롭다운의 리스너 해제입니다. 열 때 핸들러를 만들면서 해당 패널과 트리거를 클로저로 잡아두고, 닫을 때 같은 참조로 `removeEventListener`를 호출합니다. 클로저가 없으면 어떤 리스너를 떼야 하는지 특정할 수 없습니다.
▸ 140번(리스너 누수)과 직접 연결됩니다.

### 186. `this` / 화살표 함수
**A.** `this`는 **호출 방식**으로 결정됩니다.
| 호출 | `this` |
| --- | --- |
| 일반 함수 | `undefined`(strict) 또는 전역 |
| 메서드 `obj.fn()` | `obj` |
| `new Fn()` | 새 인스턴스 |
| `call`/`apply`/`bind` | 지정한 값 |
| 이벤트 핸들러 | 이벤트가 붙은 요소 |

**화살표 함수는 자신의 `this`가 없습니다.** 선언 위치의 `this`를 그대로 씁니다(렉시컬).
▸ 함정: 이벤트 핸들러를 화살표로 쓰면 `this`가 요소가 아닙니다. `e.currentTarget`을 쓰세요.

### 187. 버블링 · 캡처링 · 위임
**A.** → 139번 참조. **이벤트 위임**은 자식마다 리스너를 붙이지 않고 **부모 하나에 붙여 `e.target`으로 분기**하는 패턴입니다.
```js
list.addEventListener('click', e => {
  const btn = e.target.closest('[data-remove]');
  if (!btn) return;
  removeItem(btn.dataset.remove);
});
```
장점: 리스너 1개로 끝나고, **동적으로 추가된 요소도 자동 대응**됩니다.
▸ **JWA 실사용 사례가 더 명확합니다** — 상품 상세의 사이즈 선택 잠금입니다. 처음에는 '담기' 버튼이 없고, 사이즈를 고른 뒤에야 버튼이 생기면서 `data-panel`이 붙습니다. **이 버튼은 리스너를 붙이는 시점에 DOM에 존재하지 않습니다.** 그래서 직접 바인딩하면 절대 동작하지 않고, 상위 컨테이너에 위임으로 걸어야 합니다.
▸ HOOT UP 쪽 사례: 장바구니 개별 삭제, 카드 그리드 152개, 필터 칩. 카드마다 리스너를 붙이면 152개가 됩니다.

### 188. `preventDefault()` vs `stopPropagation()`
**A.** `preventDefault()`는 **브라우저 기본 동작**을 막습니다(링크 이동, 폼 전송, 체크박스 토글). `stopPropagation()`은 **이벤트 전파**를 막습니다.
둘은 독립적입니다. `return false`는 jQuery에서만 둘 다 하고, 순수 JS 이벤트 핸들러에서는 그렇지 않습니다.
▸ **`stopPropagation()`이 꼭 필요했던 사례** — JWA 커스텀 셀렉트입니다. 옵션을 클릭하면 셀렉트가 닫히는데, 그 클릭이 `document`까지 올라가면 바깥 클릭 감지가 받아서 **닫히자마자 다시 열립니다.** 옵션 클릭에서 전파를 끊어야 해결됩니다. 상품 카드 14개가 한 화면에 있어도 서로 간섭이 0인 것도 이 처리 덕분입니다.
▸ **다만 신중해야 합니다.** 상위의 바깥 클릭 감지나 분석 스크립트를 깨뜨립니다. 이게 139번에서 캡처 단계를 쓴 이유이고, 두 방식은 짝입니다 — 안쪽에서 전파를 끊을수록 바깥 감지는 캡처로 내려가야 합니다.

### 189. 동기·비동기 · 이벤트 루프
**A.** JS는 **싱글 스레드**입니다. 콜 스택이 비면 이벤트 루프가 큐에서 작업을 꺼내 스택에 올립니다.
```
콜 스택 → (비면) → 마이크로태스크 큐 전부 소진 → 매크로태스크 1개 → 렌더
```
- **마이크로태스크**: Promise `.then`, `queueMicrotask`, `MutationObserver`
- **매크로태스크**: `setTimeout`, `setInterval`, 이벤트 콜백, I/O
**마이크로태스크가 항상 먼저**이고, 렌더링보다도 먼저입니다. 그래서 마이크로태스크를 무한 생성하면 화면이 멈춥니다.
▸ TBT 0을 달성했다는 건 메인 스레드를 50ms 이상 막는 긴 작업이 없다는 뜻입니다.

### 190. Promise와 async/await
**A.** `async/await`는 Promise의 **문법 설탕**입니다. `async` 함수는 항상 Promise를 반환하고, `await`는 Promise가 resolve될 때까지 함수 실행을 멈춥니다.
```js
try { const r = await fetch(url); const d = await r.json(); }
catch (e) { /* reject와 예외를 함께 잡음 */ }
```
▸ 병렬이 필요하면 `await`를 연속으로 쓰지 말고 `Promise.all()`. 순차 `await` 3개는 3배 느립니다.

### 191. `querySelector` vs `getElementById`
**A.** `getElementById`가 더 빠릅니다 — 브라우저가 ID 해시맵을 갖고 있습니다. `querySelector`는 CSS 셀렉터를 파싱해서 순회합니다.
**실측 차이는 대부분 무의미합니다.** 반복문 안에서 수만 번 호출하는 게 아니면 가독성과 일관성을 택하는 게 낫습니다.
▸ 중요한 차이: `querySelectorAll`은 **정적 NodeList**(스냅샷), `getElementsByClassName`은 **라이브 HTMLCollection**(DOM 변경 시 자동 갱신). 라이브 컬렉션을 순회하며 DOM을 지우면 인덱스가 어긋납니다.

### 192. DOM 조작 성능
**A.** ① **DocumentFragment**로 모아서 한 번에 삽입 ② **읽기와 쓰기를 분리** — 번갈아 하면 강제 동기 레이아웃 발생 ③ `innerHTML` 문자열 조립 후 1회 주입 ④ 애니메이션은 `transform`/`opacity`.
```js
const frag = document.createDocumentFragment();
items.forEach(i => frag.appendChild(makeCard(i)));
list.appendChild(frag);        // 리플로우 1회
```

### 193. 디바운스 vs 스로틀
**A.** **디바운스** = 마지막 호출 후 N ms 동안 조용하면 1회 실행. **스로틀** = N ms에 최대 1회 실행.
| 상황 | 선택 |
| --- | --- |
| 검색 인풋 자동완성 | 디바운스 — 타이핑이 끝났을 때만 |
| 리사이즈 후 레이아웃 재계산 | 디바운스 |
| 스크롤 위치 추적, 무한 스크롤 | **스로틀** — 중간중간 계속 필요 |
| 버튼 중복 클릭 방지 | 스로틀(leading) |
▸ **JWA에 실제로 쓴 사례가 있습니다.** 슬라이더가 화면 폭에 따라 1장·2장·3장으로 바뀌는데, 리사이즈 이벤트마다 다시 그리면 창을 끄는 동안 수십 번 재생성됩니다. **0.3초 디바운스를 걸고, 그다음에도 구간이 실제로 바뀐 경우에만 재생성**하도록 조건을 하나 더 뒀습니다.
```js
if (w <= 767) return 'mobile';
if (w <= 1279) return 'tablet';
return 'desktop';
```
디바운스만으로는 1280px→1500px 이동처럼 **구간이 그대로인 리사이즈에서도 한 번 재생성**됩니다. 디바운스는 횟수를 줄이고, 구간 비교는 불필요한 실행 자체를 없앱니다.
▸ HOOT UP 헤더 검색도 디바운스 대상입니다.

### 194. `IntersectionObserver`
**A.** 요소가 뷰포트(또는 지정 루트)에 **들어오고 나가는 것을 비동기로 감지**합니다. 스크롤 이벤트 + `getBoundingClientRect()` 조합과 달리 **메인 스레드를 막지 않습니다.**
용도: 이미지 지연 로딩, 무한 스크롤, 스크롤 진입 애니메이션, **스크롤 스파이**.
▸ **HOOT UP에 바로 쓸 곳**: PDP 앵커 내비 5탭(소개·커리큘럼·후기·FAQ·멘토)의 섹션 스크롤 연동이 정확히 이 패턴입니다.

### 195. `countdown.js` 47줄 / `setInterval` 정확도
**A.** 목표 시각을 고정해두고 1초마다 **현재 시각과의 차이를 다시 계산**해서 표시하는 구조입니다. 남은 초를 `--` 하는 방식이 아닙니다.
**이유가 `setInterval`의 정확도 문제입니다.** `setInterval(fn, 1000)`은 정확히 1초를 보장하지 않습니다. 메인 스레드가 막히면 밀리고, **백그라운드 탭에서는 브라우저가 타이머를 1초 이상으로 throttle**합니다. 차감식이면 탭을 오래 두면 시간이 어긋납니다.
```js
const target = new Date('2026-08-20T23:59:59').getTime();
setInterval(() => {
  const diff = target - Date.now();     // 매번 재계산 → 누적 오차 없음
  render(diff);
}, 1000);
```
▸ 132번과 연결: 이 요소는 `aria-live`에서 제외했습니다.

### 196. `account-nav.js` 28줄이 21페이지에서 동작하는 이유
**A.** **마크업 규약을 정하고 스크립트는 규약만 보게 했기 때문**입니다.
```
[data-acc-trigger]  — 아코디언 여는 버튼
.is-open            — 열림 상태 클래스
aria-expanded       — 접근성 상태 동기화
```
계정은 9페이지 3그룹(LEARNING·PAYMENT·SETTINGS), 크리에이터는 12페이지 4그룹(STUDIO·COMMUNITY·REVENUE·SUPPORT)으로 **구조가 다른데 같은 스크립트가 붙습니다.** 스크립트가 그룹 개수나 이름을 모르기 때문입니다.
**"상위 카테고리 단일 오픈"** 규칙도 스크립트 안에 있습니다 — 하나를 열면 나머지가 닫힙니다.
▸ **이게 컴포넌트 설계의 요점입니다.** 스크립트가 특정 페이지 구조를 알면 페이지마다 스크립트가 생깁니다.

### 197. 리스너 메모리 누수 방지
**A.** → 140번. `removeEventListener`(같은 함수 참조 필요), `AbortController` + `signal`, `{ once: true }`, SPA면 언마운트 훅에서 정리.

### 198. jQuery로만 되고 Vanilla로 안 되는 게 있나 🔥
**A.** **없습니다.** jQuery의 주요 기능은 전부 표준으로 들어왔습니다.
| jQuery | Vanilla |
| --- | --- |
| `$(sel)` | `querySelectorAll` |
| `.addClass/removeClass/toggleClass` | `classList` |
| `$.ajax` | `fetch` |
| `.on()` 위임 | `addEventListener` + `closest()` |
| `.animate()` | CSS `transition` / Web Animations API |
| `.fadeIn()` | `transition` + `opacity` |
jQuery가 여전히 유효한 경우는 **레거시 코드베이스와 옛 브라우저 지원**뿐입니다.
▸ **JWA의 jQuery는 학습 시점의 잔재이고, HOOT UP은 의존성 0입니다.** 이 대비를 반드시 붙이세요 → 83~85번.

### 199. ES 모듈 vs 다중 `<script>` / `common.js` 통합 이유
**A.** ES 모듈(`type="module"`)은 스코프가 파일 단위로 격리되고 의존 관계가 명시적입니다. 자동으로 `defer` 동작이고 strict mode입니다.
`common.js` 677줄로 통합한 이유(CH-ARCH-01)는 **164개 HTML `<head>`의 스크립트 태그 수를 줄이기 위해서**였습니다. 헤더가 파일마다 복제된 구조라(ADR-06), 스크립트 태그 하나를 추가하면 164곳을 고쳐야 합니다.
▸ **정직한 한계**: "모듈로 갔다면 파일을 나눈 채로 진입점 하나만 두면 됩니다. 통합 대신 모듈이 더 나은 선택이었을 수 있습니다. 다만 `type="module"`은 `file://`에서 CORS 때문에 안 열려서, 로컬 확인 편의를 위해 통합을 택했습니다."

---

# 08. 웹 접근성 · 웹 표준

### 200. 접근성이 왜 필요한가 / 법적 의무
**A.** **① 권리** — 장애인차별금지법상 웹 접근성 준수는 의무입니다. 정보통신 접근성 미보장은 차별에 해당합니다. **② 대상 확대** — 영구 장애뿐 아니라 일시적(팔 부상), 상황적(밝은 야외, 한 손 사용) 제약을 포함합니다. **③ 부수 효과** — 시맨틱 마크업은 SEO와 유지보수에 그대로 이득입니다.
국내 기준은 **KWCAG(한국형 웹 콘텐츠 접근성 지침)**이고 WCAG 기반입니다. 공공기관은 웹 접근성 품질인증(WA 마크) 대상입니다.
▸ 확신 없는 조항 인용은 피하세요. "제가 아는 범위에서는"을 붙이는 게 안전합니다.

### 201. WCAG 2.1 AA는 무엇인가요?

**A.** WCAG는 웹 콘텐츠 접근성을 위한 국제적인 지침이고, A·AA·AAA처럼 적합성 수준이 나뉩니다.

실무에서는 AA 수준이 많이 기준으로 사용되며, 일반 본문 텍스트 대비 4.5:1, 큰 텍스트 3:1, 키보드 접근, 논리적인 포커스 순서, 오류 식별 같은 항목이 포함됩니다.

저는 개별 수치를 외우는 데서 끝내기보다 실제 페이지를 키보드와 자동 검사 도구로 확인하고, 수동 검증이 필요한 부분을 별도로 점검하는 것이 중요하다고 생각합니다.

### 202. WCAG의 4대 원칙을 설명해주세요.

**A.** POUR로 정리합니다.

Perceivable은 사용자가 정보를 인식할 수 있어야 한다는 원칙으로 대체 텍스트나 자막, 명도 대비가 여기에 포함됩니다.

Operable은 키보드 탐색과 포커스처럼 실제로 조작할 수 있어야 한다는 의미입니다.

Understandable은 레이블과 오류 안내, 일관된 내비게이션처럼 내용과 동작을 이해할 수 있어야 한다는 원칙입니다.

Robust는 올바른 HTML과 ARIA를 사용해서 다양한 브라우저와 보조기술에서도 의미가 전달되도록 만드는 것입니다.
### 203. `alt` 작성 원칙
**A.** → 88번 표 참조. 원칙 세 가지.
① **이미지가 전달하는 정보를 텍스트로** — 외형 묘사가 아니라 역할. 로고면 "회사명", 링크 안 아이콘이면 이동 목적지
② **장식은 `alt=""`** — 속성은 반드시 두고 값만 비웁니다
③ **인접 텍스트와 중복 금지** — 캡션에 이미 있으면 `alt=""`
▸ "이미지"·"사진" 같은 단어는 넣지 않습니다. 스크린리더가 이미 "이미지"라고 알립니다.

### 204. 아이콘 버튼 접근성
**A.** 두 방법입니다.
```html
<!-- ① 숨김 텍스트 -->
<button type="button"><svg aria-hidden="true">…</svg>
  <span class="sr-only">장바구니 열기</span></button>

<!-- ② aria-label -->
<button type="button" aria-label="장바구니 열기">
  <svg aria-hidden="true">…</svg></button>
```
**SVG에 `aria-hidden="true"`가 필수**입니다. 없으면 내부 `<title>`이나 경로가 읽힐 수 있습니다.
▸ **①이 더 안전합니다.** `aria-label`은 자동 번역이 안 되는 경우가 있고, 브라우저 확장이 못 읽는 사례도 있습니다.
▸ 실제 적용: 장바구니·알림 배지에 "2개 담김"·"2건 읽지 않음"을 숨김 텍스트로 넣었습니다.

### 205. 숨김 텍스트 CSS
```css
.sr-only {
  position: absolute;
  width: 1px; height: 1px;
  padding: 0; margin: -1px;
  overflow: hidden;
  clip-path: inset(50%);
  white-space: nowrap;
  border: 0;
}
```
`display: none`·`visibility: hidden`은 **스크린리더도 읽지 않습니다.** `text-indent: -9999px`는 RTL 언어와 포커스 스크롤 문제가 있습니다. `clip-path: inset(50%)`가 현재 표준적 방법입니다.
▸ 포커스 가능한 요소라면 `.sr-only-focusable`로 `:focus` 시 드러나게 → 134번 스킵 링크.

### 206. `aria-label` / `labelledby` / `describedby`
| | 용도 | 우선순위 |
| --- | --- | --- |
| `aria-label` | 문자열로 직접 이름 지정 | 중 |
| `aria-labelledby` | **다른 요소의 텍스트**를 이름으로. 여러 ID 나열 가능 | **최상** |
| `aria-describedby` | **부가 설명**. 이름이 아니라 설명으로 읽힘 | — |
```html
<div role="dialog" aria-labelledby="t" aria-describedby="d">
  <h2 id="t">회원가입</h2>
  <p id="d">필수 항목을 모두 입력해주세요.</p>
```
`labelledby`가 `label`보다 우선합니다. 화면에 이미 보이는 텍스트가 있으면 `labelledby`가 낫습니다 — 중복 관리가 사라집니다.

### 207. ARIA를 안 쓰는 게 나은 경우
**A.** **"No ARIA is better than bad ARIA"** — 잘못된 ARIA는 없는 것보다 나쁩니다.
① **네이티브 요소로 되면 그걸 씁니다.** `<div role="button" tabindex="0">`보다 `<button>`이 낫습니다. 키보드·포커스·폼 연동이 전부 기본 제공입니다
② **역할 중복 금지** — `<nav role="navigation">`은 불필요
③ **`aria-hidden` 오용 주의** — 포커스 가능 요소에 걸면 "포커스는 갔는데 아무것도 안 읽히는" 상태가 됩니다 → 144번
▸ **실제 적용 사례**: 08-03에 로그인·회원가입 트리거를 `<a href="/login">`에서 `<button type="button">`으로 바꿨습니다. 링크로 두면 스크린리더가 "링크"로 읽는데 이동이 없고, 우클릭 새 탭이 404가 됩니다. **태그를 의미대로 쓰는 게 ARIA보다 먼저입니다.**

### 208. 스크린리더를 써봤나 🔥
**A.** [실제 사용 여부를 정직하게]
- 써봤다면: "NVDA(Windows)로 [특정 페이지]를 탐색해봤습니다. [발견한 문제]"
- 안 써봤다면: **"체계적으로는 못 써봤습니다. macOS VoiceOver로 부분 확인한 정도입니다. 대신 키보드 탐색과 axe·Lighthouse로 검증했고, 스크린리더 실사용 검증은 제 미검증 영역입니다."**
▸ **주의**: 실제 사용 경험을 구체적으로 묻는 꼬리질문에 답하기 어려워질 수 있습니다. 대신 **"입사하면 NVDA로 검증하는 절차를 제 루틴에 넣겠습니다"**로 연결하세요.
▸ 준비: NVDA는 무료입니다. 면접 전에 30분만 써보고 오면 이 질문이 강점이 됩니다.

### 209. 키보드만으로 탐색해봤나 / 어디서 막혔나 🔥
**A.** 해봤습니다. 그리고 **JWA에서 막혔습니다.**
`:focus-visible`이 0건이라 지금 어디에 있는지 알 수 없습니다. 그리고 메가 메뉴가 호버 전용이라 하위 카테고리에 키보드로 도달할 수 없습니다. **두 가지 다 PRD에 미달로 적어뒀습니다.**
**HOOT UP에서는 이걸 잡았습니다.** 메가 메뉴에 ↑↓/Home/End 이동과 Esc 닫기를 넣었고, 닫힘 상태를 `visibility: hidden`으로 처리해서 보이지 않는 링크가 탭 순서에 남지 않게 했습니다. 알림 드롭다운은 Esc로 닫고 트리거로 포커스가 복귀합니다.
**미검증으로 남은 건 로그인 모달의 포커스 트랩입니다** → 135번.

### 210. 폼 접근성 — `<label>` 연결 2가지 / 에러 안내
```html
<!-- ① 명시적 연결 -->
<label for="email">이메일</label>
<input id="email" type="email">

<!-- ② 암묵적 (감싸기) -->
<label>이메일 <input type="email"></label>
```
**①이 권장**입니다. 스타일링이 자유롭고 일부 보조기술 호환이 낫습니다.
**에러 안내**
```html
<input id="pw" aria-invalid="true" aria-describedby="pw-err">
<p id="pw-err" role="alert">비밀번호는 8자 이상이어야 합니다.</p>
```
- `aria-invalid` — 이 필드가 오류 상태임
- `aria-describedby` — 오류 메시지를 필드 이름 뒤에 읽어줌
- `role="alert"` — 즉시 낭독 (`aria-live="assertive"`와 동등)
- **색상만으로 표시 금지** — 아이콘이나 텍스트 병기
▸ JWA는 필수 항목을 `*`로 표기하고 안내 문구를 상단에 명시했습니다(FR-AUTH-02). 다만 `aria-invalid`·`aria-describedby`는 미적용이고, 이건 개선 대상입니다.

### 211. 테이블 접근성
```html
<table>
  <caption>2026년 분기별 매출</caption>
  <thead>
    <tr><th scope="col">분기</th><th scope="col">매출</th></tr>
  </thead>
  <tbody>
    <tr><th scope="row">1분기</th><td>1,200</td></tr>
  </tbody>
</table>
```
- `<caption>` — 표의 제목. 스크린리더가 표 진입 시 먼저 읽음
- `scope="col"/"row"` — 헤더가 어느 방향을 지배하는지
- 복잡한 표는 `headers` + `id`로 셀 단위 연결
▸ **레이아웃 목적의 `<table>`은 금지**입니다. 스크린리더가 표로 읽습니다.

### 212. 모달 접근성 요건 5가지
**A.**
① `role="dialog"` + `aria-modal="true"`
② `aria-labelledby`로 제목 연결
③ **포커스 트랩** — 탭이 모달 밖으로 새지 않음
④ **Esc로 닫기 + 트리거로 포커스 복귀**
⑤ **배경 스크롤 잠금** + 배경에 `inert` 또는 `aria-hidden`
▸ HOOT UP 적용 현황: ①②는 확인, ④는 알림 드롭다운에서 검증됨, ③⑤는 **M9 미검증** → 135~136번.
▸ **`<dialog>` + `showModal()`이면 ①③④⑤가 브라우저 기본 제공**입니다.

### 213. 색상만으로 정보 전달 금지 이유
**A.** 색각 이상(남성 약 8%), 흑백 출력, 고대비 모드, 저조도 환경에서 정보가 사라집니다.
**대응**: 아이콘·텍스트·패턴을 병기. 필수 항목은 색이 아니라 `*`와 텍스트로(FR-AUTH-02), 오류는 색 + 아이콘 + 메시지로.
▸ 실제 사례: 활성 페이저를 색만으로 구분하다가 대비 부족 지적이 나와서 **활성 배경 `--color-neutral-dark` + 호버 텍스트 흰색**으로 처리했습니다.

### 214. 대비 기준 4.5:1 / 3:1
| 기준 | 대상 |
| --- | --- |
| **4.5:1** (AA) | 본문 텍스트 |
| **3:1** (AA) | 큰 텍스트 — 18pt(24px) 이상, 또는 14pt(18.66px) bold 이상 |
| **3:1** (AA) | **비텍스트** — UI 컴포넌트 경계, 그래픽, 포커스 표시 |
| 7:1 / 4.5:1 (AAA) | 본문 / 큰 텍스트 |
▸ HOOT UP 실측: danger dark **5.75**, 브랜드 블루 **4.82** — 둘 다 AA 통과.
▸ **포커스 표시도 3:1 대상**입니다. `:focus-visible` 색을 정할 때 배경 대비를 봐야 합니다.

### 215. 자동 검사로 안 잡히는 항목 🔥
**A.** 자동 도구는 **전체 기준의 30~40% 정도만** 검출한다고 알려져 있습니다. 못 잡는 것들:
① **`alt`의 적절성** — 있는지는 보지만 내용이 맞는지는 모릅니다. "image1.jpg"도 통과
② **포커스 순서의 논리성** — 탭 순서가 시각적 순서와 어긋나도 오류가 아닙니다
③ **오류 메시지의 이해 가능성** — "Error 400"도 통과
④ **키보드 트랩·조작 흐름** — 실제로 눌러봐야 압니다
⑤ **콘텐츠 대체 수단의 품질** — 자막이 있는지는 알지만 정확한지는 모릅니다
⑥ **동적 상태 변화 안내** — `aria-live` 존재는 봐도 적절한 시점에 발화하는지는 모릅니다
▸ **제가 직접 겪은 사례**: 로컬 axe가 어두운 배경의 대비 회귀를 **통과로 판정**했는데 배포본 Lighthouse에서 45건이 나왔습니다 → 129번. **도구 간에도 결과가 다릅니다.**

---

# 09. 성능 최적화

### 216. 성능 최적화 방법 나열
**A.** 네 축으로 정리합니다.
| 축 | 방법 |
| --- | --- |
| **전송량** | 이미지 최적화(WebP/AVIF, 압축, 반응형), 폰트 서브셋, CSS/JS 압축, gzip·brotli |
| **요청 수·순서** | 번들링, `preconnect`/`preload`, `@import` 제거, HTTP/2 |
| **렌더링** | Critical CSS 인라인, JS `defer`, `font-display: swap`, 레이아웃 예약(width/height) |
| **캐싱** | 해시 파일명 + `immutable`, CDN, 서비스 워커 |
▸ **실제 적용분**: WebP 328개(12MB→5.3MB), CSS 중복 272그룹 통합, 캐시버스터 370건 제거.

### 217. Core Web Vitals 3종 · 기준
**A.** **LCP 2.5s 이하 · CLS 0.1 이하 · INP 200ms 이하** → 156번 표 참조.

### 218. 렌더 블로킹 리소스
**A.** CSS와 일반 `<script>`입니다. CSSOM이 완성돼야 렌더 트리가 만들어지고, `<script>`는 파싱을 중단시킵니다.
**해결**: CSS는 Critical CSS 인라인 + 나머지 지연 로드, JS는 `defer`, 조건부 CSS는 `media` 속성으로 분리.
```html
<link rel="stylesheet" href="print.css" media="print">   <!-- 논블로킹 -->
```
▸ **71번의 `@import` 7단 체인이 이 문제의 최악 형태**입니다.

### 219. Critical CSS
**A.** 첫 화면(above the fold) 렌더에 필요한 최소 CSS를 `<style>`로 **HTML에 인라인**하고, 나머지는 비동기 로드하는 기법입니다.
```html
<style>/* critical */</style>
<link rel="preload" href="main.css" as="style" onload="this.rel='stylesheet'">
```
FCP·LCP를 직접 개선합니다. 도구는 `critical`, `penthouse` 등이 있고 빌드 단계에서 추출합니다.
▸ **정직하게**: "빌드 도구가 없는 구조라 적용하지 못했습니다. 개념은 알고 있고, 도입하려면 빌드 파이프라인이 선행돼야 합니다."

### 220. 이미지 최적화 전략 전체
**A.**
① **포맷** — AVIF > WebP > JPG/PNG. 아이콘·로고는 SVG
② **크기** — 표시 크기의 최대 2배까지만. 실제로 과대 JPG 19개를 최대 1000px·품질 80으로 재압축해 8.2MB 절감
③ **반응형** — `srcset` + `sizes`로 뷰포트별 다른 해상도
④ **지연** — 뷰포트 밖 `loading="lazy"`, 첫 화면은 `fetchpriority="high"`
⑤ **레이아웃 예약** — `width`/`height` 필수 (CLS)
⑥ **디코딩** — `decoding="async"`
⑦ **CDN** — 이미지 CDN이 있으면 리사이즈·포맷 협상을 서버가 처리

### 221. 웹폰트는 어떻게 최적화하겠습니까?

**A.** 우선 WOFF2처럼 압축 효율이 좋은 포맷을 사용하고, 실제 필요한 글자만 포함하는 서브셋을 검토하겠습니다.

사용하는 font-weight도 꼭 필요한 범위로 줄이고, 첫 화면에 중요한 폰트는 `preload`, 렌더링 지연을 줄이기 위해 `font-display: swap` 같은 옵션을 함께 사용할 수 있습니다.

다만 preload를 너무 많이 사용하면 다른 핵심 리소스와 경쟁할 수 있기 때문에 실제 사용량과 첫 화면 중요도를 보고 제한적으로 적용하겠습니다.

### 222. 한글 웹폰트가 특히 무거운 이유와 대응 방법은 무엇인가요?

**A.** 한글은 영문보다 필요한 글자 수가 훨씬 많기 때문에 같은 폰트라도 파일 크기가 크게 늘어날 수 있습니다.

그래서 전체 글리프를 한 번에 내려받기보다 실제 서비스에서 사용하는 글자만 서브셋으로 만들거나, `unicode-range`를 이용해 필요한 범위의 폰트만 다운로드하도록 나누는 방법을 사용할 수 있습니다.

또 Regular와 Bold처럼 실제 사용하는 weight만 남기는 것도 중요합니다. 가변 폰트를 사용할 수 있는 환경이라면 여러 weight 파일을 하나로 줄이는 방법도 검토할 수 있습니다.

▸ **두 프로젝트의 폰트 구성이 다릅니다. 헷갈리지 마세요.**

| | JW Anderson | HOOT UP |
| --- | --- | --- |
| 헤드라인 | Old Standard TT (serif) | Raleway |
| 본문 · UI | Roboto (variable 100–900) | Spoqa Han Sans Neo |
| 아이콘 | Material Symbols Outlined | — |

**한글 폰트 용량 문제는 HOOT UP 쪽 이슈입니다.** JWA는 영문 폰트라 서브셋 압박이 작고, 대신 **`@import` 체인에 폰트 2개가 물려 있는 것**이 성능 문제입니다(71번). HOOT UP은 Spoqa Han Sans Neo를 쓰기 때문에 위에 말씀드린 서브셋·`unicode-range` 전략이 그대로 필요하고, M11의 폰트 렌더 차단 1,590ms가 그 결과입니다.
### 223. HTTP 캐시 헤더
| 헤더 | 역할 |
| --- | --- |
| `Cache-Control: max-age=N` | N초 동안 재검증 없이 캐시 사용 |
| `immutable` | 만료 전에는 재검증 자체를 안 함 |
| `no-cache` | 캐시하되 **매번 서버에 재검증** |
| `no-store` | 캐시 금지 |
| `ETag` | 콘텐츠 지문. `If-None-Match`로 재검증 → 같으면 **304** |
| `Last-Modified` | 수정 시각 기반 재검증 |
**해시 파일명 + `max-age=31536000, immutable`** 조합이 정적 자산의 표준입니다 → 151~152번.
로컬 `serve.py`에 `no-store`를 건 것은 개발 중 캐시 혼란을 없애기 위해서입니다.

### 224. 번들 크기 줄이기
**A.** 트리 셰이킹, 코드 스플리팅(라우트별 지연 로드), 미사용 CSS 제거(PurgeCSS), 라이브러리 대체(moment→date-fns), 동적 `import()`, 압축(brotli).
▸ **이 프로젝트에서는 번들러가 없어서** 해당 없습니다. 대신 한 게 CSS 중복 272그룹 통합(10,174→9,883줄)과 JS 진입점 통합입니다.

### 225. 392페이지에서 성능 측정을 시작한다면
**A.** **페이지 유형별 대표 1개씩**입니다. 392개를 다 재는 건 의미가 없습니다. 같은 템플릿이면 결과도 같습니다.
측정 대상 5종: ① 메인(가장 무거움, 히어로+다수 카드) ② 리스트(카드 최다) ③ 상세(이미지 최다) ④ 폼(장바구니·체크아웃) ⑤ 문서형(약관 — 가장 가벼움, 기준선)
**그리고 실제 트래픽 기준으로 우선순위를 정합니다.** 실서비스면 GA로 유입 상위 페이지부터, 포트폴리오면 관람 동선 순서대로(메인 → 리스트 → 상세).
▸ 실제로 HOOT UP은 **배포본 모바일 기준**으로 측정했습니다 → 128~129번.

---

# 10. Git · 협업

### 226. 브랜치 전략
**A.** **Git Flow** — `main`/`develop`/`feature`/`release`/`hotfix`. 릴리즈 주기가 정해진 제품에 맞습니다. 복잡합니다.
**GitHub Flow** — `main` + `feature` 브랜치, PR로 머지, 머지 즉시 배포. 지속 배포에 맞고 단순합니다.
**퍼블리싱 작업은 GitHub Flow가 대부분 적합**하다고 봅니다.
▸ 솔직하게: "제 프로젝트는 혼자 하는 작업이라 `main` 단일 브랜치로 진행했습니다. 브랜치 전략은 개념으로 알고 있고, 팀 규칙을 따르겠습니다."

### 227. merge vs rebase
**A.** **merge**는 병합 커밋을 만들며 이력을 그대로 보존합니다. **rebase**는 커밋을 대상 브랜치 위로 옮겨 붙여 이력을 직선으로 만듭니다.
**원칙**: 공유된(push된) 브랜치는 rebase 금지 — 커밋 해시가 바뀌어 다른 사람 이력이 깨집니다. 로컬 정리용으로만.
▸ `git pull --rebase`로 불필요한 병합 커밋을 줄이는 게 일반적입니다.

### 228. 충돌 해결
**A.** ① 충돌 파일 확인(`git status`) ② 마커(`<<<<<<<` `=======` `>>>>>>>`) 구간을 열어 **양쪽 의도를 파악** ③ 필요한 쪽 또는 양쪽을 합쳐 수정 ④ 마커 제거 후 `git add` ⑤ 커밋
**CSS 충돌은 특히 주의합니다.** 양쪽을 다 살리면 중복 선언이 생기고, 한쪽만 살리면 상대 작업이 사라집니다. **어느 쪽이 나중 규칙인지 확인**해야 합니다.
▸ 어려우면 `git merge --abort`로 되돌리고 상대와 이야기하는 게 낫습니다.

### 229. PR 리뷰 경험
**A.** 정직하게: **"팀 PR 리뷰 경험은 없습니다."**
다만 리뷰에 해당하는 절차는 만들어서 했습니다. HOOT UP에서 AI 산출물을 지적·반려·재지시한 359건이 사실상 코드 리뷰 프로세스입니다. **완료 보고를 실측과 대조**하는 것도 리뷰어의 일이라고 봅니다.
▸ **여기서 배우고 싶은 것을 말하세요**: "제 판단이 맞는지 확인해줄 사람이 없었던 게 독학의 가장 큰 한계였습니다. 리뷰를 받으면서 일하고 싶습니다."

### 230. 이전 117건에 접두어가 없어 이력이 지저분하다 ⚠️
**A.** 지저분합니다. 08-01부터 전환해서 그날 34건 중 33건이 `feat`/`style`/`fix` 형식이고, 그 앞은 없습니다. **히스토리 후반부부터 규칙이 적용된 형태**입니다.
소급 정리는 하지 않았습니다. `rebase`로 메시지를 고치면 커밋 해시가 전부 바뀌는데, **README 검수 기록이 커밋 이력을 참조**하고 있어서 정합성이 깨집니다.
**대신 전환 시점을 문서에 명시했습니다.** 이력이 도중에 바뀐 이유를 설명 가능하게 남기는 쪽을 택했습니다.
▸ 실무라면 프로젝트 시작 시점에 정합니다. 도중 전환은 이런 비용이 붙습니다.

### 231. 이미지 원본을 Git에 커밋한 게 적절한가 / 지금 WebP로 바꿔도 히스토리는 남는데요 🔥
**A.** 적절하지 않습니다. **Git은 바이너리 파일에 취약합니다.**
- 이미지가 바뀔 때마다 전체 파일이 새 오브젝트로 저장됩니다 — diff가 안 됩니다
- 저장소 히스토리에 영구히 남습니다. 나중에 지워도 용량은 안 줄어듭니다
- clone 시간이 길어지고, GitHub 권장 용량(1GB)에 근접합니다
**당시 970MB는 관리 실패였습니다.** 이후 PNG를 WebP로 전환해 이미지가 962.8MB → 92.2MB가 됐고, 저장소 게시분은 **531MB**(WebP 92 · 영상 439)입니다. 다만 **Git 히스토리에는 원본이 그대로 남아 있어서, 저장소 자체의 용량 문제는 해결되지 않았습니다.** 한 번 커밋된 바이너리는 지워도 히스토리에서 안 사라집니다.

**대안 세 가지**
① **Git LFS** — 대용량 파일을 포인터로 관리. 실제 파일은 별도 스토리지
② **외부 스토리지 + CDN** — 저장소에는 참조만
③ **애초에 압축** — HOOT UP처럼 WebP 전환하면 12MB → 5.3MB. **같은 작업을 JWA에는 안 한 것이 문제입니다** → 75번

### 232. Git LFS
**A.** 대용량 파일을 Git 저장소에 직접 넣지 않고 **포인터 파일만 커밋**하고, 실제 파일은 LFS 서버에 두는 확장입니다.
```
git lfs install
git lfs track "*.png"
git add .gitattributes
```
clone 시 필요한 버전만 받으므로 히스토리 전체 용량을 안 받습니다.
▸ 주의: GitHub LFS는 무료 할당량(1GB 저장·1GB/월 대역폭)이 작습니다. **압축이 먼저입니다** — 실제로 WebP 전환으로 962.8MB를 92.2MB까지 줄였고, 그 정도면 LFS 없이도 감당 가능한 범위가 됩니다.

### 233. 디자이너에게 시안을 받을 때 요구하는 것
**A.** 여섯 가지입니다.
① **모든 상태** — default / hover / focus / active / disabled / loading / empty / error
② **반응형 구간** — 최소 3구간의 실제 시안. "알아서 줄여주세요"는 받지 않습니다
③ **디자인 토큰** — 컬러·타이포·간격이 스타일로 등록돼 있을 것. 하드코딩된 값이면 제가 다시 판정해야 합니다
④ **텍스트 최대 길이** — 상품명이 2줄을 넘으면 어떻게 되는지
⑤ **에셋 내보내기 규격** — SVG는 벡터로, 래스터는 2x
⑥ **폰트 라이선스** — 웹폰트 사용 가능 여부
▸ **①이 가장 자주 빠집니다.** 그리고 이게 퍼블리셔가 임의로 만들면 나중에 반드시 수정 요청이 옵니다.

### 234. Figma를 single source of truth로 확정한 이력
**A.** 07-29 첫 검수에서 나온 건입니다.
> 콘텐츠 기획서: Space Grotesk + Pretendard
> 실제 Figma 디자인: Raleway + Spoqa Han Sans Neo

**두 문서가 어긋나 있었습니다.** 어느 쪽이 맞는지 정하지 않으면 AI가 매번 다르게 만듭니다.
**Figma를 단일 원천으로 확정**하고, PRD에 변경 이력을 각주로 남겼습니다. 기획서를 고치지 않고 **어긋난 사실 자체를 기록**한 게 요점입니다.
▸ **실무 원칙**: 문서가 여러 개면 우선순위를 먼저 정합니다. "디자인 확정본은 Figma, 카피는 기획서, 기능 정의는 PRD" 같은 식으로 영역을 나누는 게 낫습니다.

### 235. Figma 노드 ID 지시가 실무에서 유효한가
**A.** 유효하다고 봅니다. 실제로 16종 페이지를 노드 ID로 지정해 생성했습니다.
```
[209-31719] b2b        [209-35536] notice
[209-32939] careers    [209-35834] faq      + md 콘텐츠
[211-26126] terms      [211-27116] privacy
```
**장점은 지시의 모호성 제거**입니다. "채용 페이지 시안대로"는 어느 프레임인지 불명확하지만 노드 ID는 하나입니다.
**실무 적용**: PR 설명이나 이슈에 노드 링크를 붙이면 리뷰어가 시안과 결과를 즉시 대조할 수 있습니다.
▸ 실제 사례: 이용약관은 당초 노드(209-36168)가 부적합해서 신규 노드(211-26126)로 **교체 지시**했습니다. ID로 관리하니 무엇이 바뀌었는지가 남습니다.

### 236. 시안에 없는 상태는 누가 정하나
**A.** **원칙적으로 디자이너입니다. 실무에서는 대부분 퍼블리셔가 먼저 발견합니다.**
제 대응 순서는 이렇습니다.
① 발견 즉시 목록화해서 질문 — 개별로 물으면 왕복이 늘어납니다. 실제로 시안 정합 반려를 할 때 **5개 항목을 한 번에 열거**했습니다
② 급하면 **기존 컴포넌트 규칙을 유추 적용하고 명시적으로 알림** — "버튼 hover 규칙을 이 카드에도 적용했습니다. 다르면 알려주세요"
③ 확정되면 디자인 시스템에 반영 요청 — 같은 질문이 반복되지 않도록
▸ **가장 나쁜 건 조용히 임의 결정하는 것**입니다. 나중에 전수 수정이 됩니다.

### 237. 개발자에게 산출물을 넘길 때
**A.** 마크업만 넘기지 않습니다. 다섯 가지를 함께 줍니다.
① **동적 영역 표시** — 반복 영역, 조건부 노출, 데이터 바인딩 지점
② **클래스 명명 규칙** — 개발자가 클래스를 임의 추가할 때 충돌하지 않도록
③ **상태 클래스 목록** — `.is-open`, `.is-active` 등 JS가 토글할 훅
④ **접근성 요구사항** — 동적 삽입 시 `aria-live`가 필요한 지점, 포커스 이동 규칙
⑤ **브라우저 지원 범위와 알려진 제약**
▸ **실제 사례가 `account-nav.js` 규약입니다** → 196번. `[data-acc-trigger]`, `.is-open`, `aria-expanded` 세 가지만 지키면 그룹 구성이 달라도 동작합니다. **이런 규약 문서가 인수인계 자료입니다.**

### 238. 개발자가 마크업을 수정해서 스타일이 깨졌다
**A.** 순서대로 하겠습니다.
① **먼저 고칩니다.** 책임 규명보다 복구가 먼저입니다
② **왜 수정했는지 묻습니다.** 대개 정당한 이유가 있습니다 — 반복 렌더링, 프레임워크 제약, 조건부 노출
③ **그 요구를 만족하는 마크업을 다시 제안합니다.** "이 구조가 필요하시면 클래스를 이렇게 두면 스타일이 유지됩니다"
④ **재발 방지** — 구조 의존적인 셀렉터(`div > ul > li:first-child`)가 원인이면 **제 CSS가 취약한 것**입니다. 클래스 기반으로 바꿉니다
▸ **핵심**: 구조에 의존하는 CSS를 쓰면 이 사고는 반복됩니다. 마크업이 바뀌어도 안 깨지는 CSS가 좋은 CSS입니다.

### 239. 기획자가 무리한 일정을 요구한다
**A.** **"안 됩니다"가 아니라 "이 조건이면 됩니다"로 답합니다.**
① 작업을 쪼개서 소요를 제시 — "전체는 5일이지만, 데스크탑만이면 2일입니다"
② **범위 조정 선택지 제시** — 반응형 후순위, 인터랙션 2차, 접근성 최소 요건만
③ **감축했을 때 생기는 부채를 명시** — "이 상태로 나가면 모바일에서 가로 스크롤이 발생합니다. 다음 스프린트에 반드시 잡아야 합니다"
▸ **실제로 이렇게 했습니다.** 08-02에 미디어쿼리 구간을 정리하면서 **"일단 구간만 정리, 구간별 디자인은 계정 페이지 완성 후"**라고 범위를 명시했습니다. 정리와 구현을 한 번에 하지 않고 순서를 나눈 판단입니다.

### 240. 본인이 정한 디자인 규칙을 다시 되돌린 사례가 있나요?

**A.** 있습니다.

HOOT UP에서 이미지 모서리를 디자인 토큰 기준으로 일괄 통일했다가 작은 썸네일에서는 20px 라운딩이 지나치게 크게 보여 해당 컴포넌트만 기존 3px 수준으로 되돌린 적이 있습니다.

또 로드맵 강의 제목의 hover 색상을 브랜드 색상으로 적용했다가 실제 화면에서 가독성과 분위기가 맞지 않아 다시 흰색으로 변경한 사례도 있습니다.

규칙을 적용한 뒤 실제 결과를 확인하고 필요하면 예외를 두었습니다.

### 241. 규칙을 다시 바꾸면 일관성이 없는 것 아닌가요?

**A.** 일관성은 중요하지만 규칙 자체가 목적은 아니라고 생각합니다.

공통 규칙을 적용했을 때 특정 컴포넌트의 사용성과 시각 결과가 오히려 나빠진다면 예외를 둘 수 있습니다.

다만 예외를 만들 때는 어느 부분이 왜 다른지 기록하고, 같은 예외가 반복되면 개별 컴포넌트 문제가 아니라 원래 규칙이 잘못된 것은 아닌지 다시 검토해야 합니다.

즉 규칙을 쉽게 바꾸기보다 결과와 근거를 확인한 뒤 제한적으로 수정하는 편입니다.
### 242. 협업 툴
**A.** Notion(문서·데이터베이스), Figma(디자인 핸드오프), GitHub(형상 관리·Pages 배포), Slack. 포트폴리오 문서군을 Notion 데이터베이스로 관리하고 있고, Figma는 노드 단위로 지시할 수준까지 씁니다.
▸ Jira·Confluence는 미경험 — 정직하게 말하고 "학습에 문제없습니다"로.

---

# 11. 실무 시나리오

### 243. IE11 대응 요구
**A.** 먼저 **정말 필요한지 데이터로 확인**하겠습니다. IE는 지원이 종료됐고, 실제 트래픽 비중을 GA로 확인해서 근거를 만듭니다.
그럼에도 필요하면(공공기관·금융 등) 이렇게 갑니다.
| 못 쓰는 것 | 대체 |
| --- | --- |
| CSS 변수 | Sass 변수로 컴파일 또는 PostCSS 폴백 |
| Grid | 구버전 문법 부분 지원. Flex 또는 float 폴백 |
| `:is()` `:where()` | 셀렉터 전개 |
| `gap` (flex) | margin + `:not()` |
| ES6+ | Babel 트랜스파일 + polyfill |
**전략은 점진적 향상입니다.** IE에는 동작하는 기본형을, 모던 브라우저에는 향상된 경험을. `@supports`로 분기합니다.
▸ **JWA가 CSS 변수 493회, `:is()` 74그룹을 쓰고 있어서 IE 대응이 필요하면 상당한 재작업**이 됩니다. 프로젝트 초기에 지원 범위를 확정하는 게 중요한 이유입니다.

### 244. Safari에서만 깨진다
**A.** 진단 순서입니다.
① **Safari 특유 이슈부터 의심** — `gap` in flex(구버전), `100vh`, `position: sticky` + `overflow`, `backdrop-filter`, 날짜 파싱(`new Date('2026-08-17')` 형식)
② **caniuse에서 사용 속성 지원 확인**
③ **실기기 확인** — macOS Safari와 iOS Safari가 또 다릅니다. 시뮬레이터가 아니라 실기기
④ **`@supports`로 폴백 분기**
⑤ 원격 디버깅 — macOS Safari 개발자 도구로 iOS 기기 연결
▸ 정직하게: "Safari 실기기 테스트 환경이 제한적이었던 게 제 검증의 약점입니다."

### 245. iOS `100vh` 문제 🔥
**A.** iOS Safari에서 `100vh`는 **주소창이 숨겨진 상태의 높이**를 기준으로 합니다. 주소창이 보이는 초기 상태에서는 화면보다 커져서 하단이 잘리고 스크롤이 생깁니다.
**해결 (최신 순)**
```css
.hero { height: 100vh;   /* 폴백 */
        height: 100dvh;  /* dynamic viewport height */ }
```
- `dvh` — UI가 접히고 펴짐에 따라 **동적으로 변함**. 대부분의 경우 정답
- `svh` — small viewport height. UI가 보이는 상태 기준(가장 작음)
- `lvh` — large viewport height. UI가 숨겨진 상태 기준(= 기존 `100vh`)
구형 대응은 JS로 `--vh`를 계산해서 넣는 방식이었습니다.
```js
const setVH = () => document.documentElement.style
  .setProperty('--vh', `${window.innerHeight * 0.01}px`);
```
▸ `dvh`는 스크롤 중 높이가 계속 변해서 레이아웃이 흔들릴 수 있습니다. 히어로처럼 한 화면이면 `svh`가 안정적인 경우도 있습니다.

### 246. "PC에서만 잘 보이면 된다"는 고객사 설득
**A.** **의견이 아니라 숫자로 설득합니다.**
① **해당 업종의 모바일 트래픽 비중 제시** — 대부분 과반입니다. 기존 사이트가 있으면 GA 데이터를 요청
② **검색 노출 영향** — 모바일 친화성이 검색 순위 요소입니다. 모바일 대응이 없으면 유입 자체가 줄어듭니다
③ **비용 구조 설명** — 나중에 반응형을 얹는 비용이 처음부터 하는 것보다 훨씬 큽니다. **제가 실제로 겪었습니다.** HOOT UP은 데스크탑 157페이지를 완성한 뒤 반응형에 들어갔는데, 헤더가 복제된 구조라 공통 컴포넌트를 정의하고 전 파일에 일괄 반영하는 방식이 됐습니다. 처음부터 반응형으로 갔으면 이 회차가 없었습니다
④ **그래도 안 되면 범위를 좁혀 제안** — "전체는 어렵지만 메인과 핵심 전환 페이지만이라도"
▸ **결정은 고객사가 합니다.** 다만 리스크를 문서로 남겨두는 것까지가 제 일이라고 봅니다.

### 247. WA 마크 대상 — 무엇부터 점검하나
**A.** **자동 검사로 잡히는 것부터 걷어내고, 수동 항목에 시간을 씁니다.**
| 순서 | 항목 | 방법 |
| --- | --- | --- |
| 1 | 마크업 유효성 | W3C validator 전수 |
| 2 | `alt` 누락 · 빈 링크 · 레이블 누락 | axe / 자동 |
| 3 | 명도 대비 | Lighthouse (**배포본 기준** — 129번) |
| 4 | 키보드 전체 탐색 | 수동. 탭만으로 전 기능 도달 가능한지 |
| 5 | 스크린리더 낭독 | NVDA 수동 |
| 6 | 제목·랜드마크 구조 | `h1` 유일성, `main`/`nav`/`footer` 존재 |
| 7 | 동적 콘텐츠 안내 | `aria-live` 배선 |
**6번이 제 실패 사례입니다** — JWA `<main>`이 392페이지 중 2페이지뿐이었습니다. 자동 도구는 `main`이 없어도 오류로 잡지 않는 경우가 있어서 **체크리스트로 따로 세야 합니다.** 세고 나서 392/392로 채웠습니다.

### 248. 레거시 CSS 20,000줄 + `!important` 투성이 — 첫 주에 뭘 하나 🔥
**A.** **고치지 않습니다. 먼저 잽니다.**

**1~2일차 — 측정**
- 파일별 줄 수, 셀렉터 수, `!important` 개수, 하드코딩 색상 수를 집계 스크립트로 고정
- 어느 파일이 어느 페이지에서 로드되는지 매핑
- **미사용 CSS 비중** 확인 (Chrome Coverage 탭)

**3~4일차 — 지도 그리기**
- 컴포넌트 구획을 식별. 어디까지가 재사용이고 어디가 페이지 전용인지
- 명시도가 높은 구간 목록화 — `!important`가 왜 붙었는지 원인 추적. 대개 **테마 스코프의 포괄 규칙**이 원인입니다(102번의 `.desk-page a` 사례와 같은 구조)

**5일차 — 안전망부터 제안**
- 시각 회귀 테스트 도입 제안. **이게 없으면 아무것도 못 건드립니다**
- stylelint를 **경고 모드로만** 붙임 — 신규 코드의 유입을 먼저 막습니다

**그 다음이 리팩터링입니다.** 순서는 ① 신규 유입 차단 → ② 안전망 → ③ 구획 단위 정리.
▸ **JWA에서 15,208 → 8,812줄을 한 경험이 이 답의 근거입니다.** 다만 그때 제가 안 한 게 안전망이었고, 그래서 스타일 유실 3건이 났습니다. **같은 실수를 반복하지 않겠다는 게 이 순서입니다.**

### 249. 로딩이 느리다는 컴플레인
**A.** ① **어디가 느린지 특정** — 특정 페이지인지 전체인지, 특정 지역·기기인지 ② **Lighthouse + Network 탭** 실측. 체감이 아니라 지표로 ③ **병목 분류** — 전송량(이미지·폰트) / 요청 순서(렌더 블로킹) / 실행(JS) / 서버 응답(TTFB) ④ **비용 대비 효과 순으로 처리** — 대개 이미지가 1순위입니다 ⑤ **개선 후 재측정.** 리포트상 절감 예상치와 실제가 다릅니다
▸ **실제 사례**: HOOT UP M11의 폰트 렌더 차단 1,590ms는 리포트상 절감 시간이 0으로 나와서, **적용 후 재측정이 필요하다고 문서에 적어뒀습니다.** 예상치를 성과로 보고하지 않는 게 원칙입니다.

### 250. 배포 후 특정 페이지에서만 스타일이 안 먹는다
**A.** 확인 순서입니다.
① **캐시** — 하드 리로드, 시크릿 창. 가장 흔한 원인
② **경로** — 절대/상대 경로. **제가 실제로 겪은 사고입니다.** GitHub Pages 하위 경로 배포에서 `href="/"`가 도메인 루트로 가서 157페이지 로고가 전부 404였습니다(DEF-01)
③ **파일이 실제로 배포됐는지** — 배포 로그, 네트워크 탭 200/404 확인
④ **대소문자** — 로컬(macOS)은 대소문자 무시, 서버(Linux)는 구분. `Style.css`와 `style.css`
⑤ **명시도 충돌** — 그 페이지에만 있는 스코프 클래스가 덮고 있는지
⑥ **HTML 오류** — 닫히지 않은 태그가 뒤 구조를 무너뜨림. validator로 확인
▸ ②와 ④가 **로컬에서는 절대 안 나오고 배포에서만 나오는** 유형입니다.

### 251. 급한 수정인데 원인 파악이 안 된다
**A.** ① **영향 범위와 긴급도를 먼저 공유** — 혼자 붙들고 있는 게 최악입니다 ② **되돌릴 수 있으면 되돌립니다.** 원인 규명보다 서비스 정상화가 먼저 ③ **재현 조건을 좁힙니다** — 특정 브라우저? 특정 데이터? 특정 경로? ④ **이분 탐색** — 최근 커밋을 절반씩 되짚습니다(`git bisect`) ⑤ **막히면 물어봅니다.** 30분 룰을 스스로 정해두는 게 좋다고 봅니다
▸ 정직하게: "혼자 작업하면서 가장 아쉬웠던 게 이 지점입니다. 물어볼 사람이 없어서 오래 붙들고 있었던 적이 있습니다."

### 252. 6개월 뒤 다른 사람이 유지보수한다면 무엇을 남기나
**A.** 이건 제가 실제로 만들어둔 게 답입니다.
| 남기는 것 | 실제 산출물 |
| --- | --- |
| **왜 그렇게 했는가** | ADR — JWA 8건, HOOT UP 9건. 결정·근거·트레이드오프 3열 |
| **계획과 실물의 차이** | Change Log — `CH-` 16건 |
| **알려진 미달·부채** | PRD 성공지표 표의 "미달" 판정 항목 |
| **컴포넌트 규약** | `[data-acc-trigger]` 같은 마크업 계약 |
| **측정 기준** | 수치의 측정 시점·방법·제외 항목 |

**가장 중요한 건 세 번째입니다.** 무엇이 안 돼 있는지를 남겨야 다음 사람이 "이건 의도인가 버그인가"를 판단할 수 있습니다. `aria-disabled`를 쓴 이유를 안 남기면 다음 사람이 `disabled`로 바꿉니다.

### 253. CMS 퍼블리싱 경험
**A.** 워드프레스로 회사 사이트를 구축·운영했습니다(약 1년). [구체 업무].
**CMS 퍼블리싱의 특징**은 내 마크업이 그대로 가지 않는다는 것입니다. 템플릿 계층(header/footer/loop)에 맞춰 쪼개야 하고, 에디터가 생성하는 마크업(`wp-block-*`)을 스타일로 감당해야 합니다.
카페24·고도몰 스킨 작업은 미경험이지만, **구조는 같다고 봅니다** — 치환 변수와 반복 구문이 들어갈 자리를 마크업으로 확보하는 일입니다. HOOT UP에서 카드 컴포넌트 하나가 152개 강의에 반복 적용되는 구조를 만든 것과 성격이 같습니다.

### 254. 쇼핑몰 스킨 작업 경험
**A.** 스킨 코드를 직접 만진 경험은 없습니다. **다만 쇼핑몰을 운영하는 쪽에 있었습니다.**
상품 등록, 기획전 페이지 구성, 상세페이지 제작을 했고, **어떤 요청이 스킨 수정이고 어떤 게 콘텐츠 작업인지 구분**할 수 있습니다. 운영자가 무엇을 자주 바꾸는지도 압니다 — 배너, 기획전, 상품 진열 순서입니다.
**그래서 그런 영역을 하드코딩하지 않는 게 중요하다는 걸 압니다.** 운영자가 못 바꾸면 결국 퍼블리셔에게 매번 요청이 옵니다.
▸ **이건 커리어 전환의 강점이 그대로 드러나는 답변**입니다. 반드시 준비하세요.

### 255. 이메일 HTML
**A.** 미경험이지만 일반 웹과 다른 점은 알고 있습니다.
| | 이메일 HTML |
| --- | --- |
| 레이아웃 | **`<table>` 기반** — flex/grid 미지원 클라이언트 다수 |
| CSS | **인라인 스타일** — `<style>` 태그가 제거되는 클라이언트 있음 |
| 이미지 | 기본 차단 다수 → **`alt` 텍스트가 실제로 보임** |
| 폰트 | 웹폰트 대부분 미지원 → 시스템 폰트 폴백 |
| 반응형 | 미디어쿼리 지원이 클라이언트마다 다름 → 유동형 테이블 |
| 테스트 | Litmus·Email on Acid 등 별도 도구 |
▸ 정직하게: "실무 경험은 없고, 필요하면 학습해서 하겠습니다. 2000년대 초 웹 방식으로 돌아가는 영역이라는 정도는 알고 있습니다."

---

# 12. mAumIo

### 256. 소개
**A.** AI 채팅 일기장입니다. 사용자가 대화하듯 일기를 쓰면 AI가 인지행동치료(CBT) 관점에서 반응하는 서비스로, Next.js로 만들고 Vercel에 배포했습니다.
**세 프로젝트 중 가장 먼저 만든 것**이고, **시안도 PRD도 없이 착수한 결과가 어떻게 되는지**를 보여주는 사례입니다. 그 진단이 HOOT UP 설계에 그대로 반영됐습니다.

### 257. Next.js를 어느 정도 다루나 / App Router vs Pages Router 🔥
**A.** **솔직히 말씀드리면 학습 중인 범위입니다.** 라우트 9개와 API 라우트 11개를 만들어 배포까지 했지만, 프로덕션 수준으로 다룬다고 말하지 않겠습니다.
**두 라우터의 차이**
| | Pages Router | App Router |
| --- | --- | --- |
| 디렉토리 | `pages/` | `app/` |
| 기본 컴포넌트 | 클라이언트 | **서버 컴포넌트** |
| 데이터 페칭 | `getServerSideProps` 등 | 컴포넌트 내 `async/await` |
| 레이아웃 | `_app.js` 단일 | `layout.tsx` **중첩** |
| 로딩·에러 | 직접 구현 | `loading.tsx` `error.tsx` 규약 |
▸ **README에 이미 선을 그어뒀습니다** — B안(Next.js 트랙)에 대해 "학습 중인 범위이며 같은 수준의 검수를 주장하지 않습니다"라고 명시했습니다 → 100번.

### 258. API 라우트 11개
**A.** 주로 [실제 확인 필요 — AI 대화 처리, 일기 CRUD, 인증 등]입니다.
▸ **면접 전 필수**: 라우트 목록을 열어서 각각 무엇인지 정리해두세요. 89번(`jwanderson.js`)과 같은 유형의 질문입니다. **수치를 말했으면 내용을 설명할 수 있어야 합니다.**

### 259. `typescript.ignoreBuildErrors: true` 🔥
**A.** **배포를 우선하려고 켠 것이고, 잘못된 선택입니다.**
이 옵션은 타입 에러가 있어도 빌드를 통과시킵니다. TypeScript를 쓰는 이유가 **컴파일 시점에 오류를 잡는 것**인데, 그걸 끄면 타입 시스템의 효용이 사라집니다. 런타임에서 터집니다.
**당시 상황을 정직하게 말씀드리면** — AI 파이프라인 첫 시도였고, 시안과 PRD 없이 착수해서 타입 정의가 정리되지 않은 채로 쌓였습니다. 배포를 막고 있는 에러를 하나씩 잡는 대신 옵션을 껐습니다.
**지금이라면** 에러 목록을 먼저 뽑고, `any`로라도 임시 타입을 명시한 뒤 옵션을 끄지 않겠습니다. `any`는 최소한 **"여기는 타입이 없다"는 표시가 코드에 남습니다.** 옵션을 끄면 아무 표시도 안 남습니다.
▸ **이 답변의 가치**: 자기 코드의 문제를 진단하고 이유까지 설명하는 것 자체가 검수 역량의 증거입니다. 변명하지 마세요.

### 260. 하드코딩 hex 554개 · 다크모드 불가 ⚠️
**A.** 사실입니다. 그리고 **이게 세 프로젝트에서 반복된 패턴**입니다.
| 프로젝트 | 상태 |
| --- | --- |
| mAumIo | 하드코딩 hex **554개** — 토큰 없음 |
| JW Anderson | hex 0건이나 **의미 토큰 참조 0회** — 최상위에서 못 바꿈 |
| HOOT UP | 시맨틱 별칭 **57.5%** — 다크모드 진입 가능 |

고치지 않은 이유는 **우선순위입니다.** mAumIo는 진단 대상으로 남겨두고, 배운 것을 다음 프로젝트에 넣는 쪽을 택했습니다. 실제로 HOOT UP은 **착수 전에** 토큰 요구사항을 PRD에 못 박고 시작했습니다.
▸ **다만 이 답변에는 약점이 있습니다.** "안 고쳤다"는 건 사실이고, 셋 다 열려 있는 프로젝트라면 우선순위 판단이 아니라 방치로 읽힐 수 있습니다. **"mAumIo는 학습 이력으로 보존하는 프로젝트"**라고 성격을 명확히 하세요.

### 261. 스트리밍 채팅에 `aria-live` 0개 ⚠️🔥
**A.** **가장 심각한 접근성 결함이고, 제가 직접 최우선으로 진단한 항목입니다.**
AI 응답이 스트리밍으로 한 글자씩 들어오는 UI입니다. 시각 사용자는 글자가 쌓이는 걸 보지만, **스크린리더 사용자는 응답이 왔는지조차 모릅니다.** 화면은 계속 바뀌는데 아무것도 안 읽힙니다.
**해결 방향은 명확합니다.**
```html
<div role="log" aria-live="polite" aria-atomic="false"> … </div>
```
다만 **스트리밍에 그대로 `aria-live`를 걸면 안 됩니다.** 한 글자마다 낭독이 발생해서 132번의 카운트다운과 같은 과다 낭독 문제가 됩니다.
**제 판단으로는 두 층으로 나눠야 합니다.**
① 스트리밍 중에는 `aria-live` 없이 조용히 채우고, 상태만 안내 — "응답을 생성하고 있습니다"
② **응답 완료 시점에 전체 텍스트를 live 영역에 1회 투입**
`aria-busy="true"` → 완료 시 `false`로 전환하는 방식도 함께 씁니다.
▸ **이 답변은 강점으로 전환 가능합니다.** 결함을 지적당한 게 아니라 **제가 먼저 최우선으로 진단해서 문서에 적어둔 항목**입니다. 그 사실을 먼저 말하세요.

### 262. mAumIo에서 무엇을 배워 HOOT UP에 반영했나
**A.** 세 가지를 그대로 옮겼습니다.
| mAumIo 문제 | HOOT UP 조치 |
| --- | --- |
| 시안·PRD 없이 착수 → 구조가 사후에 정해짐 | **사전 PRD v1.0을 착수 전 확정** + Figma를 단일 원천으로 지정 |
| 하드코딩 색상 554개 | **토큰 네이밍 규칙과 시맨틱 별칭 요구를 착수 전 명문화** |
| `aria-live` 0개 | `common.js`에 전역 live 영역 + `window.announce()` 주입, **14개 지점 배선** |
| 타입 에러 무시하고 배포 | **완료 보고를 실측과 대조**하는 절차 도입 (개수 검증 고정) |

**핵심은 "착수 전"입니다.** mAumIo에서 배운 게 **사후에 고치는 것보다 착수 전에 못 박는 게 싸다**는 것이었습니다. 그래서 HOOT UP은 요구사항 문서를 먼저 만들고 시작했습니다.

### 263. React를 실무에서 쓸 수 있는 수준인가 🔥
**A.** **아닙니다. 정직하게 말씀드리겠습니다.**
컴포넌트를 만들고 상태를 다루고 배포까지 해봤지만, 실무 수준이라고 하지 않겠습니다. README에 A안/B안을 나눈 기준이 정확히 이것입니다 — **"내가 검수할 수 있는 범위인가."**
HTML/CSS는 AI가 생성한 코드를 읽고 판단하고 반려할 수 있습니다. React·Next.js는 아직 그 수준이 아니라서, AI를 구현 도구가 아니라 **학습 도구**로 쓰고 있습니다.
**퍼블리셔로 지원하는 자리에서 React를 과장할 이유가 없습니다.** 다만 컴포넌트 단위로 사고하는 것, props로 상태를 받는 마크업을 만드는 것은 이미 하고 있습니다.

### 264. `useState`/`useEffect`
**A.** `useState`는 컴포넌트의 상태를 선언하고, 값이 바뀌면 리렌더를 유발합니다.
```jsx
const [open, setOpen] = useState(false);
```
`useEffect`는 렌더 이후 실행되는 부수 효과입니다. 의존성 배열이 바뀔 때만 재실행되고, 반환 함수는 정리(cleanup)로 언마운트나 다음 실행 전에 호출됩니다.
```jsx
useEffect(() => {
  const onKey = e => e.key === 'Escape' && setOpen(false);
  document.addEventListener('keydown', onKey);
  return () => document.removeEventListener('keydown', onKey);  // 정리
}, []);
```
▸ **정리 함수가 140번(리스너 누수)과 같은 개념**입니다. 바닐라에서 `removeEventListener`를 직접 부르던 걸 React가 라이프사이클로 관리하는 구조입니다. 이렇게 연결해서 답하면 "개념을 이해하고 있다"로 읽힙니다.

---

# 13. 압박 · 검증 질문 🔥

### 265. 화이트보드에 메가 메뉴 CSS를 써보라
**A.** 씁니다. **당황하지 말고 말하면서 쓰세요.**
```css
.gnb__item { position: relative; }

.mega {
  position: absolute; top: 100%; left: 0;
  width: 100%;
  visibility: hidden;          /* opacity:0 아님 — 포커스 제외 */
  opacity: 0;
  transition: opacity .2s, visibility .2s;
}

.gnb__item:hover .mega,
.gnb__item:focus-within .mega,
.mega.is-open { visibility: visible; opacity: 1; }
```
말할 것: ① `visibility: hidden`을 쓴 이유(포커스 순서 제외) ② `:focus-within`으로 키보드 대응 ③ 실제로는 JS로 `.is-open`과 `aria-expanded`를 함께 토글.
▸ **완벽하게 못 써도 됩니다.** 왜 그렇게 쓰는지를 말하는 게 평가 대상입니다. 143번을 준비했으면 이 질문은 오히려 기회입니다.

### 266. 새벽 커밋이 많은데 정말 혼자 했나
**A.** 혼자 했습니다. [본인 사정 — 학습 시간 확보 방식].
검증 가능한 근거를 드리면, 커밋 메시지가 **작업 대상(구획)과 성격을 담은 자체 규칙**이고, 그 규칙이 프로젝트 도중에 Conventional Commits로 바뀝니다(08-01). **외부에서 대신 작업한 이력이면 이런 전환 흔적이 안 남습니다.**
그리고 지금 이 자리에서 어느 커밋에 대해서든 왜 그렇게 했는지 말씀드릴 수 있습니다.

### 267. 문서도 AI가 쓴 건가 🔥
**A.** 네, 문서를 정리하는 과정에서 AI를 활용했습니다. 다만 문서에 들어간 수치와 판단 기준은 제가 직접 확인한 내용입니다.
PRD의 수치는 실제 프로젝트를 측정해서 넣었고, 집계 과정에서 오류가 있었던 부분도 §10.4에 수정 이력으로 남겼습니다. ADR의 트레이드오프나 미달 항목처럼 프로젝트에 불리해 보일 수 있는 내용도 실제 판단 과정이라 삭제하지 않았습니다.
결국 중요한 것은 문장을 누가 정리했느냐보다, 제가 문서에 적힌 내용을 이해하고 근거를 설명할 수 있느냐라고 생각합니다. 필요하시면 어느 항목이든 왜 그렇게 판단했는지 말씀드리겠습니다.

### 268. 미달이라고 써놓고 왜 안 고치고 냈나 🔥
**A.** 두 가지 이유입니다.
① **우선순위 판단** — 각 프로젝트에서 증명하려는 게 있었고, 남은 항목은 그 증명에 필수적이지 않다고 봤습니다. JWA의 `:focus-visible`은 접근성 축인데, 그 축은 HOOT UP에서 접근성 100으로 별도 증명했습니다.
② **더 정직한 이유** — 다 고칠 시간에 다음 프로젝트를 시작하는 게 낫다고 판단했습니다. JWA를 완벽하게 만드는 것보다, JWA의 실패를 요구사항으로 옮겨서 HOOT UP을 만드는 게 배우는 게 많았습니다.
**의미 토큰 참조 0회 → 57.5%가 그 결과입니다.**
▸ **약점 인정**: "다만 지적하신 대로 미완인 상태로 제출한 건 맞습니다. 특히 `:focus-visible`은 몇 줄이면 되는 일이라 우선순위 논리로 방어하기 어렵습니다. 이건 놓친 게 맞습니다."

### 269. 신입으로는 나이가 많고 경력으로는 경험이 없다 🔥🔥

> **이 질문이 최종 관문입니다. 반드시 준비하세요.**

**A.** 신입으로 지원하기에는 나이가 있고, 반대로 퍼블리싱 실무 경력은 없다는 점을 저도 알고 있습니다. 그래서 나이로 설명하기보다 **제가 실제로 만든 결과물과 작업 방식으로 판단받고 싶습니다.**

JW Anderson에서는 392페이지를 직접 구현하고 리팩터링 과정을 커밋으로 남겼고, HOOT UP에서는 AI를 활용한 구현 결과를 검수하면서 359건의 수정·보완 사항을 관리했습니다.
또 이전 직무에서 오랫동안 일정과 결과를 책임지는 일을 해왔기 때문에, 작업이 끝났다는 말만 듣고 넘기기보다 실제 결과를 다시 확인하는 습관이 있습니다. AI가 16개 파일을 만들었다고 했을 때 실제로는 14개였던 것을 확인한 사례나, 제 집계 오류도 문서에 수정 이력으로 남긴 것이 그런 방식의 예입니다.

퍼블리싱 실무 경험은 입사 후 더 쌓아야 하지만, **업무를 끝까지 확인하고 기록하는 태도는 기존 경력에서 가져올 수 있는 강점**이라고 생각합니다.

### 270. 왜 20대 신입 대신 지원자님인가 🔥
**A.** 다른 지원자와 단순히 나이로 비교하기보다는, 제가 가진 경험이 이 직무에서 어떻게 도움이 될 수 있는지 말씀드리는 게 맞다고 생각합니다.
저는 이전 경력에서 기획, 운영, 디자인 관련 업무를 두루 경험하면서 **정보가 서로 다를 때 기준을 정하고, 일정 안에서 우선순위를 판단하고, 결과물을 다시 확인하는 과정**을 많이 겪었습니다.
이런 경험은 퍼블리싱에서도 기획서와 디자인이 다를 때 확인해야 할 기준을 정하거나, 일정이 부족할 때 우선순위를 조율하는 데 도움이 될 수 있다고 생각합니다.
물론 마크업과 퍼블리싱 실무 경험은 제가 앞으로 더 증명해야 하는 부분입니다.

### 271. 배우는 속도가 느리면
**A.** 새로운 기술을 배우는 속도는 사람마다 차이가 있다고 생각합니다. 저는 그 차이를 **기록과 반복으로 줄이는 편**입니다.
HOOT UP에서도 검수 항목을 유형별로 정리하고, 중요한 결정은 ADR로 근거를 남겼습니다. 한 번 배운 내용은 다시 찾아볼 수 있게 정리해 두기 때문에 같은 실수를 반복하지 않도록 관리하는 것이 제 학습 방식입니다.

### 272. 단순 반복 마크업이 지루하지 않겠나
**A.** 반복 작업 자체는 크게 부담스럽지 않습니다. 오히려 반복되는 패턴을 발견하면 어떻게 공통화하고 관리할지 생각하는 편입니다.
반복이 보이면 그건 컴포넌트로 만들 신호입니다. JWA에서 카드 컴포넌트 하나를 리스트와 위시리스트가 공유하게 만든 것, HOOT UP에서 정책 페이지 5종을 `class="terms <scope>"` 컴파운드로 묶어 **완성된 terms를 한 줄도 고치지 않고** 4종이 재사용하게 만든 것이 그 결과입니다.
반복 작업을 그대로 쌓기보다 공통 패턴을 찾아 재사용 가능한 구조로 바꾸는 과정에 흥미를 느낍니다.

### 273. 퍼블리셔 직군이 사라진다는 이야기 🔥
**A.** 직군 명칭은 바뀔 수 있다고 봅니다. 프론트엔드에 흡수되거나, 마크업 개발자·UI 개발자로 불리는 곳이 늘고 있습니다.
**다만 일 자체는 남는다고 봅니다.** 사라지는 건 "시안대로 화면을 옮기는 일"이지, **접근성 기준을 세우고, 대규모 마크업의 구조를 설계하고, 디자인 시스템을 코드로 유지하는 일**은 오히려 요구가 늘고 있습니다. 접근성은 법적 요구가 강화되는 방향이고, 이건 자동화되지 않습니다.
**저는 후자를 하는 사람이 되려고 준비했습니다.** PRD·기능정의서·UI/UX 설명서를 직접 쓴 게 그 방향입니다.

### 274. AI가 퍼블리싱을 대체하면 당신의 가치는 🔥🔥

> **이 포트폴리오 전체가 이 질문에 대한 답입니다. 가장 중요한 답변입니다.**

**A.** AI가 퍼블리싱 업무의 상당 부분을 자동화할 수 있다고 생각합니다. HOOT UP에서도 실제로 164페이지 구현에 AI를 활용했습니다.
그 과정에서 제가 느낀 것은, **코드를 만드는 속도와 결과물이 실제 요구사항을 충족하는지는 별개의 문제**라는 점이었습니다. 359건의 검수 기록에서도 다음과 같은 사례가 있었습니다.
- **존재하지 않는 결과물이 완료로 보고되는 것** — "16개 완료 / 실제 14개". 결과물을 보는 게 아니라 세어봐야 나옵니다
- **"전체 적용"이 실제로는 부분 적용인 것** — 적용된 영역만 확인하면 통과합니다. 지시하지 않은 곳까지 열어봐야 합니다
- **화면상 아무 문제 없는 데이터** — 카탈로그에 없는 강의명이 그럴듯하게 생성됐습니다. **source of truth를 정해두고 대조**해야만 나옵니다

또 하나 중요하게 본 부분이 있습니다.
접근성 지표는 1페이지에서 157페이지까지 한 번도 안 무너졌는데, 하드코딩 색상은 2 → 21 → 88 → 121건으로 늘었습니다. 같은 프로젝트인데 한 축은 지켜지고 한 축은 밀렸습니다.
차이는 관리 방식에 있었습니다. 접근성 항목은 템플릿과 공통 구조에 포함되어 반복해서 유지됐지만, 색상 값은 페이지를 만들 때마다 개별 판단이 들어가면서 점차 늘어났습니다.

이 경험을 통해 품질 기준은 개인이 매번 조심하는 것보다 린트나 CI처럼 반복적으로 확인할 수 있는 구조로 만드는 것이 중요하다고 배웠습니다. 어떤 항목을 자동화할 수 있고, 어떤 부분은 사람이 직접 검토해야 하는지 구분하는 역량을 더 키워가고 싶습니다.

AI 활용이 늘어날수록 결과물을 검증하고 기준을 관리하는 역할도 더 중요해질 것이라고 생각합니다.

### 275. 가장 큰 약점
**A.** 가장 큰 약점은 **실무에서 여러 사람이 함께 관리하는 코드베이스를 경험해보지 못했다는 점**입니다.
제 코드는 제 규칙 안에 있어서 일관성이 유지됩니다. 실무는 여러 사람의 규칙이 섞이고, 레거시가 있고, 제 판단이 틀렸을 때 지적받습니다. 그 경험이 없습니다.
혼자 작업하면서는 **검증 도구를 리뷰 보조 수단으로 활용했습니다** — validator, Lighthouse, axe. 하지만 도구는 "이 구조가 팀 컨벤션에 맞는가"를 말해주지 않습니다. **이건 팀에 들어가서만 배울 수 있는 부분이라고 생각합니다.**
▸ 약점 답변의 원칙: **직무와 무관한 약점을 대지 말 것**("완벽주의라서요" 금지). 진짜 약점 + 보완 노력 + 남은 부분 인정.

### 276. 가장 부끄러운 부분

**A.** 수치 하나가 아니라 **반복된 습관**입니다. **측정하지 않고 '달성'이라고 쓴 것**입니다.

세 번 같은 일을 했습니다.

| 항목 | 무엇을 했나 |
| --- | --- |
| 컬러 하드코딩 | hex·rgb 두 축만 재고 "0건 달성"이라고 씀. **색상 키워드 19건**이 남아 있었음 |
| 랜드마크 | `header`/`nav`/`footer`를 썼다는 이유로 "시맨틱 — 적용". **`<main>`은 392페이지 중 2페이지**였음 |
| 명도 대비 | 모노톤 팔레트라 확보됐을 거라고 **가정**. 측정 안 함 |

**공통점이 명확합니다 — 기준을 좁게 잡고 선언을 넓게 했습니다.** 셋 다 지금은 해소했지만(키워드 0건 · `main` 392/392 · 대비 10.85:1), 문제는 결과가 아니라 **제가 그걸 몰랐다는 것**입니다. 누가 물어보지 않았으면 계속 "달성"인 채로 있었을 항목들입니다.

**그리고 같은 실수를 HOOT UP에서도 했습니다.** 하드코딩 색상을 180건이라고 공표했는데 실제로는 205건이었습니다. 집계 기준은 두 축으로 정해놓고 검산은 한 축으로만 돌렸습니다(§10.4 MEA-01).

**부끄러운 건 "AI 보고는 세 번 대조하면서 내 숫자는 안 셌다"는 점**입니다. 검수를 주장하는 포트폴리오에서 이건 자기모순입니다. 그래서 지금은 집계 스크립트를 고정하고 커밋별로 소급 적용합니다.

▸ **또 하나 남아 있는 것** — 원본 이미지를 Git에 그대로 커밋한 건 되돌릴 수 없습니다. 지금 WebP로 전환해 게시분은 531MB지만 **히스토리에는 원본이 영구히 남습니다** → 231번.

### 277. 오늘부터 다시 만든다면
**A.** 세 가지를 바꾸겠습니다.
① **토큰 체계를 착수 전에 확정** — JWA는 리팩터링 중반에 토큰을 넣어서 의미 토큰 참조가 0회가 됐습니다. 순서가 틀렸습니다
② **안전망을 먼저** — 시각 회귀 테스트와 stylelint를 첫날 붙입니다. JWA 스타일 유실 3건은 안전망이 없어서 육안으로 잡은 것입니다
③ **레이아웃을 Grid로** — `calc(50% - 18px)`과 `:not(:nth-of-type(Nn))` 조합 대신 `repeat(auto-fill, minmax())` + `gap`. 5구간 미디어쿼리의 상당 부분이 사라집니다
▸ **공통점은 "나중에 고치는 것보다 처음에 정하는 게 싸다"**입니다. 세 프로젝트에서 반복 확인한 결론입니다.

### 278. 수습 후 전환이 안 될 수도 있다
**A.** 이해합니다. 저를 검증하실 시간이 필요한 것도 당연하다고 생각합니다.
제가 드릴 수 있는 건 **3개월 안에 판단 가능한 결과물을 만드는 것**입니다. 두 프로젝트 다 착수부터 완성까지 한 달이 안 걸렸고, 그 과정을 문서로 남기는 게 제 방식입니다. **무엇을 했는지 애매하지 않게 보여드리겠습니다.**

---

# 14. 역질문

> **2~3개만 준비하세요. 많이 물으면 면접이 아니라 상담이 됩니다.**
> 순서는 ① 업무 범위 → ② 팀 방식 → ③ 성장. 연봉·복지는 마지막이거나 인사팀 대상입니다.

### 279. 퍼블리셔 담당 범위
**질문 그대로**: "퍼블리셔가 담당하는 범위가 어디까지인지 궁금합니다. 마크업까지인지, 인터랙션 스크립트나 접근성 검수까지 포함되는지요?"
▸ **왜 좋은 질문인가**: 직무 이해도를 보여주면서 실제로 알아야 하는 정보입니다.

### 280. 코드 컨벤션
**질문 그대로**: "팀에 마크업·CSS 컨벤션 문서가 있는지, 있다면 어떻게 관리하시는지 궁금합니다."
▸ 없다고 하면: "제가 정리해서 제안드려도 될까요?" → 기여 의사 표현.

### 281. 디자인 시스템 · 토큰
**질문 그대로**: "디자인 시스템이나 토큰 체계를 운영하고 계신지, 퍼블리셔가 그 유지에 참여하는지 궁금합니다."
▸ **본인 강점과 직결되는 질문**입니다. 토큰 이야기가 나오면 자연스럽게 경험을 덧붙일 수 있습니다.

### 282. 접근성 요구 수준
**질문 그대로**: "접근성을 어느 수준까지 요구하시는지, 자동 검사를 CI에 붙이고 계신지 궁금합니다."

### 283. Git 직접 다루는지
**질문 그대로**: "퍼블리셔가 Git으로 직접 커밋·PR을 올리는 구조인지, 개발자에게 파일로 전달하는 구조인지 궁금합니다."
▸ 실제 업무 흐름을 파악하는 실용적 질문입니다.

### 284. 3개월 기대 성과
**질문 그대로**: "입사 후 3개월 안에 기대하시는 성과가 무엇인지 알고 싶습니다."
▸ 실무 기대치를 확인할 수 있어서 우선순위가 높은 질문입니다. 의욕을 보여주면서 실제 판단 기준을 알 수 있습니다.

### 285. AI 도구 정책
**질문 그대로**: "팀에서 AI 코딩 도구 사용에 대한 정책이 있는지 궁금합니다."
▸ **주의**: HOOT UP 이야기를 한 뒤에 물으면 "AI 없이는 못 하나"로 읽힐 수 있습니다. **면접 흐름을 보고 판단하세요.** 애매하면 빼는 게 안전합니다.

### 286. 프론트엔드 성장 경로
▸ ▸ 이 질문은 회사가 퍼블리셔 직무의 장기 성장 경로를 어떻게 보는지 확인하고 싶을 때만 사용하는 편이 좋습니다. 퍼블리셔로 뽑는 자리에서 "프론트엔드 가고 싶다"로 읽힙니다.
꼭 묻겠다면 이렇게: "퍼블리싱 역량을 더 깊게 쌓을 수 있는 방향이 어떤 게 있을지 궁금합니다." — **깊이 방향**으로 물으세요.

---

# 부록. 최종 점검

## 면접 전날 반드시 확인할 것

| 항목 | 왜 |
| --- | --- |
| `jwanderson.js` 509줄 — 기능정의서로 답변 완성됨 | **89번** — 6개 덩어리 암기 |
| mAumIo API 라우트 11개 목록 정리 | **258번** — 같은 이유 |
| 회사 사이트 개발자 도구로 관찰 1개 | **3·4번** — "잘 만드셨더라"는 감점 |
| NVDA 30분 사용 | **208번** — 이 질문이 강점으로 바뀜 |
| 희망 연봉 범위 확정 | **7번** — 즉답 못 하면 협상력 상실 |
| 통근 시간 실측 | **5번** |

## 무너지면 안 되는 5문항

```
93.  AI가 다 짰다면 실력은 뭘로 증명하나
269. 나이는 많고 경력은 없다
274. AI가 퍼블리싱을 대체하면
40.  왜 15,208줄을 기준선으로 잡았나
89.  jwanderson.js 509줄에 뭐가 들었나
```
> 63번(white 키워드)은 PRD에 3축 분리로 공개돼 위험 문항에서 빠졌습니다.
> 대신 89번이 들어갔습니다 — 수치를 말해놓고 내용을 못 대면 267번(문서도 AI가 썼나)으로 직결됩니다.

## 답변 원칙 3가지

**① 미달 항목은 먼저 꺼낸다.**
지적당한 뒤 인정하면 방어이고, 먼저 말하면 자기 검증 능력입니다. 같은 사실인데 결과가 반대입니다.

**② 수치를 말했으면 근거를 댄다.**
"42% 줄였습니다"까지만 말하면 의심받고, "기준선을 7/20으로 잡은 이유는 반응형이 끝난 시점이기 때문이고, 첫 커밋 대비로는 15%입니다"까지 가면 신뢰가 됩니다.

**③ 실패는 항상 다음 프로젝트로 연결한다.**
```
JWA 의미 토큰 0회        → HOOT UP 착수 전 요구사항 → 57.5%
JWA 이미지 962.8MB       → WebP q85 전환 → 92.2MB (−90.4%)
JWA :focus-visible 0건   → reset 전역 선언으로 자력 해소
JWA main 2/392           → 측정 후 자력 해소 → 392/392
JWA 색상 키워드 19건      → 축 분리 후 치환 → 0건
JWA title 32중복         → 파일명 규칙으로 역산 → 392/392 고유
JWA 이미지 962.8MB       → WebP q85 전환 → 92.2MB (−90.4%)
JWA @import 7단          → 재측정 후 "유지" 결정 → ADR-09
mAumIo 시안·PRD 없음     → HOOT UP 사전 PRD v1.0 확정
mAumIo aria-live 0개     → HOOT UP 14개 지점 배선
```
**이 표가 이 면접의 핵심 서사입니다.** 어떤 약점을 지적당해도 이 표의 한 줄로 답할 수 있습니다.

---

## JW Anderson 즉답 수치 (2026-08-20 재검증본)

| 항목 | 수치 |
| --- | --- |
| 페이지 | **392** — 상세 320 · 목록 48 · 랜딩 5 · 주문 · 계정 등 |
| CSS | 15,208 → **8,812줄** (−42.1%) · M6 후속 반영 **현재 8,806줄** / 2,033 → **1,184룰** (−41.8%) |
| global / page / 기타 | 5,312→2,780 (−47.7%) / 9,732→**5,852** (−39.9%) / reset·module·import 164→**174** |
| 커밋 | **105건** (9일) · 이후 작업 포함 **124** · CLI 122 / 웹 2 · Conventional 규격 통과 15 |
| 팝업·패널 | **39종** · `data-panel` = `id` 1:1 계약 |
| 상품 카드 | **1,819회** 재사용 · 예외 1곳(위시리스트 하트) |
| 이미지 호버 교체 | **655쌍** · `{name}.png ↔ {name}_hover.png` |
| alt | **13,502 / 13,502 = 100%** |
| UI/UX 설명서 | **70개 항목 · 10개 카테고리** |
| FAQ | 8개 주제 · **20개 Q&A** 직접 작성 |
| 랜드마크 | header·nav·**main**·footer 각 **392 / 392** |
| CSS 변수 참조 | **493회** — 색상 149 · 여백 323 · 역할 21 |
| 상태 클래스 | **7종만** · 모바일 추가에도 신규 0 |
| z-index | **5단계 고정** 0·1·10·50·100 / px 붙은 값 2건 발견·수정 |
| 애니메이션 | **2종만** 0.35초 · 1초 |
| 콘텐츠 폭 | Desktop 1170 / Tablet 720 / Mobile 100% |
| 브레이크포인트 | ~767 / 768–1279 / 1280–1500 / 1501–1918 / 1919+ |
| 명도 대비 | `#333` on `#f3ede3` = **10.85:1** |
| 이미지 | PNG 962.8MB → **WebP 92.2MB** (−90.4%) · 3,411장 · q85 · 참조 깨짐 0 |
| lazy | **11,587 / 13,502** (첫 화면 1,915 의도적 제외) |
| `:focus-visible` | reset 전역 · `outline 2px var(--gs06)` · offset 2px |
| title | **392 / 392 고유** (중복 0종) |
| 저장소 | **531MB** (WebP 92 · 영상 439) / GitHub Pages 1GB |
| 미달 (M6 진행 중) | **의미 토큰 참조 0회** · 메가메뉴 키보드 조작 미착수 · NFR-CODE-04 진행 중 |
| 판단 (미달 아님) | `@import` 7단 유지 — **ADR-09** |
| 새로 드러난 결함 | 체크박스 토글이 `display:none` → **키보드 탭 순서 제외** |
| 미사용 토큰 | 15 → **12개** (`--rd01` `--sp90` `--sp120` + 역할 9종) |
| 해소됨 | 색상 키워드 19→0 · `main` 2/392→392/392 · title 32중복→0 · lazy 0→11,587 · `:focus-visible` 0→전역 · 이미지 −90.4% |

---

> **13년간 팔리는 상품을 만들었고, 지금은 팔리는 화면을 코딩합니다.**
> JW Anderson은 직접 짓는 능력, HOOT UP은 지어진 것을 검증하는 능력입니다.
> 그리고 두 프로젝트 모두, 제가 스스로 미달이라고 적어둔 항목이 있습니다.
